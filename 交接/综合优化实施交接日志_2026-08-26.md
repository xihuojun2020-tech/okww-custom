# 综合优化实施交接日志

日期：2026-08-26
当前版本：1.20.02（A3/A4 实机与候选构件复验均已通过）

## 1.20.02：慢速双重核验适配（2026-08-31）

- 1.20.01 重测已验证 A3 当前选中时会跳过重复点击并成功登录，任务详情只显示 A3/A4 短名。
- A4 列表项点击已正确投递，第一次核验也确认当前账号与目标完全一致；但窗口切换加两次 OCR 超过原 8 秒，第一次正确采样出现时等待刚好到期。
- 稳定核验等待扩展为 20 秒，仍保留“列表已收起 + 连续两次目标一致”的安全条件；不降低校验强度，不增加盲点次数。
- 1.20.02 隔离实测于 2026-08-31 01:31:25 开始，01:41:34 结束；A3→A4 完整序列连续执行 2 轮，两轮均通过，未运行每日任务、未写完成进度。
- 两轮均完成账号选择稳定核验、登录、退登和下一账号切换。窗口消息点击未触发登录时，既有保护逻辑刷新 OCR 后改用系统屏幕点击并成功；身份核验条件未放宽。
- 测试状态输出只出现 A3/A4 短名，最终状态为“测试通过”；手机号、备用名、UUID 和特征码未进入状态 UI。
- 实测日志：`E:\okww-candidate-1.20.00\data\apps\okww-custom\working\logs\ok-script.log`。引用日志时必须继续脱敏，不得复制原始账号身份。
- 实测后关闭候选 UI，游戏保留在开放世界安全、可恢复界面。原打包版账号配置基线 326 项再次逐文件校验：缺失 0、变化 0、额外 0；`logs` 和 `screenshots` 运行证据不属于配置基线。
- 本地编译、版本一致性、差异检查、单元/集成/UI/图像/故障注入断言均通过。游戏窗口保持开启时，测试框架曾在已输出 `OK` 后出现一次 OCR 后台线程退出竞态；失败文件单独重跑及其余图像文件均通过，未修改生产逻辑，最终仍以 GitHub 候选流水线的干净进程结果为发布门禁。
- 代码提交 `78e8eae` 已推送到 `master`。GitHub Actions 候选 Run `33326613512` 的版本校验、完整测试、打包、包烟测和 SHA-256 全部成功；未创建正式标签或 Release。
- 最终构件下载到 `E:\AI work\okww-candidates\1.20.02-78e8eae`。本地复核 `SHA256SUMS.txt` 的 5 项文件全部一致，`package_smoke.py` 通过。
- 全新隔离安装槽为 `E:\okww-candidate-1.20.02`。PyAppify 1.2.3 安装后仍回落到旧正式标签 1.19.12；关闭启动器后显式检出 `78e8eae`，以隔离 Python 直接启动 working，窗口标题和页面版本均为 1.20.02。
- 无账号配置启动时选择“退出并保持安全模式”，候选槽未生成 `account_master_config.json` 或 `daily_profiles.json`。最终再次校验正式打包版 326 项账号配置：缺失 0、变化 0、额外 0。
- 正式标签与 Release 尚未创建；如决定正式发布，应以当前 1.20.02 代码和上述候选证据为准执行正式标签流水线。

## 1.20.01：A3/A4 候选实机修复（2026-08-31）

- 1.20.00 候选提交 `5b6bba0` 已通过 GitHub Actions 候选流水线 `33320437066` 的测试、打包、包烟测和 SHA-256 校验；候选构件下载到 `E:\AI work\okww-candidates\1.20.00-5b6bba0`，隔离安装槽为 `E:\okww-candidate-1.20.00`。
- 候选 UI 已验证固定浅色、通用设置/账号设置/任务/活动/测试功能五页、页面平铺和 UI 无日志正文。无账号配置启动时选择“退出并保持安全模式”，未锚定或创建总配置。
- 现有打包版 `E:\game\okww owener` 的 326 个非日志 JSON 在候选安装前后逐文件 SHA-256 完全一致：缺失 0、额外 0、变化 0。
- 只读复制现有配置到隔离槽后执行 A3→A4、2 轮账号切换测试。首轮在 A3 已经是登录器当前选项时重复点击 A3，列表未收起，稳定核验超时并重试；人工停止后没有继续每日任务。
- 根因修复位于共享 `_select_account_with_retry`：列表已收起且当前账号已匹配目标时直接确认，不再重复打开和点击当前项；正式多账号任务和测试入口复用同一逻辑。
- 同时修复任务详情泄露旧账号标签：状态只允许短名，无法提取短名时显示“账号”；手机号、带星号手机号、U…A 备用名和特征码均不进入 UI 状态。
- 原版 OK-WW 对照版本仍记录为 `3.5.17`；游戏内特征码继续只保存，不参与本次切换判断。
- Android/MuMu 未提交文件保持原样，未纳入 PC 1.20.01。

### 候选安装限制

- PyAppify 1.2.3 的离线 setup 会克隆仓库并检出最新正式标签；没有正式 `v1.20.00` 标签时，1.20.00 候选安装包实际安装 `v1.19.12`。
- 本次未提前创建、移动或临时改写正式标签；隔离槽保留 `working-1.19.12-package-baseline`，再显式检出固定候选提交用于验收。

## 1.19.02：每日任务凝素领域名称（2026-08-26）

- 每日任务中的“凝素领域选择”从普通整数输入改为整数值下拉框；界面显示第 1～4 项游戏内名称，配置仍保存整数序号。
- 第 5～20 项显示“第 N 个凝素领域（待命名）”，本次不猜测其余名称，后续按用户提供的名称继续补充。
- 新增 `src/gui/LabelAndIntegerDropDown.py` 和 `integer_drop_down` 工厂类型；运行时通过启动阶段的 `custom_ok` 同步机制安装到 ok-script。
- i18n：五个现有语言目录已同步四个名称条目并重新编译可用的 `.mo` 文件。
- 验证：凝素领域/账号配置/完整性/管理页回归 66 项通过；i18n check/compile、`compileall` 和 `git diff --check` 通过。

## 1.19.01：配置页面右侧空白修复（2026-08-26）

- 根因：`AccountSettingsTab` 将 `CustomTab.view` 重新挂载到 `SectionPanel` 时未解除原 `QScrollArea` 的所有权，旧滚动区继续把视图维持在约 640px 的尺寸提示，造成右侧大面积空白。
- 修复：嵌入前调用 `takeWidget()`；`SectionPanel.add_widget()`、嵌入内容、账号配置根容器和序列配置根容器显式设置水平 `QSizePolicy.Expanding`。
- 影响范围：通用设置、账号设置、任务、活动、测试功能的分区内容都能使用可用宽度；保留外层滚动，不新增内层滚动。
- 验证：聚焦 UI/导航/可用性测试 17 项通过；`compileall` 通过；全量 `Test*.py` 共 409 项，其中 380 项通过、29 项为既有图像样本 `FinishedException`、8 项跳过，不是本次布局改动引入。
- 提交前额外离屏实测：账号设置嵌入视图宽度由约 638px 扩展到约 1092px（1124px 分区宽度，扣除边距）。

## 基线记录

- 本轮综合优化按 `docs/superpowers/specs/2026-08-26-comprehensive-hardening-design.md` 和对应实施计划执行。
- 当前账号安全、配置发布、实时联动和固定浅色 UI 改动已在 `28ea637`（`v1.18.00`）完成。
- 本阶段新增测试分组：`unit`、`integration`、`ui`、`image`、`fault_injection`。
- 运行目录 `账号备份`、`config_bundle_transactions`、`config_integrity_incidents`、Android 构建目录及 `.superpowers` 已加入忽略规则，避免账号隐私和构建产物进入版本库。

## 测试基线

- 账号/序列/运行时聚焦回归：33 项通过。
- 全量测试：386 项；29 项为既有图像测试 `FinishedException`，8 项跳过。
- 新增的确定性 `unit` 分组已通过。
- 图像测试暂不阻塞核心安全与配置发布验证，待后续逐项补齐资源和环境。

## 阶段 2：账号身份保护（已完成）

- 普通编辑器锁定 `profile_id`、`phone`、`masked_phone`、`nickname`、`alternate_login_name`、`game_feature_code`、`account_aliases` 及旧版身份别名。
- 新增 `src/account_rebind_service.py`：当前身份确认、身份冲突检查、显式确认、账号级备份和 CAS 发布。
- 账号配置页身份输入框只读，新增“重新绑定身份”按钮；普通任务保存不会覆盖身份字段。
- 阶段 2 聚焦回归：`tests.TestAccountIdentityProtection`、`tests.TestAccountConfigEditor`、`tests.TestAccountManagementTabs`、`tests.TestAccountIdentity` 共 20 项通过。

## 阶段 3：本机备份保护（进行中）

- 新增 `src/secure_backup.py`，使用 Windows 当前用户 DPAPI 加密/解密敏感快照；DPAPI 不可用时明确报错，不回退明文。
- 新增恢复路径校验，拒绝 `..` 穿越、符号链接组件和配置根目录外的源/目标；`ConfigBackupService.restore()` 已接入校验。
- `AccountRepository.backup_profile()` 现在保留兼容元数据 JSON，并将完整账号快照写入同名 `.dpapi` 加密旁车文件；DPAPI 不可用时敏感备份直接拒绝。
- 恢复日志阶段扩展为 `prepared → verified → activated → mirrored`，并保留旧 `old_moved/new_moved` 阶段的兼容恢复逻辑。
- `ConfigBackupService` 默认在 Windows 备份根目录应用当前用户、SYSTEM 和 Administrators ACL；可通过 `harden_permissions=False` 为兼容性显式关闭。阶段 3 的路径/权限边界已完成。

## 阶段 4：active bundle 图存储（进行中）

- 新增 `src/account_graph_store.py`，提供 active bundle 的只读完整账号/序列图和 CAS 发布接口。
- `AccountRepository` 的 active 快照读取已改用图存储边界；旧工作副本仍仅作为兼容投影。
- `tests.TestAccountGraphStore`、发布服务、仓库、序列和配置包回归共 39 项通过，包含镜像中断后 active 修订号保持不变的测试。
- 尚未完成：将所有序列/任务写入调用统一委托图存储，并增加中断镜像恢复状态机。
- 发布服务现已暴露 `PublishState`：写入清单后进入 `verified`，目录激活进入 `activated`，投影镜像完成后进入 `mirrored`；镜像异常时 active 指针保持旧版本。
- `AccountRepository._publish_master()` 已通过 `AccountGraphStore.publish()` 发布完整账号图；序列和账号的现有写入入口因此共享同一 active bundle/CAS 边界。

## 阶段 5：运行时服务契约（进行中）

- 新增 `src/runtime/account_selection_service.py`：统一使用带星号手机号优先的身份解析。
- 新增 `src/runtime/sequence_snapshot_service.py`：复用 `SequenceRepository` 创建不可变运行快照。
- 新增 `src/runtime/task_run_coordinator.py`、`task_status_model.py` 和 `login_flow_service.py`；登录适配器不复制 OCR/点击逻辑。
- `tests.TestRuntimeServices` 与账号切换/多账号任务回归共 69 项通过。
- 尚未完成：把 `DailyTask`、`MultiAccountDailyTask` 和 `TestAccountSwitchTask` 的运行入口全部接入协调器状态机。
- `MultiAccountDailyTask.create_run_snapshot()` 已改用 `SequenceSnapshotService`，并在创建快照后登记协调器；提供 `request_coordinated_stop()` 保持快照不变。

## 一次性整合补充

- 新增 `src/observability.py`：统一脱敏手机号、令牌、Cookie、登录 URL，并提供关联上下文和安全调用结果。
- 新增 `src/gui/AccountFilterBar.py`、账号页 `dirty/selected_profile_id/show_redacted_diff/reset_task_field` 接口，补充账号筛选模型。
- 版本同步测试已更新至 `1.19.02`。
- 序列管理页成员列表已改为内容自适应高度并隐藏滚动条，保留上下移动和当前行选择功能；账号较多时由外层页面统一承载布局。
- 序列选择列表、每日任务配置方案列表也已改为内容自适应高度；角色代码等高数量浏览列表保留滚动，以免页面高度失控。
- 聚焦测试：`tests.TestSecureBackup`、`tests.TestConfigBackup`、`tests.TestConfigIntegrity`、`tests.TestAccountConfigBundle` 共 77 项通过。
- 尚未完成：备份目录 ACL、所有导出/恢复入口统一接入加密服务及分阶段事务标记；完成后再关闭本阶段。

## 后续交接顺序

1. ~~完成身份字段只读和后端锁定，禁止普通编辑修改身份。~~（阶段 2 已完成）
2. 再完成 Windows DPAPI 本机备份保护与恢复路径校验。
3. 将 active bundle 收敛为运行时唯一真源。
4. 拆分账号选择、快照、登录和任务协调服务。
5. 完成账号/序列/任务页面的搜索、筛选、状态和可恢复操作。
6. 最后进行错误处理、诊断导出、CI 分组和 `1.19.02` 发布验收。

## 注意事项

- 不要提交 `configs/`、`账号备份/`、事务目录或 Android `build/` 产物。
- `masked_phone` 仍是切换首要识别依据，`alternate_login_name` 为备用，`game_feature_code` 当前只读记录。
- 每一阶段独立提交并运行本地 `.venv` 测试；不要用回滚方式覆盖已有 MuMu/Android 改动。

## 2026-08-29 单人队伍自动战斗修复

- `BaseCombatTask.load_chars()` 现按 `in_team()` 返回的实际人数（1～3）加载角色。
- 单人队伍不再访问不存在的第二、第三角色槽位，仍会返回成功并执行当前角色战斗逻辑。
- 新增 `TestChar.test_load_chars_supports_solo_team` 回归测试；未修改 Android/MuMu 代码。
- 版本更新至 `1.19.11`。

## 2026-08-30 多账号每日任务启动与生命周期修复

- 现场版本：打包版 `v1.19.11`；首次失败日志位于打包版 `working/logs/ok-script.log`，时间为 17:46～17:48。
- 现场证据显示角色仍在游戏世界且队伍 HUD 可见，但 `is_main()` 未识别该任务场景，切换入口直接等待登录账号下拉框并超时。
- `MultiAccountDailyTask.switch_to_account()` 增加共享兜底：账号下拉框不可见且 `in_team()[0]` 明确为真时先调用正式 `_switch_to_login()`；未知状态不发送输入。
- `MultiAccountDailyTask.run()` 对正常、停止和异常出口分别收敛为 `STOPPED`、`STOPPED`、`FAILED`，失败后允许创建新快照重跑。
- 权威序列 `[A4, A3]` 配合“当前执行账号 A3”仍按 A3 → A4 执行；未修改打包版账号数据。
- 配置自动备份失败根因为校验器按文件名排除了所有嵌套 `manifest.json`；现只排除快照根清单，发布 bundle 清单会正常进入备份。
- 本地多账号、切换证据、运行时、计划任务和配置备份共 86 项测试通过；发布版本更新为 `1.19.12`。
- 回滚方式：对本次发布提交执行 `git revert <release-commit>` 并发布递增补丁版本；不得移动已发布标签或覆盖账号配置。
