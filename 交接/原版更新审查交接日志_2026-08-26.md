# 原版 okww 更新审查交接日志

日期：2026-08-26  
项目：个人版 ok-wuthering-waves  1.15.00

## 原版版本记录

- 本轮以原版 `ok-oldking/ok-wuthering-waves` 的 `v3.5.0` 为起点进行审查。
- 已审查至原版最新稳定标签 `v3.6.5`。
- 同时检查了 `v3.6.6-beta.1` 与 `v3.6.6-beta.2`，但它们属于测试版，不作为个人版稳定基线。

## 已确认适配内容

- 每日任务无音区领取后等待、无体力 Esc 退出、继续确认对话及分组滚动逻辑。
- 16:9/超宽屏冷却 OCR 适配。
- 战斗前后切治疗角色。
- F 机制识别与新版跳过确认对话识别。
- 梦魇、凝素、模拟领域、声骸合成等每日任务增强。
- Rover、Phoebe、Zani 等角色代码；个人版 Rover 使用增强后的 `HavocRover` 类名实现，不能直接覆盖为原版 `Rover.py`。

## 未直接覆盖内容

- 原版 Web 角色代码页面与 `CharacterCodeTask` 尚未接入；个人版已有桌面端 `CharacterCodeTab` 和自定义角色加载器。
- 原版 `config.py`、账号切换、每日任务主流程和角色目录未整体覆盖，以避免破坏个人版账号安全模型。

## 验证记录

- 角色代码编辑器与自定义角色加载器：6 项通过。
- 角色逻辑测试：64 项通过，1 项跳过。
- 无音区相关逻辑已在 `src/task/TacetTask.py` 和 `src/task/DailyTask.py` 中确认存在。

## 后续注意

后续继续审查原版更新时，以 `v3.6.5` 作为稳定对比基线；测试版内容必须单独标记，经过个人版适配和回归测试后才能进入稳定发布。
