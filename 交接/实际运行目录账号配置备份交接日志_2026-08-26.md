# 实际运行目录账号配置备份交接日志

日期：2026-08-26

## 正确来源

目录角色约定：`E:\game\okww owener` 是推送后的 Windows 打包版；`E:\AI work\ok-wuthering-waves-master` 是开发版源码。打包版内部的 `data\apps\okww-custom\working` 才是本次运行配置的实际工作目录。

用户指定的实际运行目录为：

`E:\game\okww owener\data\apps\okww-custom\working`

导出前完整性检查通过：12 个账号、2 个序列、0 个错误。

## 备份文件

`D:\游戏数据备份\okww_账号配置_实际运行目录_20260826_121115.json`

该文件是项目现有配置包格式，包含账号 master、序列、可移植运行数据、偏好和四分区 SHA-256 manifest，不包含设备与程序设置。文件大小为 13,576 字节。

## 验证

导出后对同一文件执行 `preflight_import`：通过；账号数 12，序列数 2，错误 0。

此前生成的 `okww_账号配置_20260826_120830` 来源是当前工作区，不是用户指定的实际运行目录，因此不作为本次备份依据，未覆盖或删除以保留审计记录。
