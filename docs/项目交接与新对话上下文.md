# OK-WW 项目交接与新对话上下文

更新时间：2026-09-09（Asia/Shanghai）。当前目标版本：`1.42.06`；发布提交、标签、CI、NAS 与 Release 状态见第 9、10 节，接手时仍须先核对 Git 与实际安装状态。

## 1. 先读这些结论

- A–D 代码整改已完成，不重复从头审查历史问题。后续已完成首次安装、快捷键/窗口诊断、多账号与领奖体力修复、自动诊断上传等工作，版本经过多轮推进。
- `1.38.00` 深塔属性与共享体力规划已实现，提交 `5f335f13`、注解标签与分支已推送。
- `1.38.01` 打包版退出与后台补传隔离已实现，提交 `2569cc91`、注解标签与分支已推送。尚未在本次文档同步中核验该标签的 CI 或安装器 Release 完成状态，不能把推送成功写成构建发布成功。
- 最近全量复跑：88个测试文件、846项用例，838通过、8项原有跳过。首次全量在缺图跳过的界面测试后出现 OCR 后台退出超时，单独复查和全量复跑均正常，未修改 OCR 代码。
- 2026-09-08 文档同步时只读核对：用户打包安装为 `1.38.00`，尚非 `1.38.01`。后台任务已单独迁移成功，因此后台修复与主界面版本不能混为一谈。
- 用户明确要求手动操作游戏和测试；助手检查日志、定位问题与修复代码，不自动操作游戏或虚拟机界面。A3、A4为用户提供的测试账号简称。

## 2. 工作区与数据边界

| 项目 | 路径或状态 |
| --- | --- |
| 源码 | `E:\AI work\ok-wuthering-waves-master` |
| 分支 | `codex/account-switch-foreground-bitblt` |
| 远端 | `origin` → `https://github.com/xihuojun2020-tech/okww-custom.git` |
| Python | 优先 `.\.venv\Scripts\python.exe`，PowerShell直接调用 |
| 用户安装 | `E:\game\okww owener` |
| 安装内源码 | `E:\game\okww owener\data\apps\okww-custom\working` |
| 隔离测试安装 | `E:\OKWW-Test\HostApp\data\apps\okww-custom\working` |
| 独立诊断后台 | 安装盘根目录 `OKWW-Background/<安装ID>/<代码指纹>`，当前设备在E盘 |
| 虚拟机 | `OKWW-T15-Win11`；最后已知为关闭，不能当作实时状态 |
| 笔记本 | R7 7840、16G、4060，长期运行旧程序且有大量账号数据，不用作破坏性或迁移测试 |

用户删除了11份 `docs/reviews/` 下的历史审查/验收文档，当前 Git 中表现为删除；这些删除不是本次改动，不恢复、不混入提交。旧交接文档此前未跟踪，本次同步将其作为当前接手文档纳入版本管理。

必须遵守 [AGENTS.md](../AGENTS.md)：代码发布同步固定宽度版本、更新日志、验证、提交、注解标签和推送；纯文档不升产品版本，不移动已有标签。账号切换测试继续复用生产路径，默认 A1→A3→A4，不维护独立切换实现。保留账号配置、Windows凭据、待上传队列及恢复备份。既有自动NAS上传策略继续有效，不另行上传凭据或擅自发送消息。

## 3. 接手顺序

1. 阅读本文和 AGENTS，核对 Git 状态、config.py、用户最新要求。历史文档与实际源码冲突时，以新证据为准。
2. 优先处理用户对 `1.38.01` 更新、退出测试的反馈；核对其实际安装版本，再读对应时间的主程序及启动器日志。
3. 阅读 [退出与后台隔离实施记录](superpowers/plans/2026-09-08-exit-background-isolation.md) 和 [NAS部署说明](NAS诊断部署与读取说明.md)，不要重复迁移已匹配的任务或强杀所有Python。
4. 深塔测试反馈参照 [属性与全局体力规划](superpowers/plans/2026-09-07-abyss-element-planning.md)，核查日志中的全局配队预览、属性排除、共享体力和中塔半区规则。
5. 如继续早期T15/T16验收，先核对已有记录；只将有实际证据的项目标为完成。安装解包、离线用例与真实游戏验证是不同证据。

## 4. 深塔 1.38.00 的最终规则

角色定位来自 `CharFactory` / `CharType`，不采用用户早期参考名单覆盖定位。弗洛洛、秧秧SP是主C；琳奈元数据为衍射，秧秧SP为湮灭。漂泊者按识别形态区分，头像别名合并体力账本。

左右塔分别设置一个顺属性和一个逆属性；中塔1—2层、3—4层各自设置两个同级顺属性及两个分级逆属性，共12项下拉。配置通过框架Config长期保存，运行开始冻结，清理扫描结果和切换账号不重置。

- 中塔一级逆属性禁止主C和副C；纯治疗按OK-WW定位允许。
- 中塔二级逆属性尽量避开主C，副C允许。
- 左右塔依次优先无逆属性输出、逆属性副C、必要时逆属性主C。
- 顺属性收益按主C判断，双主C均检查，治疗不能为队伍制造顺属性收益。
- 先生成预设及同定位替补候选，再共享同一角色体力安排各层；中塔优先同时预留两个半区，且保持用户设置的整塔执行顺序。
- 左右每层每人消耗1/2/3/4，中塔每层5；跳过一层后不安排该塔后续层。
- 中塔换半区仅在相同队伍仍合法且被选中时直接继续，否则返回重扫重组；从第三层恢复使用后半区规则。
- 中塔无法安全分配时停止整个任务。大仓库使用有界搜索，日志明确标注裁剪，不保证数学上的全局最优。

实现位于 `src/task/abyss_allocation.py`、`abyss_team_planner.py` 和 `AutoAbyssTask.py`。仍沿用单页或两页角色识别，未识别角色不参与规划。50名角色、7938候选、12层无属性约束的离线测量约1.6秒，不代表游戏通关耗时。尚待用户手动验证真实选人、半区切换、兜底和硬禁停止。

## 5. 打包版退出故障与 1.38.01 修复

9月8日11:23主程序退出日志出现终止启动器“拒绝访问”；11:24启动器更新到1.38.00时，将后台上传PID也列入“应用仍在运行”，阻止更新。PID32504被核对为计划任务启动的诊断上传进程；当时主程序退出后它仍使用安装目录内Python。权限报错不等于已证明权限等级不一致。

修复包括：

- `diagnostic_runtime.py` 在安装盘外层独立目录准备不可变Python/诊断代码快照，只带必要标准库、Pillow和Windows凭据模块；自检导入路径后使用。
- `source.json` 保留原来源路径和安装身份；原日志队列、设备标识、Windows凭据不搬迁、不重置。
- 主程序唤醒、计划任务及上传/清理子进程使用独立环境，忽略继承的Python环境变量。
- `MainWindow.closeEvent` 不再同步等待PID方式强杀启动器；执行器或app清理异常也继续退出Qt，关闭到托盘行为保留。
- `main._exit_cleanup` 保留已有祖先进程身份检查，并跳过经来源绑定核验的独立上传进程。
- `scripts/repair_diagnostic_runtime.py` 可从新源码修复旧安装，无需旧安装先完成升级；`--apply` 才迁移任务，失败恢复XML备份。

## 6. 当前设备后台迁移证据

任务 `okww-diagnostics-d815f4f51c33e25c` 已迁移到：

`E:\OKWW-Background\d815f4f51c33e25c\fa9b9449ff86724fb4bc\python\pythonw.exe`

工作目录为该快照目录，参数为 `-E -s -m src.runtime.diagnostic_uploader --root <原队列>`。原任务备份：

`E:\OKWW-Background\d815f4f51c33e25c\fa9b9449ff86724fb4bc\migration\20260908-114808-873.xml`

迁移后核对旧安装目录下不再有上传Python；11:49实际运行返回0，文档同步时再次核对最近12:35运行返回0。这说明后台任务成功结束，不代表所有历史批次均已上传，也不替代打包主界面的关闭验收。

## 7. 验证、发布与剩余事项

- `1.38.00`：88个文件、840项，832通过、8跳过；100项深塔相关测试通过。
- `1.38.01`（历史记录）：88个文件、846项，838通过、8跳过；包含独立后台导入/本地合成批次传输、迁移成功及回滚、关闭异常、托盘、保留独立进程等测试。当前完整测试结果见第 9 节。
- 日志：`E:\OKWW-Test\exit-1.38.01-tests.txt` 为首次超时记录；`exit-1.38.01-recheck.txt` 为全量通过记录；对应结构化结果在 `test_out/test_runs/20260908-114501-803`。
- `1.38.01` 版本校验、代码提交、注解标签、远端推送已完成；本次未核验CI或Release产物，不虚报打包发布状态。
- 该段原定“更新至 1.38.01”的步骤已过期；当前应等待 `v1.42.00` Release 资产生成，再完整退出旧程序后升级、重启并按第 9 节验收。源码与安装目录必须分清。
- 仍待深塔新规则实机验收；早期T15/T16完整矩阵也不能据此整体标为完成。先前用户A3→A4→A3切换曾成功，不代表其他账号与全部异常场景已覆盖。

## 8. 资料入口

- [程序结构说明](程序结构说明.md)：总体机制及最新增量。
- [更新日志](../更新日志.md)：产品About与Release说明共同来源。
- [NAS部署与读取说明](NAS诊断部署与读取说明.md)：后台、队列、凭据与迁移维护。
- [深塔设计与实现](superpowers/plans/2026-09-07-abyss-element-planning.md)。
- [退出修复与迁移记录](superpowers/plans/2026-09-08-exit-background-isolation.md)。
- [早期整改计划](superpowers/plans/2026-09-06-review-remediation-plan.md)：历史A–D与T15/T16上下文，不能当作当前版本缺陷清单。

新对话可直接要求：“先读本交接文档和AGENTS，核对实际版本；继续检查用户手动测试日志。保留用户修改、账号与后台队列，不自动操作游戏，不重复已完成的迁移或修复。”

## 9. 2026-09-09 发布与运行时恢复交接

- NAS 批次 `20260909-133100-008598147a19` 在 13:55:33 记录自动深渊因尤诺长时间大招结束触发 `CombatStateUnknown`。根因是公共战斗层正确拒绝把无证据的界面消失视为完成，但深塔旧流程没有继续读取自身结算页。`1.42.06` 在深塔局部捕获该异常，以完整结算标题和按钮组合复核成功或失败；结算仍未知时重新抛出原异常，不误报通关。恢复路径只重置角色内部状态，不在结算页发送战后切人输入。聚焦验证为108项通过、3项既有跳过；完整测试为98个文件、966项用例，958项通过、8项既有跳过、0失败、0错误，结果在 `test_out/test_runs/20260909-141850-200`。
- `v1.41.07` 已存在且没有可安装 Release，未移动或覆盖。发布恢复顺延为 `v1.41.08`：提交 `fb35e5ff`，注解标签和分支已推送。根因是安装器源码校验器漏将 `.ps1` 视为可做 LF/CRLF 等价比较的文本文件；修复后换行差异允许通过，真实内容变化仍拒绝。本地 `TestPackageSmoke`、`TestReleaseReadiness` 共 25 项通过，`validate_release --tag v1.41.08` 通过。
- `1.42.00` 统一在取色和血条检测前固定有效截图，区分 `GameProcessLost` 与 `FrameUnavailable`；梦魇巢穴对 `CombatStateUnknown` 做目标/血条/同分母进度三态复核，仍在战斗时同一挑战最多续战一次；领域入口 `WaitFailedException` 保存现场并最多恢复重试一次，连续失败时不进入战斗、领奖或扣体力。
- Mornye 单次大招未确认没有增加自动重按。只有新版本实机日志在窗口与截图健康时同场景连续出现至少 3 次，才另立任务评估受控重按。
- 聚焦回归共 66 项通过、3 项因本机缺少图像样本按既有规则跳过。与 CI 相同的 `run_tests.ps1 -Group all` 共运行 98 个测试文件、953 项用例，945 项通过、8 项既有跳过、0 失败、0 错误；结构化结果位于 `test_out/test_runs/20260909-090442-835`。`validate_release --tag v1.42.00` 与 `git diff --check` 均通过。最终提交 SHA、标签、Actions 与 Release 资产状态需在本节继续补记。
- 最近远端核对：`v1.41.08` Actions run `34297343549` 与 `v1.42.00` run `34298027790` 的 `validate-version`、`tests` 均成功，二者均在 `package` 的“Build unsigned installers”步骤运行；Release 页面尚未生成。不得把此中间状态写成发布成功。
- 另一台设备仍需在完整退出并重启新版本后手动验收：关闭游戏时应报告窗口/截图失效而非 `NoneType.shape`；梦魇巢穴 35/41 等短暂丢目标不得提前完成；领域入口卡住最多恢复一次且不扣体力；NAS 新批次不再出现旧二次异常。

## 10. NAS 主发布与旧后台地址修复

- `1.42.05` 修复周本左侧旧“击败…”与新“离开…”同时存在时的优先级错误：逐 OCR 框判断、战后提示优先、连续两帧稳定确认，并记录原始提示和阶段。修复依据为 9 月 9 日日志中 10:01:36 已进入战后、10:02:38 又回退“等待下一阶段”的异常跳变，以及原解析器按拼接文本开头优先接受“击败”的实现。
- `1.42.05` 的 87 项聚焦回归通过；NAS stable 已发布，ZIP 大小 `35900435` 字节，SHA-256 `0630c3cf89bb37e26ea39e885f9c9343d5d1b72eb25acbce2912f5be98c2afcc`，远端复算一致，客户端从 `1.42.04` 检查可发现该版本。GitHub 标签和 Release 状态仍须独立核对。

- `1.42.03` 修复了计划任务系统核验、上传器锁竞争和唯一临时文件，但只推送 GitHub，遗漏 NAS stable 包，所以软件内更新仍显示 `1.42.02`。GitHub 标签与软件内 NAS 更新互不联动。
- `1.42.04` 将首次诊断游标改为完整收集当前 Python 进程创建后已写入的启动日志，同时保留旧版本历史边界；本版本必须完成 NAS 包、清单回读、GitHub 标签和 Release 四项独立核对。
- `1.42.04` NAS stable 已发布：`okww_update_v1.42.04.zip` 大小 `35900081` 字节，SHA-256 `671b546abb863f04de1754f68f6944242d24c595d8b517231f2502fa85a4f415`，远端复算一致；以 `1.42.03` 调用客户端检查已返回可用版本 `1.42.04`。相关 70 项聚焦测试通过；隔离全套运行有 84 个文件、899 项通过，既有图像测试 `TestConfirm.py` 在用例通过后因进程未退出达到 180 秒监督超时，不能记为全套通过。

- 再次检查确认，`1.42.01` 只迁移了计划任务绑定，遗漏了 `%LOCALAPPDATA%/okww-custom/diagnostics/automatic-v1/<安装ID>/settings.json` 中持久化的上传目标。相同 policy 的旧设置会覆盖新版 `DEFAULT_TARGET`，所以界面和上传器仍可能显示 `.170`。
- `1.42.02` 在统一 `settings()` 入口只迁移两个明确的旧官方 `.170` 路径到 `.161`，保留设备身份、时间边界、凭据、日志队列和待上传批次，并保留其他自定义目标。新增测试覆盖两个旧路径、身份保留和自定义地址不变。
- `1.42.02` 实际验证与发布证据：39 项诊断回归通过；本机 `1696…074b` 设置已从 `.170` 迁移为 `.161`，设备标识和开始时间不变。修复提交 `d8b502ed` 与标签 `v1.42.02` 已推送 GitHub；Actions run `34308729582` 正在执行，检查时 Release 为 404。NAS stable 已发布，ZIP 为 `35899051` 字节，SHA-256 `919bd7f941dde09187b8372a5195e14fbbf5e33a18942e22157bcefb34174329`，NAS 文件复算一致。

- 当前源码默认诊断目标和局域网更新清单均为 `192.168.3.161`。本机 `E:\OKWW-Background` 中发现三个历史不可变快照仍包含 `192.168.3.170`；历史文件存在本身无害，问题是共享的 `scheduler.json` 只按 revision 和时间判断，可能让新版跳过计划任务重绑。
- `1.42.01` 将调度器修订号升至 4，并要求缓存中的后台 Python 与工作目录精确匹配当前快照。升级启动后会重新安装所属计划任务，保留凭据、日志队列和待上传数据。
- 后续每次版本发布都必须同时发布 NAS 和 GitHub：NAS stable 清单作为局域网主通道，GitHub 标签与 Release 作为备份通道；两边使用同一版本内容，分别记录成功或失败状态。
- `1.42.01` 实际发布证据：代码提交/标签 `bb74dfad` / `v1.42.01` 已推送 GitHub，Actions run `34299703467` 正在执行；NAS `stable/latest.json` 已原子切换到 `1.42.01`，更新 ZIP 为 `35898866` 字节，SHA-256 `97e03628d2e5052d66dbb97aec6a942b976cdc788fa87a4b2e378a69e6e1dccc`，复算一致。
- 本机安装 `d815…e25c` 的计划任务已迁移到后台快照 `8fed…731f`，任务 Python 和工作目录均指向该快照，快照内 `DEFAULT_TARGET` 为 `192.168.3.161`。旧 `.170` 快照保留但已不再由该任务调用。
