# 桌面账号与界面方案实施记录

**依据：** 用户桌面 `OKWW界面与账号配置调整方案-2026-09-11.md`，不采用此前聊天中的残缺方案。

**目标：** 分离提醒与执行配置，精简任务入口，统一账号和工具布局，完善手动留证及历史展示。

**技术：** 现有 PySide6、账号 CAS 发布服务、SQLite 证据仓库；不新增依赖。

## 并行修改约束

- 起始基线为 `acbe4e09`，源码版本 1.55.00。
- 已联系同工作区任务“阅读项目文档”确认文件和版本归属。
- 不覆盖既有 reviews 删除，不更改其他任务的战斗实现。
- 修改共享文件前重读最新内容；测试前协调框架覆盖目录，发布前重新确认版本与提交范围。
- 桌面文档已获实施批准。录像仅锁定支持页面范围，总开关和时长保留原行为。

## 执行清单

- [x] 独立提醒：新增 `src/account_reminders.py`，在账号 `extensions` 内保存稳定提醒 ID；校验 ID、去重并保持选项顺序，不改 task_config。新增 `tests/TestAccountReminders.py`，验证空默认、输入校验与执行配置隔离。
- [x] 账号表单：`AccountConfigTab` 使用现有草稿与确认保存流程编辑提醒，保留无关扩展；完成检查读取同一字段。
- [x] 录像：账号字段元数据增加中文标签和只读页面范围；旧部分选择在用户保存账号时经备份后规范化，执行端采用全部已支持页面。总开关、时长、旧录像不变。
- [x] 任务卡片：只调整展示字段，单账号序列筛选不改变执行语义；多账号列表复用原始序列顺序，移除静态说明但保留错误。
- [x] 活动：显示名变更保留旧标识；两个占位卡片不注册为可运行任务；按显式更新时间稳定排序，不使用文件时间。
- [x] 紧凑布局：账号、工具复用共享行和默认折叠，移除重复外框和无效间距，保留安全提示。
- [x] 完成检查：提醒筛选、只读历史摘要；人工截图初始未选账号，保存必须确认；绑定冲突提示，保留原始运行绑定，不改变自动采集。
- [x] 验证：使用合成账号及图像；定向与全量测试、双宽度和高 DPI 渲染、历史保留和序列稳定检查。
- [x] 同步用户说明、更新日志与源码版本 1.56.00；并行任务已结束并允许增补文档索引及交接，保留其历史记录。

## 验证结果

- 最终全量：108 个文件、1155 项测试，1147 通过、8 项既有跳过，无失败或错误；报告 `test_out/test_runs/20260911-100418-534`，输出 `test_out/desktop-plan-final-all.log`。
- 最终定向：109 项通过，输出 `test_out/desktop-plan-final-focused.log`。覆盖全量运行期间补充的提醒输入边界和活动排序元数据检查。
- 收尾补测：切换浏览账号时立即清空旧账号画面与运行摘要，22项证据/界面测试通过（`test_out/desktop-plan-binding-final.log`）；暂存后的发布检查18项通过。
- 六页 760/1100 宽度默认折叠渲染：`test_out/desktop-plan-release-ui`；150% 缩放且全部展开：`test_out/desktop-plan-expanded-ui`。无新增内层滚动条或横向溢出，已目视检查任务、账号、工具和完成检查。
- 中文显示名称目录已更新编译；五语言目录语法与重复条目检查通过。
- 原有证据删除实现未改，新增运行记录不设置保留期；视频入口不扫描目录猜测归属。未操作真实账号、游戏、NAS 或安装器。
- 本次发布目标为注解标签 `v1.56.00`；实际提交与推送结果以 Git 和最终回复为准，不将标签推送等同于安装器完成。

独立提醒的最小验收：`set_reminders(account, ['weekly_boss'])` 仅生成新的账号扩展数据；原 account、task_config、UUID 和已有扩展内容均保持不变。运行 `./.venv/Scripts/python.exe -m unittest tests.TestAccountReminders`。
