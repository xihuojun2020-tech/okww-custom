# 周本挑战首版实施方案

本方案根据用户 2026-09-08 提供的七张流程截图及执行授权实施。首版仅注册到测试功能，所有剩余次数刷同一个目标；逐次选目标、每日及多账号调度另行实现。

## 行为约定

- 从大世界进入 F2 / 素材获取 / 战歌重奏，连续新帧读取本周剩余 N/3；0 直接返回，非法/冲突读数停止。
- 十个周本使用稳定标识存储、中文实际名称显示；逐屏 OCR 名称并点击同一行直接挑战，不使用旧的九项滚动估算。
- 难度页再次核对名称与次数，读取单次体力费用，保留游戏默认难度。保持当前队伍，单人挑战、开启挑战后等待场地。
- 使用公共 Boss 战斗、寻敌、移动能力；不调用刷声骸任务的循环、滚轮交互、取消领奖或异常递归重启。
- 战后识别 F 领取奖励，进入有挑战成功、奖励展示与两个操作按钮的结算页，才能记一次领奖。同一结算页不会重复记数。
- 有剩余且当前体力可支付一次时重开；达到初始次数或体力不足则退出。退出后再次读取列表，只有 0/3 算完成。
- 首版仅消耗当前体力，不自动转备用、不使用道具或购买体力。意外弹窗停止，不盲目确认。
- 暂只支持简体中文游戏、16:9。截图识别验证不替代各 Boss 实机验收。

## 文件与实施步骤

1. `src/task/weekly_boss.py`：目标表、次数/费用/余额纯解析、同一行标题与按钮匹配、结果记录。
2. `src/task/WeeklyBossTask.py`：任务配置、有限页面等待、翻页选择、战斗、领奖重开及最终复核。返回本轮结果供未来 Daily 调用，首版不写账号完成记录。
3. `tests/TestWeeklyBossTask.py`：0/1/2/3、非法/冲突 OCR、按钮配对、计数、失败与停止、体力不足、复核非零、重开超时、配置与注册回归。
4. `tests/TestWeeklyBossImages.py`：七张用户图片的去身份区域样本，验证实际页面 OCR、标题、F 交互、结算与体力。只保留必要区域，原图不提交。
5. `config.py` / `run_tests.ps1` / `i18n/*/LC_MESSAGES/ok.po`：任务注册、官方测试分组、所有现有目录翻译及 .mo 编译。
6. 运行指定文件测试、发布校验及官方全量测试；修正失败后再核验。
7. 在当前最新版本上递增中间版本，更新日志同步；只提交本功能文件，创建匹配注解标签并推送。实际游戏未经测试的范围如实说明。

## 验收检查

- [x] 单次领奖结算只记一次，战斗失败/领奖未知不记数。
- [x] 初始 N 决定 N 次领奖、N-1 次重开；非零最终复核不报完成。
- [x] 目标按钮必须与名称同一行，支持十项及跨屏滚动，列表到底有界结束。
- [x] 默认难度、当前队伍不改动，旧 FarmEcho 行为不变。
- [x] 游戏丢失、停止与截图故障传播，移动按键清理。
- [x] 体力不足退出并复核，不消费备用。
- [x] 实际截图 OCR 与合成状态转换回归通过。
- [x] 翻译编译、版本一致性、全量测试完成。

## 2026-09-08 离线验证记录

- 实施版本 `1.39.00`，基于 `1.38.01`。未修改其他会话的深塔和交接文件。
- 官方全量结果：`test_out/test_runs/20260908-140738-116/`，90 个文件，876 项测试，868 通过、8 项既有跳过，无失败或超时。
- 周本专项 22 项、截图专项 8 项通过；截图覆盖十个列表条目、次数/费用/当前体力、默认难度页、编队按钮、F 领奖及结算。领奖关键页面另覆盖 720p、1080p、2160p，原始样本为 1440p。
- 截图确认名称为“失坠困咎之庭”和“昔日咏叹之钟”，修正聊天初稿两处笔误。仅规范化已观察到的分隔点和“紅/红”，不使用模糊名称匹配。
- 专项截图仅保留必要游戏 UI 区域，其余为黑色；不提交账号特征码、角色身份栏或桌面覆盖信息。生成入口 `scripts/prepare_weekly_boss_fixtures.py`。
- 实际使用公共战斗轮转、寻敌、F2 导航和走向宝藏能力，不调用 FarmEcho 的整个运行循环，也不猜测十个新副本与旧特殊 Boss 预设的对应关系。
- 真实加载、Boss 阶段机制、三次领奖、退出后的真实次数变化尚未实机验收；本版入口明确放在测试功能，不声称十个 Boss 均已实测。

## 后续接口约束

第二阶段按 UUID 接入周一、周日独立检查，周日不因周一完成而跳过。位于普通体力任务之前，周本后重读体力与活跃度。每次单独选周本需新增明确模式与目标列表，并沿用本任务的游戏剩余次数与结算确认机制，不以目标列表长度充当本周额度。

## 2026-09-08 多阶段战斗跟进

`1.40.01` 已加入左侧任务提示三态复核：

- `击败……`、`与岁主「角」对战`：战斗未完成；文字变化仍属于阶段切换，不领奖。
- `领取奖励`、`离开……`：连续两帧确认后进入寻奖；“离开”只作战后信号，不直接退出副本。
- 空白或陌生提示：有限等待；“挑战成功”和近身 F 领奖仅作辅助证据，不能覆盖明确的战斗提示。

普通 `combat_once()` 返回和 `CombatStateUnknown` 已走同一复核，但实机历史故障 `not in_team while switching` 还存在一个未覆盖来源：战前或战后切治疗位于 `combat_once()` 自身的异常转换区间之外，可能直接抛出 `NotInCombatException`。建议后续补丁仅修改 `WeeklyBossTask`：

1. `_fight()` 同时捕获 `CombatStateUnknown` 与 `NotInCombatException`，保存原异常后进入现有阶段复核。
2. 复核为 combat 时等待 `in_combat(target=True)` 并继续循环，不增加领奖次数。
3. 复核为 post 时进入寻奖；仅 `CombatStateUnknown` 需要补做其原有战斗状态清理，避免对已经执行过 `combat_end()` 的战后切治疗异常重复清理。
4. 复核超时时重新抛出原异常；`CharDeadException`、`TaskDisabledException`、`FrameUnavailable`、`GameProcessLost` 和配置异常不得捕获。
5. 增加两项回归：直接 `NotInCombatException('not in_team while switching')` 且提示为 combat 时继续下一阶段；提示始终未知时保留并抛出同一个原异常。

该跟进尚未实现，不属于 `1.40.01` 已完成范围；实施时按补丁版本更新版本号、更新日志并重新执行周本专项、公共战斗、发布一致性及全量测试。
