# 海墟库存扫描与暂停恢复设计（用户已在对话中批准）

范围：已验证成功的出口移动保持不变。当前周期金、紫、蓝信物效果预先收集写入程序；运行中逐屏识别具体信物和库存，不逐个点击读效果，只点击最终目标。按实际预设成员、顺逆属性、触发机制及库存联合分配上下半；不替换成员，不绑定预设编号。绿色不参与本次选择。

已知解锁信物弹窗有界自动关闭。未知页面、识别或动作失败释放输入并暂停，由用户处理后点击原继续按钮。阶段完成依靠结果确认，不依靠已经发出点击；继续后重新检查当前页面和上下半身份。人工已完成的动作不重做，返回当前层编队页则按需重新建立库存和计划；未知进度再次暂停，不从第7层盲目重开。

只支持本进程检查点，不实现程序重启后的恢复。暂停不暂停游戏世界，不在后台发出操作；主动停止仍传播取消。原周本、多账号与公共暂停代码不修改。用户已选择手动实测，不自动启动游戏或任务。

资料与验证：每条静态规则保存来源，未核验新信物不猜。截图回归至少覆盖720p/1080p/1440p、锁定、0/有限/无限、误识别负例；模拟覆盖库存共享、只点击目标、人工进入下半、结果页、下一层、未知状态、暂停与取消。

本次为中等功能改动，版本1.81.00；通过验证后提交、标记并发布GitHub和指定NAS更新源。
