# NAS 诊断实施验收（1.36.00）

## 本地结果

- 基线 v1.35.00；新增本地诊断、不可变批次、独立上传与持久重试、截图审核门禁、生命周期接入、设置状态入口、系统计划任务安装脚本和 NAS 证据索引。
- `run_tests.ps1 -Group all`：86 个测试文件通过，804 个用例，其中 8 个按现有环境条件跳过；无失败/错误。结果目录 `test_out/test_runs/20260907-212930-867`。
- 后续路径/封口复核后，15 项诊断专项重跑通过；新增界面测试 1 项通过，分组注册测试 3 项通过。
- `python -m pip check`：No broken requirements found。
- `python scripts/validate_release.py`：1.36.00。
- 源码更新包：`dist/nas-1.36.00/okww_update_v1.36.00.zip`。
- 使用模块入口 `python -m scripts.verify_update_package ... --previous-ref v1.35.00` 验证 246 个文件，哈希一致、配置保留。
- 包 SHA256：`ea8d8bb17e914be04dd13ca9e079a9cb1e246db6aef946f889484cb2f4f25b9c`。

说明：上述 python 均使用本仓库 `.venv/Scripts/python.exe`。最初直接运行 verify_update_package.py 因模块搜索路径失败，改用其模块入口后校验通过；未修改该既有脚本。

## 未完成的外部验收与明确边界

1. B7 原包缺失：未改领奖区域和体力确认逻辑，不能宣称已修复。
2. NAS 候选共享只读探测返回 Windows 1326；没有修改凭据，没有真实上传成功证据。
3. 本机未发现旧 NAS 同步脚本/任务；系统计划任务脚本已交付，但未执行安装、迁移或跨重启测试。
4. 真实游戏任务异常和截图保存仍需目标设备运行验证；本次未操作游戏。
5. 图片默认待审核，不做自动界面打码；只允许明确审核后的图片生成可上传批次。
6. 达到存储上限后保留原件并停止新增诊断，不自动删除历史证据；需人工归档。
7. NAS 工具只验证和生成证据索引，没有调用 AI/云端模型，没有部署 NAS/OpenClaw 服务。

操作、协议与回滚步骤见 [NAS诊断部署与读取说明](../NAS诊断部署与读取说明.md)。
