# 海墟第10层出口绕行复核（1.85.04）

## 范围与证据

打包版已在 2026-09-23 14:36:39 升至 1.85.03。第9层随后成功进入下半海域并结算；第10层 14:46:21 开始出口寻路，14:47:21 因 60 秒未找到 F 提示暂停。该分钟无“出口标记丢失”或“无进展回正”状态。未审阅 NAS 诊断 ZIP。

本机暂停截图 `E:/OKWW-Data/14caed06b36648828f3621dcce84b949/screenshots/14-47-21.683_sea_ruins_paused_original.png` 显示角色仍在跑动，出口标记在画面右侧。现有模板能识别该截图中的标记，归一坐标约 (0.784, 0.572)，匹配分数约 0.932。截图只能证明暂停时的位置，不能重建完整 60 秒路径。

## 原因与处理

`_walk_sea_exit` 的“无进展”条件要求两侧场景像素差不超过 3、标记坐标变化不足画面 1%，持续 4 秒。海面动画和角色绕行都可能使场景不断变化，因此该条件无法触发，失标恢复也不会触发。1.85.03 的有限中键回正只覆盖这两个条件，第10层实际未进入它们。

在既有出口寻路中增加独立的偏离时间检查：标记距画面中心超过宽度的 15%，距离上次回正至少 8 秒时，先释放输入，再中键回正，重新读取帧、确认上半结束阶段及标记。与已有失标和静态无进展恢复共享最多两次额外回正额度，保留 60 秒总预算、3 轮有限脱困以及 F 提示复核。

## 验证限制

动态场景合成测试确认：即使每秒画面变化使静态无进展条件一直不成立，偏离标记仍触发两次有界回正。海墟相关离线测试已通过。背景输入是否使实机角色有效转向、以及第10层是否顺利进入下半海域，仍需在更新后的打包版验证。
