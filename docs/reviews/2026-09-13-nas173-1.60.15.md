# NAS 173 迁移验收（1.60.15）

## 原因

当前源码已使用 .173，但开发工作区仍注册着早期独立后台快照：默认 .170，持久化目标 .161。源码更新不能直接修改已有的不可变快照；必须在启动时更新任务动作并迁移保存设置。

## 修改

- nas_location 支持旧官方共享、直接 AI诊断 共享以及斜杠路径，一律产生唯一 .173 候选。
- 更新源读取时将迁移结果原子写回配置。
- 调度修订号 6，替换任务动作前停止已确认归属的旧任务。启动链仍是 settings → prepare_runtime → uploader --ensure-task → 当前快照上传。
- 修复工具 --apply 同步迁移目标。旧凭据仍可查找，旧地址不作为自动回退目标。
- 历史报告、不可变旧快照和迁移测试不进行字符串覆盖；新注册动作使用新快照。

## 本机验收

两个安装对应的计划任务均迁移至当前代码快照：开发工作区 1696e9d4ce19074b、已安装程序 d815f4f51c33e25c。两个实际 spool 及开发工作区 MSIX 重定向配置的目标均为 .173，未清除待传队列。

NAS .173 权限探测通过 write/read/rename/delete。38 项回归通过：TestNasLocation 6、TestDiagnosticPolicy 24、TestLanUpdateService 4、TestLanUpdateTransport 4。

## 另一台设备

更新并启动 1.60.15 后，自动迁移保存的旧官方地址，生成当前独立上传快照并更新本安装的计划任务。默认诊断目标为 \\192.168.3.173\羲火君 共享给我\AI诊断；更新源在该目录的 OKWW-Updates/stable/latest.json。不需重新填写旧凭据。

版本分发和迁移逻辑已验证；尚未直接登录另一台设备确认其更新后的运行状态。验收时检查程序版本、诊断目标和计划任务动作，并确认 NAS 新会话上传。
