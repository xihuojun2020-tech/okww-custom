# 9月12日晚使用端输入失败与 NAS 地址修复

读取位置：`\\192.168.3.173\羲火君 共享给我\AI诊断\待分析\日志\okww-custom\2026-09-12`。原始日志未修改。

在用户提供的 `2026-09-12_1922_ok-script.log` 内按日志实际时间筛选18点和19点，共1517条带时间记录，976条ERROR，其中972条是同一游戏窗口的 `PostMessage` Windows错误5（拒绝访问）。这是该文件内日志行数量，未把其他重复快照叠加，也不等于972次独立任务失败。

18:02:45开始连续输入被拒绝，18:02:48出现自动战斗锁敌失败及显卡滤镜提示。输入投递已有直接失败证据，不能用该提示证明滤镜是根因。18:01:52启动记录的app_version、app_profile、pyappify_executable均为None，说明没有常规启动器环境标识；不能仅据此确定进程是否提权。Windows UIPI权限差异是首要排查方向，需在使用端核对游戏和自动化进程的完整性级别。

18:05:55、18:51:32的GetCursorPos同样报拒绝访问，说明还存在输入桌面不可访问的情形，可能涉及锁屏、会话切换或安全桌面，现有日志不足以区分。18:51:25开始窗口exists=0，后续到19:22:27持续无帧而跳过后台任务；这段记录不能认定游戏仍保持可操作状态。

使用端恢复步骤：保存任务状态并完整退出当前程序，从正式启动器以管理员权限启动；确认游戏和工具在同一已解锁交互会话，游戏窗口存在且未最小化，再测试自动战斗。先观察是否仍有PostMessage错误5，再判断锁敌识别；不修改滤镜阈值来掩盖输入失败。没有对使用端进行远程提权、重启或游戏操作，恢复效果待实机核验。

代码修复1.60.11：NAS公共地址表只保留173，172转为旧地址迁移别名；诊断已有settings迁移逻辑会保留设备身份和队列，后台运行时指纹包含nas_location.py，会生成新快照。旧凭据仍可从Windows凭据管理器别名读取，但网络连接只发往173。更新配置读取时规范旧官方源，包下载绑定173来源。上传不再将30秒预算平分给两个地址。其他自定义共享和HTTPS来源不改。

早期上午的技能动画未确认、周本导航及角色试用错误需按对应版本分别判断，不将其与晚间Windows输入拒绝合并成一个根因。源码版本当时为1.60.10；使用端最新启动没有可靠产品版本字段，不能据该启动日志断言已运行1.60.10。

补充地址来源：NAS中的`NAS同步校验诊断包_20260912.zip`仅含一份18:56人工同步报告。该报告称使用端将Z盘映射到172，但`nas_log_sync.ps1`第16行仍为170，断线重连会恢复旧地址。此为报告所述，未直接读取使用端脚本。当前仓库和可访问的`E:/game/okww owener`均未找到该脚本，无法在此替使用端修改。该机需把脚本重连UNC和Z盘持久映射都改为`\\192.168.3.173\羲火君 共享给我\AI诊断`，不能仅更新程序内上传设置。历史报告所推荐的172已被本次用户要求取代。

验证：NAS地址、更新源及诊断策略33项通过，诊断管线及证据35项通过。首次运行时一项后台快照目录重命名报WinError5，单项重跑及完整33项重跑均通过；保留为环境瞬时失败记录，未隐藏或跳过测试。更新包按v1.60.10合成升级验证，发布器完成173写后哈希校验并更新stable清单；后续独立回读及GitHub状态另行核对。

19:34独立回读通过：173 stable为1.60.11，ZIP大小37,725,206字节，SHA-256为`5c93cee0929b21d7e4ddac5192d38d837d8dff2f7fef107c0bb92933c2d906ac`，远端整包复算与清单一致。程序以1.60.10检查返回“发现局域网版本1.60.11”。GitHub提交和标签另行执行，安装器构建不以NAS成功替代。
