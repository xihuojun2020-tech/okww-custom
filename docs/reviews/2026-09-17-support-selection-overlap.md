# NAS 若梦声骸选择状态重叠诊断

## 范围与证据

审核包：`okww诊断证据_20260917_手动_164541_3e9cbb73.zip`，NAS 待分析/压缩包目录，2010191367 字节。逐字节读取计算 SHA256：`6a79306ae51afb70d997538a97a12b5408a2590fec7ce7e8ad413870f525e382`。

压缩包共 5947 个成员、631 个日志片段。此次针对最新会话 `20260917-163717-d60231711d15` 的 38 个日志片段检索声骸失败及版本，检查对应 16:45:29 故障截图，不代表逐一审查全部历史会话、截图或账号。

- 16:37:18 日志确认目标设备启动版本 v1.79.08；16:37:50 确认从 v1.79.07 更新。
- 16:41:06 至 16:41:24，导航操作 `cec96ca0870949d3a37bae04c4b6c212` 的 source=True、target=True，状态 unknown，等待 18 秒失败，输入 0/3。
- 16:45:11 至 16:45:29，操作 `c5f83501c37f4a48ae719a44f84cf9db` 同样复现。
- 两次最终观察均为 slot=1、index=0、page=True、category=True、selected=True，期望与实际均为攻击型。
- 对应截图成员 `batches/20260917-163717-d60231711d15--164529-818bb7f5f3dd/截图/okww-custom/2026-09-17/20260917-163717-d60231711d15/164529-818bb7f5f3dd/6dc05226e8d6426eb371a13e49d7e815.png` 显示第一个声骸已选中、攻击型文字正确，装配按钮可见。金色面具仍锁定，按原类别回退并非此次错误。

## 根因

1.79.08 新增 `navigate_ui('选中支援声骸', self._support_page, chosen, ...)`。选中声骸不离开选择页，因此 `_support_page` 与 `chosen` 同时为真。

共用导航器 `BaseWWTask.navigate_ui` 为避免冲突误点击，将 source 与 target 同时成立定义为 UNKNOWN。新增调用错误使用了非互斥条件，导致目标已满足也无法完成。即使一开始未选中，选中后也会发生同类冲突。

这是 1.79.08 引入的确定性回归，不是此次 OCR、锁定判定或后台输入失败。上次测试分别验证选择状态和通用导航，却未串联真实选择谓词与生产导航器，因而漏检。

## 修复方案与验证要求

仅修改若梦声骸选择调用：待点击状态限定为“位于选择页且目标尚未确认选中”；目标状态继续为页面、选中边框及类别同时确认。两者互斥，已选中立即完成，未选中才限次补点。保留通用导航器的冲突保护、面具优先、装配检查、绑定修复与挑战重试，不做全局放宽。

回归必须调用生产 `_equip_supports` 和 `navigate_ui`，用真实截图及受控帧序列覆盖默认已选中零次点击、首次点击丢失后补点、选中后进入装配，以及错误页面/未知状态不装配，不能仅模拟互斥的虚构页面。

## 诊断归档时状态

诊断完成，修复设计待用户确认，尚未修改代码、发布或覆盖打包版；未操作目标游戏或改变账号配置。报告随审核标记保存，仅该压缩包进入已审核保留流程，未审核包不受影响。

## 用户确认后的实施与验证

仅将若梦选择导航的 source 改成“选择页且 not chosen”，未修改共用导航器。版本同步为 1.79.09。新联合回归使用实际 NAS 故障截图和真实 OCR，运行生产 `_equip_supports` 与选择步骤的 `navigate_ui`，仅模拟采集帧、时钟、输入及前后编队/装配转换。

旧代码在默认已选中和第二次点击后选中两个子场景均真实复现超时；修复后检查三槽均进入装配、默认选中零次额外点击、首次输入丢失后共两次选择点击、三次仍未选中不装配，以及空白未知页面零次点击不装配。测试不把模拟装配转换等同于实机装配成功。

8 个相关文件共 152 项通过，0 失败、0 错误、0 跳过；本版未重跑全量测试。1.79.08 全量测试虽通过但漏掉该联合场景，本次新增测试针对该缺口，不以测试总数代替实机验证。尚未自动安装或实际运行目标账号。

已审核 ZIP 对应 NAS 报告及其校验记录保留原诊断快照，不覆盖；本次实施报告另随新版更新发布。

## 发布回执

- 提交 `0412f6e3`：`fix: make support selection navigation states exclusive`；分支 `codex/echoes-support-overlap` 和注释标签 `v1.79.09` 已成功推送 GitHub origin。
- 以 v1.79.08 覆盖升级验证，413 个文件一致、账号配置保留。包大小 38240720 字节，SHA256 `2c841327c3a11473929a94575dfc35c8a77f0310ce22415c5c821687f101d9b5`。
- 指定 NAS stable/latest.json 已读回 1.79.09，包为 `releases/v1.79.09/okww_update_v1.79.09.zip`，UTC 发布时间 `2026-09-17T10:44:48Z`。本报告同步到该版本目录。
