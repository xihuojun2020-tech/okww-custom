# 2026-09-25 深塔选中身份复核故障

## 范围与证据

- 诊断包：`okww诊断证据_20260925_手动_210319_c324eac3.zip`，上传于 2026-09-25 21:04，SHA256：`1509fed3e38471557bfac0bb298e43a5e1875a3dc8d0a147dba32e6dabe40383`。
- 核对包内 `20260925-204828-e7e857b7b2dc` 会话的日志汇总、21:02:09 事故记录和事故前后原始截图。使用端版本为 `1.86.03`。
- 21:01:41、21:01:46、21:01:49 分别确认奥古斯塔、尤诺、守岸人已选中；21:01:54、21:01:58、21:02:00 又逐一确认三个选中标记。
- 21:02:09 在 `_verify_planned_selection_markers → _selected_records` 报错“选中卡片身份未知，禁止清理或开始战斗”。故障截图显示编号 1、2、3 三名角色，没有第四个编号。

## 原因

全页复核在滚动重叠页面重新用头像特征匹配每个带选中边框的卡片。它把任一头像匹配失败立即作为致命的未知选中卡处理，即使同一轮已经对计划中三名角色逐一完成身份和标记确认。诊断截图显示滚动条约 0.3222 与 0.4065 的两种页面位置，角色卡会在相邻页面重复出现。这次停在第二次头像核验，未发生点击“完成”或开始战斗。

进一步用包内 21:02:00 和故障帧验证：旧 `_read_selection_number` 对二值化选中角标会将清晰的“2”误读为“1”；相同裁剪使用放大的原彩图，真实 OCR 返回 `[2, 1, 3]`，故障帧可见的两张完整选中卡返回 `[1, 3]`。

## 修复

- 对最终配队核验保留计划成员逐一头像身份及选中标记检查。仅当随后全页头像匹配失败时，扫描所有选中标记并读取游戏角标编号；只接受恰好 `1..N` 的编号集合。
- 编号缺失、额外标记无法读号、卡片定位失败或识别出与计划不同的身份时继续停止，避免误开战。
- 选中编号 OCR 改用 4 倍放大的原彩图，保留暗色角标背景，避免二值化伪影。
- 增加未知身份发生的页码、槽位与识别结果日志，便于后续设备复核。

## 验证与限制

- 深塔相关 119 项离线测试通过；包括头像全页识别失败后编号复核通过、额外编号或未读编号拒绝。
- 用此次原始截图和项目 OCR 引擎实测，21:02:00 页面返回 `[2, 1, 3]`，故障页面返回 `[1, 3]`。
- 尚未在发生故障的设备上完成一次真实深塔挑战。若游戏角标 OCR 在其他分辨率无法确认，任务仍会安全停止，并留下详细日志与截图。
