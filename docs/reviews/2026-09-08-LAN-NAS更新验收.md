# 1.41.00 局域网 NAS 更新验收

## 环境

- 发布设备：当前开发设备
- NAS：`192.168.3.161`，SMB TCP 445
- 共享根：`\\192.168.3.161\xihuojun 共享给我\AI诊断\OKWW-Updates`
- 来源版本：`v1.40.02`
- 目标版本：`1.41.00`
- 发布提交：`25f13820`（验收记录提交前）

## 已验证

- unit、integration、UI 三组自动化测试通过。
- 更新包从 `v1.40.02` 合成验证通过，262 个受控文件一致，合成目标的 `configs` 保持不变。
- NAS 发布工具成功创建版本目录，并在版本 ZIP 与 `SHA256SUMS.txt` 完成后发布 `stable/latest.json`。
- 客户端默认 UNC 配置从 NAS 读回清单，针对 `1.40.02` 返回 `available 1.41.00`。
- 客户端从 NAS 完整下载并校验 35,893,422 字节更新包。
- NAS 读回 ZIP SHA-256 与清单一致：`da7552e95501b1bd2d1050f054c8f91d83028d101fc23e4a1b84268b63743d03`。
- SMB 445 可用；5666 为飞牛 fnOS 明文 HTTP 管理页，未作为更新源。

## 尚需客户端实装

以下项目不能用开发设备当前 `1.41.00` 安装覆盖自身，需在仍运行 `1.40.02` 的非主用客户端执行后再创建发布标签：

1. 通过通用设置安装 `1.41.00` 并确认自动重启。
2. 确认账号配置和合成标记保持不变。
3. 下载中断网时确认旧版本不变。
4. 在测试副本触发安装失败并确认状态为 `rolled_back`。

在上述四项完成前，本记录不声明端到端实装验收完成，也不授权创建 `v1.41.00` 标签。
