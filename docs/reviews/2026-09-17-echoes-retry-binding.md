# 若梦重试与特征码绑定状态修复

## 检查范围与根因

检查本机 1.79.07 打包版 2026-09-17 日志、运行摘要及故障截图；没有审核新诊断 ZIP，也没有删除原始日志或操作游戏。

- 13:38 的 A3/A4 若梦启动因特征码未绑定退出，之后有自动战斗后台轮询。绑定读取直接拒绝 `current_task.running`，无法区别后台轮询；执行器后台任务返回 True 的 continue 路径还跳过了状态清理。暂停后台任务保留调用栈，会继续挡住绑定。
- 13:46:06 第三位守岸人期望 index 2 生存型，故障图仍选中 index 0 攻击型。金色面具当时未解锁，回退 index 2 正确；问题是单次选择点击没有生效，随后仅等待八秒，没有补点。没有证据证明是类型 OCR 错误。
- 13:58:26 和 14:09:34 两次失败发生在“重新挑战若梦副本”。截图显示真实失败结算且按钮仍在，日志显示 source=True/target=False；导航最多一次点击、约四秒后输入预算耗尽，未执行完配置的三次战斗尝试。窗口 visible=False 是伴随证据，不能据此断言唯一输入失败原因。
- `EventSettlement` 是正常控制流信号，包含成功和失败结算。共享战斗检测把它打印成 ERROR 后继续抛出，增加了噪声；并不表示所有出现该堆栈的关卡都失败。后续日志已有成功推进到堕梦神躯的证据。

## 实现

1. 执行器 finally 统一清理本轮任务 running/current_task，覆盖 continue 和异常路径。暂停后台轮询时通过已有中断异常退回循环，保留用户启用开关；前台任务的暂停状态不放行。
2. 绑定请求遇到后台执行时留在有期限的队列，等轮询退出后在执行器线程读取，不从战斗调用栈中读取账号。真实前台运行、暂停及账号切换的保护保留，过期请求仍报错。
3. 声骸选择复用现有稳定页面导航：最多三次输入，间隔至少三秒，选中且类别吻合即停止。未知页面不点击；保留面具优先、角色回退、三槽占用检查。
4. 重新挑战按钮最多三次输入、间隔至少五秒；战斗尝试配置独立，按钮补点不消耗额外战斗次数，切入目标页面后不再点击。
5. 增加明确的战斗控制流异常基类，正常结算绕过 ERROR 日志且继续传给若梦结算处理；真正异常继续记录。

版本及更新日志同步为 1.79.08。未修改角色轮转、账号配置或放宽最终关卡判定，没有新增依赖。

## 验证与边界

新增回归先在旧实现出现 6 项失败/错误；修改后 8 个针对性测试文件共 173 项通过。覆盖后台完成残留、暂停后台退出、绑定排队至空闲、原有前台拒绝、正常结算日志、声骸与挑战点击预算、首次丢失后恢复、已到达不再点击和三次上限。两个新故障图片已去除顶部和底部识别信息，并使用生产 OCR/选择逻辑验证。

此次修复保证失败后的恢复流程能执行，不保证每场战斗都获胜；战斗中的个别大招动画未确认仍可能发生，没有把这些独立警告隐藏或直接视为成功。未自动安装打包版，实机完整流程需更新后验收。

全量回归 `run_tests.ps1 -Group all` 已完成，166 个测试文件共 1722 项，0 失败、0 错误、8 项跳过（结果目录 `test_out/test_runs/20260917-150254-181`）。部分故障注入用例会打印预期错误日志，统计以测试结果为准。

从 v1.79.07 临时检出并覆盖更新验证通过：413 个文件一致，账号配置保留。更新包 `okww_update_v1.79.08.zip` 为 38240545 字节，SHA256 为 `33330cbcc4287f883b04ea47fbf12f7c3aa876e3d38cc6de669ca9b23811f312`。

## 发布回执

- 提交 `126b1ad4`：`fix: recover event clicks and clear background binding blockers`。分支 `codex/echoes-retry-binding` 和注释标签 `v1.79.08` 已成功推送 GitHub origin。
- 指定 NAS `\\192.168.3.173\羲火君 共享给我\AI诊断\OKWW-Updates\stable\latest.json` 已读回 1.79.08，更新包为 `releases/v1.79.08/okww_update_v1.79.08.zip`，UTC 发布时间 `2026-09-17T08:29:10Z`。报告同步保存到该版本目录。
- 未自动安装或覆盖正在运行的打包版，需用户更新后验证实机绑定与若梦重试。
