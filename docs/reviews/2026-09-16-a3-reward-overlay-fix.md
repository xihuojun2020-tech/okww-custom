# A3 每日领奖遮罩中断修复记录

## 范围与证据

对应用户确认的定向修复，基于完整已发布的 1.79.01（`62ba369d`），在独立分支 `codex/daily-reward-overlay` 实施，未覆盖原工作区未提交修改。

已审阅包：`okww诊断证据_20260916_手动_170800_bb70edae.zip`，SHA256 `84d7fcbd608559641c22acae7a1050957c12a1746555bb70d96e11db82871ea4`。原诊断报告已注册 NAS 保留流程，本记录是实施补充，不修改其审阅收据和报告哈希。

1.79.01 会话中，A3 补跑在 17:06:41 达到 140 活跃度，17:06:52.997 领取核验抛出 `DailyActivityIncomplete`。截图标题仅“获得”，随后出现“点击空白处继续”；旧规则只识别“获得物品/获得奖励”等，遗漏该遮罩。异常阻止邮件、战令、周常与留档。调度保留了失败 A3，但“序列本轮已处理完成”文案容易被理解为全部成功。

## 实施

- `DailyTask._daily_reward_overlay` 在领奖上下文识别简繁中文标题、继续提示与英文提示，先于底层每日页面判断。
- `_restore_daily_claim_page` 最多采样 16 次、点击空白处 3 次；标题动画等待后再点击，连续 3 个无遮罩每日页样本才返回。未知页面不盲目打开 F2，不把遮罩下无红点视为已领。
- 积分领取、宝箱点击后及最终确认复用恢复路径。重新读取所有档位，支持一次领取多档及已领取补跑；保持原有邮件→战令→周常→按配置留档→完成记录顺序。
- 关闭失败继续抛出未完成异常；最后账号失败时明确提示“本轮结束，仍有账号未完成”，附原失败阶段/原因。未改选号、登录、角色战斗实现。
- 版本与更新日志同步为 1.79.02。无新增依赖或任务配置。

## 验证

先添加回归测试，旧代码有 5 项因缺少遮罩识别方法失败；实现后定向测试全部通过：领奖流程 17、每日回归 15、每日结果恢复 14、多账号 113、真实截图 5，共 164 项。

新增脱敏原始图片：`reward_daily.png`、`reward_opening.png`、`reward_ready.png`。生产 OCR 分别识别为普通每日页、opening、ready；遮罩下空红点不作为完成证据。流程测试绑定真实 `claim_daily` 和 `_run_daily_inner`，验证后续顺序，以及遮罩卡住时不执行后续、不写完成。

全量 `./run_tests.ps1 -Group all` 通过：166 个测试文件，1697 项（其中 8 项跳过），0 失败、0 错误；结果目录 `test_out/test_runs/20260916-175629-505`。发布回执另附。

更新包覆盖验证已通过：以 v1.79.01 为旧版基线，412 个受控文件全部一致，目标账号配置保留；SHA256 `6b3403dd575b1671f9c7b5fe5e8fc41963bce8f3c699e0cd20dd76a9ae32ec75`。

## 验证限制

未启动游戏、未操作真实账号；离线测试不等于目标设备已安装或 A3 实机收尾通过。标题动画 fallback 和实际关闭延迟仍需目标设备验收。A3 首轮切人超时属于另一问题，本次未宣称解决。NAS 唯一使用 `\\192.168.3.173\羲火君 共享给我\AI诊断`。
