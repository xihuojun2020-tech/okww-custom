# 1.78.00 多账号每日任务修复记录

范围：落实 9 月 15 日多账号专题诊断方案。证据及包哈希见 `2026-09-16-multiaccount-sept15-followup.md` 与补充报告。本次不重新删除或移动证据，不修改账号配置，不运行真实账号消费。

## 已实施

1. **停错指南页**：侧栏存在只证明已开书。新增正文标题确认，目标正文未出现时最多补点两次，返回大世界重开一次；仍未知明确失败，不在任务页读取体力。每日执行与留档共用活跃度页面识别，不依赖 180 任务条目。
2. **体力串位与单帧失败**：资源值按水平位置绑定，接受空白及全角斜杠，要求唯一的当前 `/240` 与备用整数；领奖失败复用三次新帧读取，并再次确认仍是领奖页。真实旧截图验证结果为当前 80、备用 159，而不是 -1/80。
3. **声骸假完成**：移除微小字符模板作为拾取提示依据，保留明确文字候选；每日调用捕获模式时，每次退场均回读游戏任务。候选成功但仍 0/1 时继续目标队列。完成检查点升级 v2，使用或写入捕获检查点前核对 1/1；全清与捕获签名仍分开。局部补齐成功可清除捕获错误，但不清除全清失败。局部补齐失败后仍进入已达成奖励领取。
4. **奖励漏领**：识别 20/40/60/80/100 档各自红点，逐箱最多点击两次，处理奖励遮罩，再确认页面与红点消失；不再固定点击未达成的 100 档。剩余红点或不明页面均不报告领取成功。
5. **复查后不继续**：未知消耗进度保留 None，不再当零；预算按实际剩余消耗进度向单次成本取整，凝素/模拟/无音及养成规划共同采用。复查后优先补声骸，再最多两步补已确认的消耗缺项；没有进展或缺项未知则停止，不重新跑整套账号任务。材料规划启用时仍走规划，不绕过账号配置。
6. **周本剧情弹窗**：只确认本次已选择原目标后出现的明确剧情前往提示，最多一次，随后核验原周本。重复调用保留同账号同周期的待补检结果；新一轮仍可补检，跨刷新不写完成。

## 备用转换边界（未完成实机验收）

战斗后仍撤销旧活跃度授权，不能为恢复进度放开星声/溶剂或批量转换。

已接入战前最小缺口转换：新鲜未满分、账号绑定、已确认剩余预算、足够备用、明确转换数量和唯一确认按钮同时满足后才确认；之后比较当前/备用两个余额，未知结果锁止重复确认。默认批量数量不等于本次缺口时拒绝。

**现有 NAS 样本没有备用转换弹窗。本版没有臆造滑块或输入框坐标，也没有实现对未知弹窗数量的自动改写。** 因而实际 UI 若没有当前解析器支持的显式数量标签，或默认选择全部备用，该分支仍会保留截图并停止补齐；不能宣称备用转换在目标设备已完全解决。需要真实弹窗样本后才能校准数量控件并完成这一部分端到端验收。

## 验证

- 新增 `TestDailyFollowupImages`：真实资源弹窗、错误页面、160/180 进度、声骸 0/1、四个红点宝箱、剧情提示区域；720/900/1080/1440 缩放回放。所有 fixture 底部 UID 已遮盖。
- 新增 `TestDailyOutcomeRecovery`：位置/歧义解析、未知值、剩余预算、精确转换、满分/过期授权/未知/批量拦截、不确定结果锁止、局部进展及无进展上限、候选拾取不跳过后续目标。
- 更新现有导航、奖励、周本、完成检查测试，继续运行生产复用的账号切换与多账号测试；没有另写切号实现。
- 全量隔离回归：`test_out/test_runs/20260916-134915-001`，162 个文件、1648 项测试，0 失败、8 项跳过。随后新增三个整段日常案例，`TestDailyOutcomeRecovery` 共 13 项通过；周本最终版本再次回归通过。测试入口为 `.venv/Scripts/python.exe scripts/run_test_file.py`，加载与生产相同的 `custom_ok` 覆盖。

## 实机交接要求

安装后核对版本 1.78.00，复测旧的两个失败场景，再跑所选多账号两轮。逐账号核对分数、领取档位、声骸 1/1、当前/备用变化、周本独立状态。对备用转换分支优先提供包含完整弹窗及资源栏的截图；不能将离线模拟输入或截图回放当作实际消费成功。

目标 NAS 仅为 `\\192.168.3.173\羲火君 共享给我\AI诊断`。GitHub 标签与 NAS 更新包使用同一提交；发布成功不等于目标设备已安装或实机验收通过。
