# 若梦声骸装配：按加号与槽位占用确认

## 范围、证据与方案

本机打包版 1.79.04 在 22:47:10 再次于第二位装配后超时。三人姓名核验通过；这次装备的是优先级更高的 index 7，其固定裁剪比对仅 0.419，裁剪左移 4、下移 2 后可达 0.881。此前去锁修复覆盖了 index 8，没有解决所有已装备图标对齐差异，测试覆盖不足。

用户提出识别空槽加号，并明确批准：选择时仍确认目标，装配后只判断加号消失且槽内出现图标；画面不清继续等待。此次按该方案实施，不继续逐个修补具体声骸模板。仅检查本机散装证据，未审阅新 NAS ZIP，未删除日志。

## 实现

- 用已有完整空槽截图裁出 26×26 加号模板；在归一化画面对应槽位的中央小范围匹配，容忍几像素偏移。
- `support_slot_state` 返回 empty/occupied/unknown：加号分数达到 .80 判空；无加号且内部灰度标准差至少 20、边缘占比至少 .08 才判存在图标；否则未知。不匹配具体声骸身份，不以“没识别到加号”单独判成功。
- `_equipped_slots` 先核对编队页面和姓名，再判断槽位，并记录 `support_slot_observation`（页面确认结果及三个槽状态）。每次装配确认当前及此前槽位；收尾和进入战斗前统一确认全部三槽。
- 选择页原有声骸解锁、类别、选中边框、装配按钮确认保留；未修改战斗逻辑、点击坐标或连续导航确认机制。
- 版本与更新日志同步到 1.79.05，基于完整 1.79.04，保留原工作区无关修改。

## 验证

7 个相关测试文件共 106 项通过，0 失败、0 错误：TestEchoesContinuation、TestEchoesContinuationImages、TestEchoesRemainTask、TestEchoesRemainImages、TestUITransition、TestReleaseReadiness、TestTestGroups。

- 三次故障原图 equipment_failure_1/2/3 在 720/1080/1440/2160 下均为 occupied/occupied/empty。
- 原有三槽已装备、三槽空和部分装配图片检查通过；纯黑、纯灰、纯白及去掉加号但没有图案的槽位均不当作成功。
- 生产 OCR 联合回放三张图片：页面、姓名及前两槽同时通过，三槽整体检查因第三槽空而正确失败。
- 连续装配测试使用真实 `_equip_supports`、槽位状态函数和最新 index 7 故障帧；第三槽用显式合成的最终帧，验证顺序 0/1/2 和所选记录 [1,7,1]。合成最终帧不是实机成功证据。

未自动安装补丁、未启动游戏或操作真实账号；未重跑仓库全量测试。图标存在只证明占用，不独立证明图标身份；具体目标通过前置选择确认保证。真实设备完整挑战仍待验收。

## 发布回执

- 提交 `44bf513b`：`fix: verify event support occupancy using empty-slot plus markers`；分支 `codex/echoes-slot-occupancy` 和注释标签 `v1.79.05` 已成功推送 GitHub origin。
- 以 v1.79.04 覆盖升级验证：413 个受控文件一致，账号配置保留；更新包 38238925 字节，SHA256 `0cf120a1e0d79fba226bc79b3cfa889c974fd6478692d1cbe75b503363b9163b`。
- NAS `\\192.168.3.173\羲火君 共享给我\AI诊断\OKWW-Updates\stable\latest.json` 已读回 1.79.05；包 `releases/v1.79.05/okww_update_v1.79.05.zip`；UTC 发布时间 `2026-09-16T15:07:15Z`。
- 报告随 NAS 版本目录保存；未宣称安装器 CI 构建完成或目标设备已更新。
