# 1.78.01 多账号每日回归修复实施记录

## 范围

依据今日 NAS 故障诊断和用户确认的“复苏物品页面点击确认”要求实施。本次发布使用独立工作树/分支 `codex/daily-regression-repair`，从 `7c4be7fb` 分离，未带入原工作区正在开发的 1.79.00 共鸣模拟功能、翻译和历史报告删除。

## 已实现

- 指南敌迹页简中标题纠正为“敌迹探寻”，保留侧栏和正文联合判断。
- open_daily 每次先领取已完成目标，再读取进度与活跃度、更新备用波片策略；claim_daily 先领目标积分，再领宝箱。每次重读按钮，最多 12 次点击、两次下滚、连续两次无进展即报未完成，不点击前往/已领取。
- 活跃度使用专用数字区域；原图漏读时二值化纹理背景再 OCR，仍保留未知。结果截图留档也同步使用新区域和回退。
- combat_once 内允许先确认真实复苏物品弹窗，每次弹窗最多两次确认、每轮战斗最多三次成功原地复苏。弹窗消失且队伍回来后，通过新的 CharRevivedInPlace 信号退出旧角色动作、刷新队伍并继续当前挑战。此信号不继承死亡异常，不落入 DomainTask 的离场恢复分支。
- 原地复苏失败仍调用原任务的 revive_action：副本退场治疗、特殊挑战禁止传送等既有策略保留。AutoCombatTask 的历史限制保留。并非给所有恢复结果统一改含义，也没有修改角色轮转或账号切换实现。
- 基础恢复方法不吞掉停止、丢帧、进程丢失、配置保护中断。低血量复苏后刷新角色，并保留用户已有治疗切换设置，不伪称物品回满血。

## 验证方法及发现

- 真实故障图测试先在旧实现失败，再验证修复：敌迹页、零分/多个待领取、复苏弹窗。0 和既有 90 分样本在 720/900/1080/1440 分辨率回放，领取/前往按钮区分；截图底部 UID 已遮盖。
- 新增流程覆盖目标积分→宝箱→重复执行、无进展上限、未知数字时按钮进展、领取前后备用授权、奖励弹层、复苏成功不退出、副本生产战斗继续、失败回原策略、次数上限和停止异常。
- 同步完成留档的旧坐标 mock，按帧组织 OCR 测试以允许同帧预处理重读。
- 全回归发现 TestChar 的剩余增益测试把两者均设为约 13 秒，却依赖实时钟选定固定对象；改为明确 12 秒与 13 秒，不修改生产切人逻辑。
- 独立工作树最初直接链接整个 .venv，触发路径归属测试；改为独立解释器入口、共享依赖库，并补齐 pythonw。没有放宽生产路径保护。

## 验证边界

离线测试与图片回放不等同实机点击结果。未启动游戏，未替用户消耗复苏物品/波片，未操作账号。A1/A3/A4 的新一天、部分完成未领奖、已完成及原地复苏续战仍需目标设备验收；备用波片转换控件的旧实机待验项不在本次修复中冒称解决。

版本更新包采用与 Git 提交一致的生产来源，不包含测试图片、用户配置、日志或截图。

## 离线验收结果

- 独立发布工作树测试清单 164 个文件最终全部通过，1670 项测试，8 项跳过，0 失败、0 错误。修正测试数据及测试环境后补跑失败/未执行文件，汇总位于 `test_out/test_runs/20260916-162358-922`，不是声称首轮一次全部通过。
- 今日真实图片回归 4 项，包含四分辨率 0/90 分与按钮识别；流程回归 15 项。既有账号切换、多账号、领域恢复和完成留档测试均纳入完整清单。
- `verify_update_package.py --previous-ref v1.78.00`：408 个生产文件一致，合成账号配置保持不变。
- 更新包 `okww_update_v1.78.01.zip`：38211181 字节，SHA256 `9480d2becc3ff6def80350f6f42657c54e5d15d6a4efcd4873801242e887f3ac`。
- Git 推送及 NAS 发布结果另行核对，不将本节离线通过视为设备已安装。

## 发布回执

- 生产提交 `e49446d2b93d619973969877801b8f9b543a7dcb`，附注标签 `v1.78.01`，分支 `codex/daily-regression-repair` 已成功推送到 `xihuojun2020-tech/okww-custom`，远端标签解引用与生产提交一致。
- NAS `\\192.168.3.173\羲火君 共享给我\AI诊断\OKWW-Updates\stable\latest.json` 已回读确认版本 `1.78.01`，包大小和 SHA256 与上述一致；发布器完成写入校验。
- 未自动安装到本机或其他设备，未执行实机每日任务，未删除日志。GitHub 标签已推送不代表云端安装器流水线已完成。
- 原工作区保留本次修复文件和其他并行改动。后续 1.79.00 等版本发布须包含本修复提交（合并本分支或等效纳入全部相关改动），避免新功能版本覆盖本次修复。
