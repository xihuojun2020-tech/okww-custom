# 全面审查整改执行记录

基线：`3fd591a5` / `1.31.15`。对应[实施计划](../superpowers/plans/2026-09-06-review-remediation-plan.md)。本记录区分已实现并验证的改动与后续工作。

## 批次 A：1.32.00

已实现 T00–T06，修复审查报告 R01–R06；同时为快照一致性提前实施 T12 的操作内图复用，并移除发布验证测试中固定写死的旧版本号。T12 的规模测量仍在性能批次完成。

- 测试入口提供专属进程树、默认 180 秒超时和逐文件 JSON 结果；覆盖输出 OK 后退出挂起、子进程泄漏、不相关进程保留、无结果直接退出等场景。
- 发布拆为准备与激活，导入、编辑、删除共用事务；旧动态文件和账号状态删除均位于提交前可恢复区域，预览冲突阻止覆盖。
- 持久日志使用固定目标键和带 SHA-256 的旧字节内容；不从日志接受任意恢复路径。日志所在目录经过 ACL 保护，永久备份继续使用既有快照机制。此实现避免依赖外部备份 hook 的具体目录格式。
- 不同内容版本根据实际 active 恢复，同版本动态更新根据持久 COMMITTED 标记恢复；提交后维护错误保留新图和恢复材料。Windows 命名互斥量与共享可重入锁协调多个实例及运行数据写入。
- 每轮固定账号顺序、参数、起点和返回账号；普通编辑下一轮生效，账号删除/身份修改在后续输入前阻止继续。Daily 和子任务共用运行身份检查，finally 释放绑定。
- 停用备用名不再进入任何匹配候选；连续测试仍复用生产切换，默认 A1→A3→A4；测试页正确读账号仓库并报告错误。
- 任务页改用一个当前序列成员只读展示，验证 0/1/10/11/50 个序列。边界测试额外发现并修复了 working/master 序列归属排序不同产生的完整性误报。
- 新增界面文字已同步并编译现有 5 个 gettext 目录。

验证结果：全量入口 80 个文件正常结束，704 项用例中 696 通过、8 项因原有本地图片缺失跳过，无超时。随后补充 4 项恢复/输入测试并完成相关文件复测：配置包 24 项、发布 10 项、删除 5 项、运行集成 14 项、日常活跃度 10 项均通过。全量结果位于本机 `test_out/test_runs/20260906-221639-477/`，控制台记录为 `test_out/code_review/batch_a_regression.log`。

关键证据：10 个强制进程退出场景覆盖准备、旧文件更新、active 切换前后及 COMMITTED，包含新旧 revision 相同的重新导入；两个受控 Windows 进程持相同预览同时提交，恰好一个成功、另一个获得版本冲突；编辑与完成记录并发不丢失记录；损坏恢复快照保留并拒绝猜测。

验证边界：使用临时目录、合成身份和离线输入替身；没有启动或操控游戏，也没有验证本版本安装器的真实安装、启动或在线连续登录。提交和标签推送不等于 GitHub 构建产物已通过验证。

## 批次 B：1.32.01

T07–T11 的程序修复及离线验证已完成，解决 R07–R11；安装器解包/实装验收仍与 T15 合并，不计作本批已验证内容。

- `storage.resolve_config_backup_dir` 是纯路径解析器，`get_config_backup_dir` 读取设置；启动、清理、序列修复、Daily 及通过 Daily 转发的设置按钮共用同一规则。备份位于 configs 内的设置统一回退默认目录。
- 整体恢复持有账号变更锁并拒绝运行/暂停任务。源仅允许选定备份目录，目标固定为 config_dir，继续拒绝穿越、符号链接及 Windows junction；manifest 拒绝重复、绝对或越界条目，复制使用预检时保留的清单。
- 暂存和旧配置目录都在目标父目录。恢复日志改放目标侧 `.configs-restore/journal.json` 并使用 ACL 与 fsync 原子 JSON 写入，启动在账号初始化前处理；这样配置目录暂时不存在时也能发现日志。兼容选定备份目录的旧 v1 日志。未提交的目录交换回滚，activated 持久化后完成清理；损坏日志抛错并保留证据。
- 恢复后调用账号事务恢复方法，重新验证 active、manifest 和旧投影一致；发布服务支持显式 config_dir，用于在任意命名的快照/暂存目录执行相同校验，同时补充 active revision 与 bundle 路径边界检查。
- 恢复期间清理保留快照；正常容量和数量清理在删除失败或无进展时警告并退出，下次可重试。新建快照不会在返回调用方之前被清理掉。
- 月卡配置 UI 使用 0–23，旧 24 在任务初始化时转为 0，防止控件截成 23；执行层仍显式兼容并验证外部值，保留提前 30 秒和日期推进语义。
- ZIP 检查覆盖多层 configs、运行目录、大小写、分隔符、越界、重复和链接；通知例外只接受空配置或已关闭通知并清空密钥/身份的默认值。命令分别报告已检查 ZIP 与尚未检查的 EXE/MSI 数量，CI 页面名为 artifact-content-check。
- 角色热替换按特征注册表归属匹配，保留现有状态及额外切换/退场计时。直接 BaseChar 子类可从内置连续切换 v1、v2 再回内置；运行、暂停或动作中仅保存并提示下次重建/重启。加载时直接编译当前字节，避免同秒同长度源码命中旧 .pyc。未新增默认导航。
- 两条新 UI 文案同步现有 5 个 gettext 目录，保留原翻译并编译 .mo。

验证：全量入口 81 个文件、729 项用例，721 通过、8 个原有缺图跳过，无失败、无超时。结果目录 `test_out/test_runs/20260906-224444-296/`，日志 `test_out/code_review/batch_b_regression.log`。关键重点用例包括独立仓库以及 C: 备份盘 → E: 配置盘的实盘往返、账号图/完成文件逐字节一致；准备、校验、第一次改名、第二次改名及提交后中断恢复；源变化、复制失败、目录占用、最终校验失败、损坏 active 和日志；清理失败及 no-op 有界退出；午夜和日期推进；角色类型/状态迁移。

验证边界：目录替换中断使用真实临时磁盘状态和退出式异常，非整机断电测试；没有操控真实游戏或账号，也没有安装、启动本版本 EXE/MSI。安装器内容清单与实装验收仍属于 T15。

## 批次 C：1.33.00

T12–T14 已完成。详细数据见[性能整改测量](2026-09-06性能整改测量.md)。

- 2／10／50 个合成账号的投影和序列快照各校验一次；投影中位数约 1.4／2.8／9.8 ms，快照约 1.4／3.2／14.1 ms，不增加跨操作永久缓存。
- 真实同步保存时间约 180／278／834 ms，50 账号序列更新约 1010 ms，达到计划的异步化门槛。账号各项提交、序列写入、维护页导入/恢复/验证和启动备份改用 QThreadPool；草稿与版本在 GUI 捕获，确认及完成回调回到 GUI 线程。
- 每个操作控件组只允许一个提交；失败保留草稿，外部刷新也不清掉用户已编辑表单。迟到结果核对选中账号，窗口销毁后自动断开 Qt 回调。重绑和删除序列支持预览版本参数，整体恢复支持预览清单与账号图版本比较。
- 50 账号实际 GUI 保存约 882 ms 完成，点击提交约 1.1 ms 返回，10 ms 定时器最大事件间隔约 62.6 ms；追加 500 ms 慢盘延迟后约 1360 ms 完成，最大事件间隔约 48.6 ms。未缩减磁盘校验或事务步骤。
- 证据采样先过滤后复制，保留 60 秒/30 帧/2 秒规则并增加每会话 128 MiB 上限；强制帧也受限，超大单帧只记录原因。失败写入移交队列所有权，写完释放；成功直接清空。4K 合成测量保留 5 帧、118.652 MiB，成功后归零。
- 对新增输入保护的测量显示：Daily/Multi 在 2／10／50 账号规模约 3／6／20 ms；保留完整校验，真实高频动作体验仍在实机验证边界内。
- 检查 GitHub 后发现 B 的 CI 测试在 RUNNER~1 与 runneradmin 等价路径比较时失败，安装包阶段未执行。已统一测试预期与恢复路径的 canonical 比较，新增真实 Windows 短/长路径恢复日志用例，未移动或重写 B 的已发布标签。

验证：全量 81 个文件、742 项用例，734 通过、8 个原有缺图跳过，无失败/超时，目录 `test_out/test_runs/20260906-231121-081/`，日志 `test_out/code_review/batch_c_regression.log`。随后增加 Windows 短路径恢复测试并完成备份 22 项、安全备份 5 项、账号初始化 4 项复测。GUI 专项共 18 项通过，包括注入 500 ms 写盘、线程身份、定时事件响应、重复提交、选中账号改变、草稿保留、迟到结果及导入确认线程。

远端状态：A 的 GitHub Actions 已成功；B 的 Actions 因上述路径测试失败，未完成安装器构建。C 的推送与后续 CI/产物状态分别核对，不将本地测试当作安装器实装验证。

## 批次 D：1.33.01

T15 的依赖/来源整改与 T16 的离线风险验证已落地；安装器完整生命周期和真实游戏矩阵仍未验收。执行跨越 2026-09-06 至 2026-09-07。

- 运行依赖继续使用 ok-script 1.0.190 / onnxocr 0.0.20，固定 OCR 和直接/开发依赖，并补齐框架要求的 mouse 0.7.1；移除无生产导入的 GitPython 声明。Python 支持说明统一为 3.12。setup.py 从 config.py 和 requirements.txt 读取元数据，移除无 .pyx 输入的 Cython 步骤与重复包遍历。
- 在 `test_out/code_review/clean_env_d` 新建隔离虚拟环境，安装两份锁定文件后 pip check 通过；通过生产 `_sync_custom_ok()` 同步覆盖文件，发布就绪 15 项和主窗口/受控退出 6 项在这个环境通过。本地 .venv 补回缺失的 PySide6/Addons 6.9.1 元数据与开发依赖后 pip check 也通过。
- 旧 `~k` 与 `~k_script-1.0.185.dist-info` 的版本、时间和命名与 pip 卸载备份相符，因没有当时安装日志，不断言具体中断原因。两目录原样移动到被 Git 忽略的 `test_out/code_review/environment_quarantine/` 保留。与干净框架逐文件比较，唯一不在覆盖目录的本机差异属于已弃用 nemu_ipc Android 模块，不属于当前 PC 生产路径。
- About 改为读取随包更新日志；发布说明也提取同一明确版本章节。删除内嵌历史与无条件 return 后死代码。版本验证拒绝仅在正文提到版本、重复当前版本条目和不匹配标签。
- 增量包收录完整 src/custom_ok、资源/翻译和对应依赖声明，不读 .venv 或 configs；仅选受控文件，必需源缺失即失败。应用到临时 v1.33.00 源码后核对 236 个文件 SHA-256，并验证合成配置原字节保留。该更新包定位为源码目录更新，目标设备仍需用自己的 Python 3.12 环境安装锁定依赖。
- CI 固定 pyappify 启动器源码 v1.2.3（与批次 A 成功构建相同），移除会重写 config 版本/依赖的框架内联步骤，增加依赖检查、增量内容验证、安装器解包清单和源码核验。离线安装器缺源码、版本/文件不同或私有配置混入时不能进入发布步骤。手动候选如下载到不同标签源码，同样拒绝，而不会把另一版本当作当前候选。

### T16 逐项结论

| 事项 | 结论与证据 | 验证范围 |
| --- | --- | --- |
| 启动器退出归属 | 已修复：保存完整可执行路径匹配的祖先进程，psutil 身份检查后只结束其后代和本启动器；不再按全局进程名杀进程 | 两目录同名 C# 惰性启动器实际运行，一份退出后另一 PID 保留；保留 Python venv 中间启动链，无法确认归属的孤儿 WebView 不处理 |
| 跨实例写入 | 已由批次 A 修复；两个受控进程同预览提交，一个成功、一个冲突 | Windows 命名互斥量、临时配置目录；并发记录不丢失 |
| 损坏 active 回退 | 已由批次 A 修复；真实 Daily/Multi 读取和输入保护拒绝损坏发布图 | 不以旧 master 静默替代已有损坏 active；历史从未发布格式保留兼容 |
| 停止状态 | 已修复：stopping 与 stopped 分开；框架 disable/check_enabled 抛 TaskDisabledException，实际输入阶段停止后不再增加投递计数 | 选号、登录、世界等待三个阶段；证据收尾、捕获释放、鼠标状态恢复；仅当前运行任务可改变共享协调器，取消排队任务不误停另一入口 |
| 证据隐私 | 已修复实际 StartTab 日志导出入口：嵌套 JSON 和文本字段脱敏，Authorization/Cookie 整行去敏；图片、未知二进制、链接和无效文本不进入分享 ZIP | 合成手机号/昵称/凭证写入事件、日志和图片，通过实际 GUI 导出方法验证不泄露这些样本；本地原件保持不变。不能把规则脱敏当作任意自由文本的隐私保证 |
| 两套完成记录 | 当前生产写入/读取统一走 ConfigIntegrityService 的 configs 运行状态；AccountRepository 的独立逐账号完成 API 无生产调用 | 调用点检索与现有集成/备份往返覆盖主路径；独立 API 保留，明确 configs 快照不覆盖其运行状态目录，不合并/丢弃记录 |
| 战斗时间边界 | 已修复：combat_once 的非正 wait_combat_time 在任何输入前抛 ValueError | 0、负数和正常正数的假场景；现有源码调用均为正数 |
| 检测后端与矩形输入 | 已修复：宽高对应增益、单检测维度、原始输出被修改；可选依赖延迟导入并给出可操作错误 | 两后端 384×640 / 640×384 / 640×640 模型目标与横竖图组合，0/1/2 候选；缺失 ONNX Runtime；不是任意模型动态输入的推理验证 |
| 视觉热点 | 已测量并优化：复用相同模板的 360 张旋转图 | 三张既有小地图、每张 5 次；角度和置信度逐次一致，缓存命中减少约 8–32 ms，额外缓存约 3.23–13.15 MiB；没有改搜索范围/阈值 |
| 实际游戏边界 | 未验证 | 失焦、DPI、多显示器、断线、长动作停止和按键释放仍需要另行安排的受控实机环境；离线输入替身不能替代 |

### 安装器实物证据与未完成项

已从 GitHub 下载批次 A 的 China 离线安装器，文件大小 576,427,620 字节，SHA-256 与 GitHub 产物摘要一致。通过官方 7-Zip 25.01 解包且不运行安装器，核验 13,574 个文件；repo 与 working 两份源码各核对 230 个关键文件。每份有 168 个文件仅 CRLF/LF 不同，保留实际原始 SHA-256，不把规范化比较称作原始字节相等。未发现 configs、账号备份或运行日志混入；公开 Git reflog 属于启动器更新所需的克隆元数据，单独处理。

本机证据：`test_out/code_review/installer_a_manifest.json`、`installer_a_source_comparison.json`、`installer_a_listing.txt`。检查工具为可重复运行的 `scripts/inspect_installer.py`，已加入 CI；纯 ZIP 检查继续由 package_smoke.py 完成。

**T15 最后一项仍未完成：** 本机没有 Windows Sandbox 和可用的 Hyper-V 命令，未安排其他隔离 Windows 环境，因此没有在本机执行安装、第一次打开、正常退出、再次打开、旧配置副本迁移与卸载；没有运行/修改 `E:/game/okww owener` 的现有安装。上述实物来自 A，只证明已测试格式的解包检查能力，不冒充 D 安装器验收。D 自己的构建和内容核验结果以其 CI 为准。

### 发布与验证记录

A（v1.32.00）和 C（v1.33.00）的 GitHub Actions 已成功；B（v1.32.01）因 Windows 短路径测试失败，未完成安装器构建，修复在 C。旧标签均未移动。

D 最终全量为 81 个文件、760 项用例：752 通过、8 个原有缺图跳过，无失败或超时。结果目录 `test_out/test_runs/20260907-001018-993/`，日志 `test_out/code_review/batch_d_regression.log`。期间旧的 About 版本字面量测试已更新为共同来源检查；新增停止归属回归后重新完成全量。框架仍可在退出时报告既有 Qt/GC ResourceWarning，但受控测试进程均正常结束。

最终源码增量包 `okww_update_v1.33.01.zip` 校验 236 个文件，临时 v1.33.00 更新后配置保持不变；SHA-256 为 `aa8c9765296ff710b4fdf244e916015fa75fd1a8a35ee7c5f7afb91531dd43a0`。它是本机候选文件的原始摘要，CI 重新生成的 ZIP 因时间戳和文本换行可能有不同包摘要，仍需通过内部文件一致性检查。固定版本检查 `validate_release.py --tag v1.33.01` 通过。

本批发布分支为 `codex/account-switch-foreground-bitblt`，注解标签 `v1.33.01`，目标 `origin`。提交/推送与 D 的后续 GitHub 构建状态分开核实；不把推送成功当作安装/实机验证通过。
