# 1.60.00 完成检查与截图复制验收

日期：2026-09-12。范围：用户第 9、10 项需求。实机验收由用户手动完成，开发过程未控制游戏或发送微信消息。

## 已实现

- 每日：活跃度、残像聚落、战令，北京时间每天 04:00 切换；每周：周本、每周乐园，周一 04:00 切换。
- 新截图：若梦仍有回声、群声共振模拟域、清弦纪流年、第二索拉·诡影迷踪、深塔、海墟、矩阵。保留最新图直到更晚采集的截图保存，旧图补传不会覆盖新图。
- 未知新图不继承旧完成结论；本周期其他可信记录单独展示，不将不同时间的正常变化永久标为冲突。历史和原图长期保留。
- 账号提醒补齐 12 项、按三组排列，旧占位提醒和证据保留待归类；不改变自动任务配置。
- 账号页面复用已有局部密度方法，折叠标题最小 40px，分组和设置行继续收紧，其他页面默认值不变。
- 卡片按钮、预览按钮、右键菜单复制原图像素；缺失、解码失败不覆盖剪贴板。

## 验证

全量隔离运行：`test_out/test_runs/20260912-095147-295`，115 文件、1209 项，1201 通过、8 项既有跳过，无失败或错误。

全量之后补充故障测试并最终定向运行：`TestCompletionEvidence.py` 29 项、`TestCompletionCheckUI.py` 7 项通过。结果在 `test_out/final-TestCompletionEvidence.py.json` 和 `test_out/final-TestCompletionCheckUI.py.json`。覆盖迁移前备份、事务异常回滚、时间无效保留、重复迁移、跨日跨周、旧图补传、同时间排序、保存失败保留已有图片、跨账号切换后复制固定证据、原图像素与尺寸、缺图不改剪贴板、无周期项目待检查筛选。

离屏 Qt 渲染：100%、125%、150% 缩放，1040px 和 680px 账号窗口，以及 1200px 完成检查。显式加载微软雅黑以保证离屏字体正确。检查三分组、长活动名称、复制按钮、换行与滚动；合成账号相同展开状态下，内容高度由 1535 降至 1319 逻辑像素，减少 216（约 14%）。图像与尺寸记录位于 `test_out/completion-160-visual/`，不提交合成截图到版本包。

首次测试发现迁移备份 SQLite 连接未显式关闭，已改为 `closing` 并通过文件清理与回滚验证。布局检查发现首帧按旧视口宽度排列，已延迟到布局完成后重排并复验。

## 实现与方案的对应

进一步收紧账号布局时发现现有 `compact_settings` 已处理基础内边距，因此复用并增加账号专用参数，未修改三个共享组件的默认构造行为。迁移备份为数据库索引快照；原图和原始 JSON 说明保持不可变，数据库内 metadata 与查询周期同步更新。

旧活动提醒可以取消旧项并选择正式项后保存；历史证据保持原身份，本版没有批量重新归类历史图片的工具。手动完成确认仍通过已有保存证据弹窗完成，不把旧图确认自动转移至新图。

## 发布与实机边界

源码版本与发布标签：`1.60.00` / `v1.60.00`，GitHub 仓库 `xihuojun2020-tech/okww-custom`。安装器 CI 状态未验证，不将源码标签等同于安装器构建成功。

更新包：`dist/completion-160/okww_update_v1.60.00.zip`，36,733,014 字节，SHA-256 `f37ca8dbff56b01911e2ae76ca3077891c5ddadc6fc3cd93593c463eaf55169a`。以 `v1.59.00` 为基线的离线更新验证通过，358 个受控文件一致，账号配置保留。

NAS 已发布并回读验证：`\\192.168.3.172\羲火君 共享给我\AI诊断\OKWW-Updates\stable\releases\v1.60.00\okww_update_v1.60.00.zip`；`stable/latest.json` 指向 1.60.00，远端包哈希与本地一致。沿用 .172/.173 回退能力，本次实际发布地址为 .172。

微信实际 Ctrl+V 粘贴、真实游戏导航和异机安装由用户验收；已验证 Qt 剪贴板的原图格式与像素。微信指定联系人绑定及自动发送仍属于后续阶段，本版未部署发送自动化。
