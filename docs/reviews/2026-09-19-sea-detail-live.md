# 海墟详情与信物页实机修复记录（1.80.03 待验收）

## 证据与根因

本机安装位于 `E:/game/okww owener/data/apps/okww-custom/working`。运行截图实际存储位置由 `configs/runtime_storage.json` 指定，不是安装目录旧 screenshots。

1. 20:07:43 的 `sea_ruins_timeout` 截图已在第7关详情页。原逻辑标题识别为真，数字局部OCR结果为空，导致“船体点击后未确认7层”。补充数字7图像与险滩名称联合复核，非7层继续使用原OCR，不放宽成任意详情页。
2. 已通关页面按钮是“再次挑战”；实机点击头像仍可打开编队，无需重置成绩。入口兼容开启挑战、开始挑战、再次挑战，仍检查当前层号。
3. 20:45:18 截图已打开下半信物页，默认选中的旧信物次数为0，底部是“数量不足”。原 `_token_page` 强制要求“携带”按钮，误判页面未打开。改用标题和实际卡片确认；真正装备时仍检查数量、按钮及装备后的图案。
4. 21:00:20 已完成上半场，出口转镜头因游戏不在前台停止。用户确认出口标记与周本奖励标记行为一致并批准复用周本方案；最终移除 `_turn`、系统相对鼠标输入和 `ensure_in_front`，直接复用 `walk_to_box`。海墟标记适配为屏幕Box，终止条件为准确F提示；状态丢失、标记丢失、超时、取消均由finally释放按键，按F前再次获取帧确认提示。

## 实机进度

- 第1轮：详情复核通过，识别并应用两支完整预设，上半信物成功；下半信物页误判后安全停止。
- 第2轮：信物页修复后，两队预设及信物通过，进入地图、F开启挑战和上半自动战斗通过；上半1410分，出现“前往下半海域”。出口前台保护停止。
- 通过桌面控制确认右上角用户指定图标能从信物页面返回大世界。
- 用户不希望前台转镜头，改为后台方案；准备实测时游戏进程已关闭，日志连续报告no frame available，后台出口尚未实机验收。

## 本机更新及限制

停止程序后备份原任务文件和config至 `E:/game/okww owener/backups/sea-1.80.03-test`，仅替换任务文件、版本配置并添加数字模板。未修改账号配置、预设成员或重置成绩。最终后台出口代码需要新进程加载；安装和发布状态以交付消息为准。

新增已通关详情、默认信物数量不足的脱敏截图回归。详情页在720p/1080p/1440p正确识别第7关，拒绝第8关，并识别再次挑战按钮。原海墟规则、流程、每日领取、多账号回归继续通过。

四张用户提供的出口局部素材均纳入识别与HUD排除测试。新增模板只含主体，不含方向箭头或距离文字。保留既有三种完整场景和分辨率回归。共享周本寻路实现不作修改。

尚未完成后台出口、下半、结算、继续挑战及8—11层端到端验收；不宣称任务已可全程顺利执行。下一步需用户打开游戏并进入可测试状态。

最终验证：正式分进程测试器运行周本任务/截图、海墟规则/流程/截图、每日领取与多账号共7组200项，全部通过。停止本机旧进程后已部署后台出口文件，部署前版本保存于 `E:/game/okww owener/backups/sea-1.80.03-background`。用户明确要求版本更新后自行手动测试，因此不再自动启动游戏或任务。
