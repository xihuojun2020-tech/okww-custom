# 多账号每日任务修复与验收记录

版本：1.75.07。对应计划：`docs/superpowers/plans/2026-09-15-multi-account-recheck-repair.md`。

## 实际根因与修复

NAS 1.75.02 会话 A1/A2/A3 在打开索拉指南时失败。A1 日志明确记录 18.02 秒、输入 0 次。将真实图片送入生产检测函数后，世界队伍判断为真，同时灰色指南页签以约 0.33 分数超过旧 0.30 阈值。这使通用导航同时看到 source 和 target，按保护逻辑进入 UNKNOWN。

修复在 BaseWWTask 的指南页面边界：排除世界队伍 HUD，目标页签阈值 0.60，并要求另一个指南页签达到 0.70。保留通用矛盾状态禁止输入的行为。已打开指南时直接选页签；导航后使用同一返回帧再次验证按钮，避免回到宽松的 wait_book 检测。没有增加无限重试或全局放宽队伍识别。

GUI 的 TaskTab 原来在格式化过程中读取后台仍在修改的 task.info；现在每次刷新先对普通 dict 做浅拷贝，摘要、行数和表格使用同一快照。修改在受版本控制的 `custom_ok/ok/gui/tasks/TaskTab.py`，随更新包覆盖，不靠修改本机 site-packages 交付。

不可达目标抛出 TargetUnavailable；多账号失败记录增加 retryable。包含明确因果链的梦魇包装异常也可识别该原因。此账号仍为 pending 且不进入 done_set，本轮不自动补跑；用户解锁后新启动一轮仍可尝试。其他瞬态失败沿用现有“正常轮后最多一次补跑”和已有登录恢复流程，没有新增叠加重试。

导航错误增加“未发送输入／输入后未确认切页”分类，记录 source/target、帧时间标识、窗口存在/可见状态，仅在状态变化时记录日志；失败保存有限截图，取消不额外保存失败截图。用户停止及身份/配置保护维持原有传播路径。

## 旧问题验收

- 未知体力三次新帧复读、真实零体力、备用消耗限制、用户停止已有实现，回归通过。
- 领域退出已有新帧检查、世界/领域联合检测及连续稳定窗口；本次核对 wait_condition 的 settle_time 实际为连续成立才返回。
- 不可达目标原本已有识别，本次补齐调度层不反复补跑。
- 梦魇检查点已有账号绑定、模式/目标签名及 04:00 切日检查；没有新增通用持久化调度框架，体力/活跃度仍复查。
- 未改生产选号/登录方法，TestAccountSwitchTask 仍复用生产逻辑，A1/A3/A4、别名与掩码识别相关测试通过。

## 测试结果

使用仓库 `.venv/Scripts/python.exe` 和 `scripts/run_test_file.py`，后者加载与发布一致的 custom_ok 覆盖。完整测试清单已补登记漏项，包括本次回归和此前新增的测试。

工作区全清单执行结果：160 个测试文件完成，1,607 项测试中 8 项跳过，0 失败、0 错误。测试结果 JSON 在 `test_out/test_runs/20260915-142113-046`。首次失败的旧夹具已修复后复跑：活动清单硬编码计数改为实际目录契约；截图恢复夹具使用受控时钟验证两秒退避；每日任务夹具补齐执行器和正确类型的配置默认值。未为迁就测试修改这些生产功能。

新增覆盖包含：A1/A2/A3 世界截图和真实指南在四种分辨率的 16 个回放组合；已打开指南不关闭；未知页面不输入；不确定输入不重复投递；不可达包装异常不重跑且可在新一轮恢复；两组各 10,000 次 GUI 刷新（格式化中途修改、后台线程增删），摘要与行数一致。

图像测试仍有框架退出时 GC ResourceWarning，以及部分 Qt 弃用提示；没有导致断言失败，不能据此认定生产内存泄漏。

## 发布和验证边界

从本次提交快照单独构建、校验并发布更新包，防止工作区其他活动修复混入。提交、NAS 包哈希及最终发布验证结果在发布后补记。

没有在另一台设备操作游戏，因此尚未完成 30 次真实指南开关、两轮多账号每日任务或 Windows DPI 变化测试；软件缩放回放不能代替这些实机检查。没有证据声称远端已安装本版，也没有宣称 GitHub 安装器 CI 已完成。

已确认本次代码消除了复现样本中的错误页面组合；18.02 秒为旧失败等待时间，不是实测平均提速。CPU、RSS 和整体成功率没有同设备修复后数据，不给出百分比提升承诺。
