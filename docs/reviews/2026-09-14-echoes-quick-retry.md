# 1.70.04 快速编队进入失败修复

## 本机证据与结论

2026-09-14 15:27:03 日志确认 A3 特征码核验成功，与期望账号一致。15:27:20 抛出“未进入快速编队页”，调用位置为 EchoesRemainTask._navigate 中点击快速编队之后的页面等待。15:27:21 故障截图仍显示三人编队、右下“快速编队”，并未进入角色列表。

旧归一化坐标 (0.695, 0.919) 在截图按钮内部，不能归因为明显坐标错误。旧实现首次识别按钮后立即点击一次，随后只等待 8 秒，不等待页面稳定也不重试。页面动画尚未完成或输入偶发丢失均可能造成该结果；原日志不足以确定具体是哪一种，不能声称已实证动画原因。

## 实施

在隔离工作树实现 _open_quick，使用原账号 guard、next_frame、sleep 和 _click。每次最多等待 4 秒，连续三帧（间隔 0.35 秒）确认按钮中心偏移不超过画面尺寸的 0.5%，再点击新识别的中心；坐标除以当前帧宽高归一化，支持 1080P/2K。

点击后等待角色列表最多 3 秒，再读取新帧检查。已经进入直接完成；仍有快速编队按钮才进入下一轮稳定确认，总共最多点击三次；页面已变化立即停止。延迟到下一轮才进入时记录 entered_late，不追加点击。用户取消和账号核验异常正常向外传播。

每次在日志记录时间、坐标、次数、等待耗时和结果；记录也加入 last_result.quick_entry_attempts。使用现有截图接口保存 echoes_quick_*_before/after，遮挡顶部 2.5% 和底部 2.5%，避免上传性能叠层及账号特征码。截图失败记录异常类型；不会因此打断已经成功的编队，也不承诺失败时一定存在截图文件。

本次只修改快速编队进入流程，未开启挑战，不修改角色选择顺序或账号绑定。

## 验证

本地 .venv 运行：TestEchoesRemainTask 20 项、TestEchoesRemainImages 1 项（多组图像子测试）、TestAccountFeatureVerification 19 项、TestCharacterTrial 41 项，共 81 项通过。

新增首次点击无效后重试、最大三次、页面变化停止、已进入不点击、延迟切页取消重试、不稳定按钮零点击、用户停止、截图失败隔离测试。点击中心归一化覆盖 1080P 和 2K。

真实 15:27 故障截图已脱敏加入 fixtures；与已有页面在 720P、1080P、1440P、2160P 下做实际 OCR，验证编队按钮与角色列表判据互斥、点击中心在按钮内。缩放样本不等于四种分辨率分别实机运行。本轮没有操作真实游戏，最终点击响应仍待验收。

## 使用端验收

更新并重启 1.70.04，确认每日任务选中目标账号及特征码绑定正确，运行若梦仍有回声。预期进入编队后稍等页面稳定，再进入角色列表并选试用三人，完成编队即停止。若首次输入丢失，可看到重试记录。

若失败，提供同一时间日志和 echoes_quick_* 截图；结合 result=still_formation/page_changed、点击坐标和 before/after 对照判断。不应仅根据总错误文本再次猜测坐标偏差。

本轮查阅本机文本日志及独立截图，未审阅 NAS ZIP，不执行 ZIP 已审阅标记。发布仅暂存本次任务文件，保留原工作区其他修改。
