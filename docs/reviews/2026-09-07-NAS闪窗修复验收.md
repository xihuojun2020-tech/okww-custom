# NAS 补传闪窗修复验收（1.37.01）

## 原因与修复

本机每分钟执行的 `okww-diagnostics-1696e9d4ce19074b` 原先直接运行 `.venv/Scripts/python.exe`，以交互用户启动时会短暂创建控制台。任务安装器现改用同目录 `pythonw.exe`；后台上传子进程原有 CREATE_NO_WINDOW 保留。

增加任务修订号 2，旧的“已安装”缓存不能跳过本次迁移。本机任务已立即修正，并读取实际动作确认使用 pythonw.exe。新增 `-Preview` 可仅查看任务动作，测试不需要注册真实任务。

## 上传核对

- 修复前核验本机已有的 5 个 NAS 完整批次，全部校验通过，涉及 2 次合成测试运行和 1 张合成截图，待补传数为 0。
- 最新成功时间为 2026-09-07 22:05:45；这些不是实际游戏运行日志。
- 本机没有发现正在运行的 main.py 主进程，该安装的增量收集边界尚未由正式程序启动建立。
- 修复后只创建运行 `20260907-223003-13c42f352d19`，未手动触发上传；静默计划任务在 22:30:35 执行，独立上传 2 个新测试批次，返回码为 0。

## 验证

- 12 项诊断策略测试、15 项诊断链路测试、1 项界面测试通过，共 28 项。
- 新增回归覆盖旧缓存立即迁移、重复检查缓存、安装脚本预览选择同一环境的 pythonw.exe。
- 测试早期任务命令模拟未隔离 Windows 模块自动加载，产生过一个指向测试临时目录的任务；已按名称、所属安装目录和精确测试参数核验后删除。最终测试改用无写入的 Preview 模式。
- 版本校验通过；从 v1.37.00 覆盖升级模拟校验 249 个文件，哈希一致，账号配置保留。
- 更新包：`dist/nas-1.37.01/okww_update_v1.37.01.zip`。
- SHA256：`10e57ef4e86bfa27e9e5488bac2764a5403eec2ce553f8464cc36acedfbd20c4`。

本次没有改变上传边界、日志留存、NAS 凭据或游戏任务逻辑。其他设备更新并启动本版本后自动迁移对应补传任务。
