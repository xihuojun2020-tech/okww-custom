# 2026-09-23 18:02 初露峥嵘停在开启挑战：诊断与修复

## 范围

- 证据包：`okww诊断证据_20260923_手动_180252_ceaadc99.zip`，48,081,857 字节，上传回执及实算 SHA256 均为 `8eb5f2a2e672e468342a3a4ec01cfb531d7cc717fac8e17e374059f4e70e8802`。
- 复核包内 17:12、18:00 两个会话的汇总日志，以及 18:02:15–18:02:21 的 19 张故障附近截图；使用仓库 OCR 对 F 可见和 F 暂时消失的原始帧进行离线复读。
- 本报告针对初露峥嵘整个可见流程核对状态转换，并给出本次确认的停滞根因。设备端完整实跑仍需验证。

## 时间线与根因

1. 18:01:36 启动初露峥嵘；前四名角色被识别为已完成，第五名进入试用。18:01:58 点击“前往试用”，18:02:07 导航确认到达介绍页。
2. 18:02:08、18:02:11 分别按 Esc，18:02:14 地图稳定并开始寻找“F 开启挑战”。日志没有“发送 F”记录；18:02:15.960 直接抛出 `RuntimeError: 开启挑战交互发生变化`，异常点是 `CharacterTrialTask._start()` 按 F 前的二次 OCR。
3. 故障截图中，右侧挑战交互的“开启挑战”文字一直存在，F 字母却在几帧短暂消失。离线 OCR 对可见帧同时读到 `F` 与 `开启挑战`，对相邻帧只读到 `开启挑战`。旧逻辑第一次要求 F+文字来结束前进，下一帧仍要求两者同时出现；动画中的 F 消失触发错误，故没有按 F。这不是点击延迟，也不是已经进入战斗。

## 流程复核与修复

- 活动入口、五人列表、已完成判定、第五人“前往试用”均由日志确认正常。
- “下一页 / 开始试用”识别后直接 Esc 的路径实际执行两次 Esc，随后确认地图稳定；本次没有点击“开始试用”。
- 地图前进阶段已捕捉到完整的右侧 F+“开启挑战”交互。修复只调整此后的二次确认：在地图状态仍成立、右侧唯一“开启挑战”文字仍可见时，允许 F 图标短暂消失，发送 F；文字或地图状态消失则在 3 秒内停止而不发送输入。仍须在 10 秒内确认进入战斗，失败最多再试一次。
- 战斗、完成提示、离开模拟领域、返回活动页、领奖和最终复核沿用原有有界状态检查；本次任务没有到达这些阶段，故不能声称设备端已通过。相关离线流程测试覆盖了上述任务路径的既有行为。

## 验证

- 从本包相邻故障帧生成脱敏回归素材，底部身份区域全部置黑；真实 OCR 分别验证完整提示与 F 消失但文字保留。
- 新流程测试验证“完整提示先出现，随后 F 消失但文字仍在”会发送一次 F；文字消失时不发送 F。
- `python -m unittest tests.TestCharacterTrial tests.TestCharacterTrialImages`：53 项通过；`git diff --check` 通过。这里的 Python 使用仓库本地 `.venv`。
- 仍需在另一设备运行第五人完整试用，确认 F 发送、进入战斗、完成/离开、领奖，并核对最终 5/5 记录。
