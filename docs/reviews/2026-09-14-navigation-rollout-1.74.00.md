# 1.74.00 稳定等待推广与使用端交接

日期：2026-09-14。基线为1.73.01 / c97f5940，包含并行会话的每日任务恢复修复。本轮在隔离工作树完成，未覆盖原工作树未提交文档，未操作游戏或消耗资源。

## 已实现范围

| 路径 | 行为与保护 |
| --- | --- |
| 深塔角色列表返回 | 角色列表→编辑队伍→楼层详情→三塔总览按已知页面顺序返回，允许跳过中间页；整条路径18秒、最多4次Esc。未知页不盲按，倒退方向立即停止 |
| 深塔开启挑战 | 同帧编辑队伍标题和开启挑战按钮证明来源，环境特性证明已进入加载阶段。只提交一次，不确认结果时不重放；已显示提示则零点击 |
| 深塔进入地图 | 环境特性仍存在时才允许有限重试Esc，同时要求提示消失且进入队伍/世界状态；最多120秒 |
| 角色列表标题 | 识别范围避开左侧图标，修复真实截图在720P缩放时被读为“三详情”的问题。进入、完成编队、返回三个调用点保持一致 |
| 材料培养目标 | 复用指南稳定页签导航；选中底色横向取样改为0.27～0.29，避开原0.31～0.335范围内的切换圆钮 |
| 材料背包/资源页 | 使用配置的Bag Key；确认武器/声骸/资源及容量标题后再切资源页。已经在资源页零输入，未知背包不点击资源坐标 |

复用现有navigate_ui的新帧稳定确认、任务/窗口上下文检查和诊断摘要。没有新增UI，保持原工具页风格。材料目标、库存扫描与消费授权逻辑没有扩大。

## 页面证据与测试

24个测试文件269项通过，输出在隔离树test_out/*-174-final.txt。

- 深塔既有脱敏完整夹具failed/overview/roster验证失败页拒绝、三塔总览、角色列表分类；缩放720P、1080P、1440P、2160P。真实图像测试发现标题图标污染并完成修正。
- 楼层详情与编辑队伍分类使用既有生产标题/按钮契约及组合标记单测，尚无本轮完整实机页面样本证明其在所有布局下可用；识别不到将停止，不发送猜测按键。
- 控制流覆盖首次按键丢失恢复、整条返回共享4次预算、未知页、返回方向异常、开启挑战无加载证据仅点击一次、关闭提示首次Esc丢失。
- 材料真实图像13_33_40、17_02_22等验证培养页签底色；17_29_37、17_29_48、17_30_02验证背包标题，未知页面负例拒绝。配置快捷键、资源页首击丢失、已达目标零输入均通过。
- 同时回归材料扫描/存储/滚动、深塔战斗与返回、公共导航、每日活跃度/备用体力策略、1.73.01每日恢复及梦魇入口。

自动测试和图片缩放只验证已有样本及控制流，没有完成本机/使用端原生1080P和2K游戏验收。未审阅新的诊断ZIP。

## 用户明确跳过及保留范围

用户原先跳过领域结算、乐园周常、声骸融合；本轮进一步确认跳过战令、邮件、模拟领域和聚落专用入口。此次没有迁移这些专用路径；已发布的公共指南页签与1.73.01恢复修复继续保留。

35文件矩阵沿用1.72.00报告，并累加1.73.00及本报告的覆盖。不能将“复用公共层”表述为整个模块全部完成。深塔选塔/选层/进入快速编队、凝素和刷声骸的序号目标身份、未注册任务，以及切号登录退出专用循环仍保留原实现；缺少可靠目标后置证据的步骤没有增加自动重试。任务总流程和实时战斗不套重试循环。后续若继续迁移这些细节，须先补明确页面终态及目标身份样本。

## 使用端AI操作

1. 唯一NAS目录：`\\192.168.3.173\羲火君 共享给我\AI诊断`。从AI交接读取本报告；关闭旧程序后更新stable到1.74.00，核对程序版本及latest.json的包哈希。
2. 先验收材料读取：自定义背包快捷键仍生效；进入资源页后才扫描；培养目标页选中后不重复点；未知页应停止并保留诊断。
3. 在正常授权的深塔任务中检查角色列表进入/完成，以及角色或体力不足时逐页返回；加载慢时开启挑战不重复提交，环境特性关闭后进入地图。
4. 分别在1920×1080和2560×1440运行，记录原生分辨率、步骤名、实际结果以及完整脱敏页面。出现未知页先保存证据，不扩大输入次数、不改消费授权。
5. 不运行用户跳过项目作本版验收。发生回归可退回1.73.01；活动消费若存在pending记录，先按1.72.00交接文档核验实际余额/库存，不能降级后直接重放。

更新ZIP不包含文档，本报告单独复制到NAS的AI交接目录。GitHub源码/标签和NAS包发布不代表安装器CI或使用端实机验收已经完成。
