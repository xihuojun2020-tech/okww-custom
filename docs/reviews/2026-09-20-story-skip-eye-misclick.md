# 本机打包版剧情跳过误点击修复

## 范围与证据

检查本机 `E:/game/okww owener/data/apps/okww-custom/working`，版本确认为1.82.03；只读日志，不操作游戏、不修改运行中的安装目录。未读取NAS诊断ZIP。

- 用户21:56:49截图：左上有真正跳过图标，右侧有日志、自动播放、眼睛控件及对话选项。
- 本机 `logs/ok-script.log`：21:57:24记录剧情跳过页面转换超时；21:57:50.921记录 `left_click skip_dialog_hex (2439, 245)`，归一化坐标 `(0.952734375, 0.1701388888888889)`。
- 用原版本直接回放用户原图，四种分辨率均返回右侧眼睛，复现一致。因此不是版本未更新，也不是只需点击对话选项。

## 原因与修复

旧识别在左右顶部搜索，六边形外框使眼睛模板整体相似度超过阈值；同时绿色背景下真正按钮未达到阈值。此前两张图的负例通过不能推断所有背景可靠。

1. 搜索限定左上15%宽、22%高区域，保留已知两种位置，排除右侧功能区。
2. 新增真实绿色背景模板，原整体阈值0.85不变。
3. 额外验证图标中央快进符号，阈值0.75；即使把右侧眼睛放到左侧，也不能仅凭外框通过。
4. 保留单次入口提交及确认流程，未用无限点击或降低全局阈值掩盖误识别。

## 验证与限制

修复后原图返回左侧约 `(105,245)`。新增三场景×四种分辨率的完整 `AutoDialogTask` 识别入口回放，核对非大世界、无错误确认页、最终按钮坐标；新负例包括右侧控件移位。此前勾选与梗概流程继续回归。

真实日志与离线回放确认了误点击原因及定位修复，但未操控游戏验证完整跳过过程，也未将运行中的打包版文件直接热替换。用户需更新并重启后重新启用已因超时停止的剧情跳过任务。
