# 若梦仍有回声：最后一位声骸未装配修复

版本：1.75.01。日期：2026-09-15。

## 检查范围与证据

检查本机打包版 `E:/game/okww owener/data/apps/okww-custom/working/logs/ok-script.log` 和本机诊断目录的 `10-56-22.857_echoes_remain_failed_original.png`。本次未审阅 NAS 诊断压缩包，不执行归档已检查标记。

10:56:06、10:56:11 前两个角色装配返回确认成功。10:56:13 第三个角色千咲打开支援声骸页面；10:56:22 在装配点击前抛出“声骸选中或类型未确认，未点击装配”。截图显示第二个控制型声骸已有金色选中框，装配按钮可用。

原始截图离线复现：页面识别为真、选中框识别为真、选择索引为 1、装配按钮识别为真；类型 OCR 输出 `控製型`（置信度约 0.862），精确比较 `控制型` 为假。不是点击丢失，也不是第三个卡片的坐标错误。

## 修改

仅在活动声骸类别匹配中将完整字符串 `控製型` 映射为 `控制型`，继续使用既有空白和 Unicode 归一化。未扩大普通按钮和页面的匹配范围，不接受局部词语、错误类别或重复命中。

选中复核与装配导航共用同一类别规则。页面、类别、选中框仍须同时通过；装配后仍回读角色姓名与对应槽位图像。锁定排除与输出/治疗/辅助优先级不变。

复核超时日志增加角色槽位、声骸索引、预期类别、实际 OCR 文本、页面/类别/选中框布尔结果，每次失败记录一次。

## 验证

- `TestEchoesContinuation.py`：15 项通过，覆盖原有装配、战斗和推进逻辑及类别别名、错误类别、缺失和重复结果拒绝。
- `TestEchoesContinuationImages.py`：6 项通过，新增实际失败截图复现；控制型选中通过，错误类别及其他未选中卡片拒绝，装配按钮可识别。
- 新截图夹具去除顶部硬件监控与底部身份覆盖区域。
- 图像测试退出时仍有框架 GC ResourceWarning，测试结果通过。

未操作游戏进行实机验收。用户更新后需确认千咲装配成功返回编队，并继续开启挑战。

## 发布

代码提交 `51cd6ddc` 和 GitHub 注释标签 `v1.75.01` 已推送。NAS 更新根目录 `\\192.168.3.173\羲火君 共享给我\AI诊断\OKWW-Updates` 的 `stable/latest.json` 已读回确认版本 1.75.01。

更新包位于 `stable/releases/v1.75.01/okww_update_v1.75.01.zip`，大小 38,191,917 字节；SHA256 为 `1f23bb369093e5f091e8d9a7ef118cb66a5f8e998d34dffeabb4e8d60095de97`。相对 v1.75.00 的 404 个文件验证通过，账号配置保留校验通过。GitHub 安装器 CI 完成状态不在本次确认范围。
