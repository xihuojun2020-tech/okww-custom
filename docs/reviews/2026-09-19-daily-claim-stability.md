# 2026-09-19 多账号每日 NAS 故障调查与修复

## 范围和证据

- 检查包：`okww诊断证据_20260919_手动_122157_8b9536ee.zip`，534562478 字节。
- SHA256：`7d012c5e2bf7631c68f917f4b9c058bef30132e69756329303bfe5471a81ad57`，本地副本与 NAS 附带校验一致。
- 唯一 NAS：`\\192.168.3.173\羲火君 共享给我\AI诊断`。
- 重点检查会话 `20260919-111017-6e096ed25460` 的日志、异常前后截图及元数据，定位 A3 每日失败；不宣称归档中所有历史战斗问题均已解决。

## 结论与证据链

实际故障设备运行 **1.79.09**，日志有从 1.79.07 更新到 1.79.09 的记录，不是 1.80.00 海墟运行证据。主日志第 8630 行及 8686—8689 行可核对版本。

A3 在 11:38:40、11:50:22、12:00:28、12:01:36 的运行或补跑出现同一异常：`DailyActivityIncomplete: 领取任务积分时离开每日页面`。调用路径为 `_run_daily_inner → open_daily → _claim_daily_objectives`，已发布代码第 2286 行。

日志第 14449—14484 行显示导航已确认每日页，积分为 20，随后领取扫描的单次页面判断失败；14490 行之后记录补跑失败，14509—14510 行汇总部分失败。15133—15172 行是再次发生的同类路径。

领取扫描每轮仅取一次画面，只要 `_daily_page_ready` 返回假就立即终止整个账号每日流程。滚动或页面过渡期间的短暂失识别没有经过已有稳定性门控。这是可复现的脆弱点；重试账号也会再次走到同一判断。

12:01:35.664 的相邻截图及异常后截图仍在每日页，积分 20，没有奖励遮罩。真实 OCR 回放均能识别为每日页。采样约一秒一次，**没有采到触发判断的精确子帧，不能证明具体是哪一处 OCR、页签或底栏锚点瞬时缺失**。未将相邻正常截图冒充识别失败样本。归档中的 BitBlt、共鸣解放确认和非编队战斗事件未证实为本异常根因。

## 改动及多账号影响

1. `_claim_daily_objectives` 复用 `_restore_daily_claim_page`：最多 16 轮、每轮等待 0.25 秒、要求连续三次确认每日页；已识别奖励遮罩最多关闭三次。持续未知页面仍停止，不盲点、不虚报完成，取消异常继续向上传递。
2. 没有新增账号选择、登录、别名匹配、重试或切号算法。多账号受益于它调用的共同 DailyTask 领取路径。
3. 独立发现 1.80.00 开发提交以 1.79.00 为基础，遗漏并行发布的 1.79.01—1.79.09。修复分支合并 v1.79.09，保留已发布每日、奖励遮罩、复苏、若梦和多账号失败汇总修复，同时保留海墟。此项是发布回归风险，不是本包直接故障的版本证据。
4. 版本与更新日志同步为 **1.80.01**。在独立 worktree 实施，未覆盖原工作区已有未提交修改。

## 验证

- 新增短暂失识别恢复、持续离页有界退出、取消传播三项测试。修改前两项失败，修改后三项通过。
- 归档截图脱敏裁去上下边栏并缩放为测试素材，覆盖异常前、异常后每日页，以及大世界负例；构建脚本要求精确归档 SHA256。
- 使用项目本地 .venv 和 `scripts/run_test_file.py`，加载与生产相同的 custom_ok 覆盖，分进程运行 **15 组、235 项测试，全部通过**。
- 组别：DailyClaimStability / Images、DailyRegressionFlow / Images、DailyActivityFlow、DailyOutcomeRecovery、DailyFailureRecovery、DailyReservePolicy、MultiAccountDailyTask、AccountSwitch、AccountSwitchEvidence、SeaRuins / Flow / Images、TestTestGroups。
- 先前直接将多个图像测试类置于同一 unittest 进程出现全局测试环境关闭异常；改用正式独立进程 runner 后通过。第一次重跑命令因输出目录尚未创建未启动测试，创建目录后完整重跑。
- 新增测试以及海墟测试已加入 run_tests.ps1 分组。diff 空白检查通过，源代码更新包已生成；相对 v1.79.09 覆盖验证通过（422 个文件，目标配置保留）。包 SHA256：`ed69a1aa2b1f3a680c3aca5197d8382d9093484a54b6a24e23739bb1e6e8d8b8`。

## 验收限制与发布状态

未运行完整真实游戏多账号任务，未进行真实 A1→A3→A4 连续切号；不能将模拟及截图测试当作实机通过。建议安装修复后重点观察 A3 的每日领取与多账号失败汇总，再执行上述切号顺序。

GitHub 远程标签查询遇到连接重置；在远端标签确认失败时不猜测创建版本标签。此次修复与本地包不代表 GitHub 已发布，也未自动安装到故障设备。NAS 归档仅对本报告对应包执行已检查登记，其他包不做清理；报告长期保留，已检查证据按项目规则三天后可过期。
