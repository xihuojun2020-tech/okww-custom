# 深塔错误复查：仅 9 月 11、12 日

本轮只从 `\\192.168.3.173\羲火君 共享给我\AI诊断` 的 2026-09-11、2026-09-12 日志目录读取资料，包括普通日志、事件/元数据 JSON、压缩包和未完成批次。扫描 9,734 个目录或顶层文件条目，检索 28,913 份文本内容，得到 145 条深塔相关命中。数字包含重复上传和元数据，不代表独立故障数量。

## 新证据及结论

新增手动日志 `待分析\日志\okww-custom\2026-09-12\2026-09-12_2135_ok-script.log` 中，20:42:13 开始深塔，20:43:08 失败。最近启动记录为 v1.60.11。11 日所读资料未发现深塔执行错误。

关键行：

- 48494：深塔开始。
- 48606：第 1 屏扫描完成，14 名角色。
- 48618：第 2 屏首次识别完成，14 名角色、未知头像 0 个。
- 48619～48630：第 2 屏重新读取。
- 48631～48649：`重复角色体力读数矛盾，停止共享体力规划`，堆栈指向 `_scan_character_pages` 第 1355 行。

这是角色体力扫描阶段的跨屏记录冲突；尚未进入本次编队及战斗。上一份报告不能用于解释这次故障，本报告作为当前事件的结论。

## 截图与离线回放

同日截图目录中的 `0912_204305_20-43-05.409_abyss_card_numbers_unknown_p1_r2_c2_frame_original.png` 和 `0912_204308_20-43-08.442_abyss_energy_conflict_original.png` 已查看并使用当前真实 OCR/头像识别方法回放。

- 第一屏第 1 行第 5 列，画面体力 0，回放记录 `char_moning` 为 4，确认读取器存在误读。
- 弗洛洛在第一屏为未知，第二屏识别为 7；未知本身不会进入已知体力冲突比较，因此不能仅凭它的告警认定为根因。
- 重叠角色在这两张保存图的回放中没有复现相互矛盾的已知值。第一屏保存图是数字未知时的证据帧，不保证与后续重读并写入账本的帧一致；因此尚不能确定运行时触发冲突的具体角色和两次读数。
- 日志多次出现 `原始='10'，数字轮廓=1，结果=0`。相应卡片确有体力 0，OCR 可能把闪电读成前导 1；不能据此取消全部数字修正。

本地回放脚本、原图副本及结果位于 `test_out/nas-abyss-recheck-1112/`；回放屏蔽截图保存并不进行游戏操作。原始日志节选保留源文件行号。

## 代码机制与修复建议

`_scan_character_pages` 发现相同角色在两屏有不同的已知体力后，只重读当前屏；仍与上一屏不同就停止。这道保护避免错误共享体力账本，应保留。问题在于上一屏的数值未必正确，而重读只要求当前屏重新匹配旧值；当前日志也没有记录冲突角色、旧值、新值及重读值。

建议按以下顺序修复：

1. 冲突诊断增加角色 ID/名称、原页与新页、卡片坐标、旧值/新值/重读值、OCR 原文、数字轮廓数；保存两侧实际参与比较的卡片及完整帧。
2. 体力读取采用卡片局部数字区域，明确区分闪电与数字；兼顾金黄、橙色的 0/低体力。一个卡片多个 OCR 候选冲突时不得简单按遍历顺序覆盖；轮廓数与 OCR 意义冲突返回未知并重读。
3. 使用同一角色的两侧新帧做有界复核，而非将上一屏第一次读数当作必然正确。多帧一致后更新记录；不能取最大值、直接取最小值或跳过冲突。
4. 全部帧仍不一致时保留停止保护，提示具体角色，并允许重新开始扫描；不得继续消耗角色体力。
5. 回归覆盖真实 0、2、7、8、10、闪电干扰、OCR 候选冲突、未知变已知、旧页误读/新页正确及两侧持续冲突。用本次 1920×1080 图片作回放，实机由用户验收。

调查阶段未修改代码。后续已按此方案实施，见 `2026-09-12-abyss-energy-1.60.13.md`。
