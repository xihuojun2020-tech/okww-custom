# 2026-09-16 多账号每日任务：1.78.00 回归诊断

## 范围与证据

本轮只诊断和提出修复方案，没有修改生产代码、回滚或发布版本。

实质审阅 NAS `待分析/压缩包/okww诊断证据_20260916_手动_145947_a11baefa.zip`：770835852 字节，SHA256 `68e72ac6c9cdf3c3dd17728f998ba4a7f64c806ef0716964726eed2b71b9f989`，流式计算并与上传收据一致。NAS 根目录仅使用 `\\192.168.3.173\羲火君 共享给我\AI诊断`。

该包含 3041 个条目，聚合日志涉及 1.77.05、1.77.06、1.78.00 三个会话。前两个会话未检出 MultiAccountDailyTask/ERROR 文本；本轮多账号故障证据来自 14:22 启动的 1.78.00 会话。包内日志存在重复转发，不能用 ERROR 行数计失败次数。今天 11:16 的小包及今天上传的昨日自动包未实质审阅，不登记为已检查；本报告不是对所有历史包的穷尽审计。

## 结论

不是单纯设备不兼容，也不是没有升级。1.78.00 提交 `ebd779ea77496970b69f1450fc5ef6eb597192ca` 引入两处可直接复现的功能回退：删掉领取任务活跃度的步骤，以及新增错误的敌迹页面标题条件。另有旧的零分 OCR 问题被放大。此前测试没有覆盖这些真实前置状态，不能以测试通过数量代替实机验证。

### A4：任务目标已完成，活跃度未领取，错误判为失败

- 14:42:01 开始每日；14:45:54 聚落结束。
- 14:47:22、14:48:31、14:49:30 领取副本奖励后当前波片依次为 160、80、0，备用波片保持 391。
- 14:50:02 已识别消耗波片任务 180/180。
- 14:50:03、14:50:09 领取流程记录领取档位为空、没有红点；14:50:16 抛出 DailyActivityDetectionError。
- 事件 065016 的第 8 帧显示底部活跃度为 0；登录、消耗 180 波片、通关聚落、获得声骸等目标已经完成，右侧仍有黄色“领取”。前四项目合计 120 分尚未入账；下方另有变奏技能项目。

旧版（包括 1.77.06）的 claim_daily 有顶部任务行领取动作；1.78.00 将逻辑替换为只查宝箱红点，漏掉了前置步骤。正确顺序是“完成目标 → 领取目标积分 → 解锁宝箱 → 领取宝箱”。0 分时没有宝箱红点完全正常。反复补跑战斗无法修复遗漏的领取动作。

### A3：实际出现复苏弹窗，恢复流程却拒绝正确页面

- 14:26:23 开始每日；聚落流程结束后进入材料副本。
- 14:40:00 切换角色，随后队伍识别消失；14:40:01 进入角色死亡恢复路径。
- 事件 064001 第 9、11 帧明确显示“选择复苏物品”，因此不能断言只是误判死亡，也不能据此说全队死亡。
- 退出副本后恢复路径调用 openF2Book(gray_book_all_monsters)。14:40:43 因页面核验超时导致 A3 失败。
- 事件 064043 第 7 帧显示已经打开正确页面，标题为“敌迹探寻”，页面有敌人列表及“探测”。新增 _guidebook_content 却要求“残象探寻”。实际 OCR 返回“敌迹探寻”，导航标签也匹配，但内容判断返回 False。

调用链：DailyTask → ForgeryTask → DomainTask 战斗 → 切人/复苏 → 退出副本 → revive_at_tower_and_heal → 打开敌迹页 → 错误标题校验 → 超时。延长等待时间不能修复恒不成立的标题条件。

### 活跃度 0 的 OCR 漏读

同一张 A4 截图，现有 `.19,.8,.30,.93` 区域 OCR 只返回“活跃度”，生产函数返回 None；较窄数字区域 `.188,.84,.285,.895` 返回 0（置信度约 0.84）。数字区需用真实多分辨率样本验证，不能直接把所有 None 改成 0。

宽区域是旧实现；`5b3e7b85`（1.31.01）引入读取失败后保留未知的严格处理，并非 1.78.00 新增。1.78.00 不再领取任务积分，让页面持续停在 0 分，将旧缺陷放大。未知状态保护本身应保留。

### A1 及其他告警的边界

A3 失败后能够切换到 A4，A4 失败后 14:51:50 能够进入 A1；这批证据不支持把账号登录作为首要根因。包截止约 14:59:49，A1 尚在战斗，没有最终结果，也没有这轮全部账号补跑后的最终记录。

角色解放 animation_unconfirmed 告警不等于账号终止；存在可见窗口中的告警及其他成功施放记录，不能全部归因于后台输入。角色死亡的更早战斗成因尚未确证，不应借此大改所有角色轮转。

## 回归定位与测试缺口

| 修改 | 性质 | 与今天证据的关系 |
| --- | --- | --- |
| ebd779ea / 1.78.00 删除任务行领取 | 新回归 | A4 已完成目标但积分未入账 |
| ebd779ea / 1.78.00 新增错误敌迹标题 | 新回归 | A3 正确页面被拒绝，恢复链终止 |
| 旧数字 OCR 区域 + 5b3e7b85 未知保护 | 旧缺陷暴露 | 真实 0 分被读成 None |

昨日已审报告曾有 1.76.02 完成多个账号的记录，说明近期存在成功路径，但不证明整个旧版无缺陷。1.77.06 不含以上两处新回归，也仍有此前已发现的问题，因此优先局部修复而非整体倒退版本。

本轮用实际 DailyTask、生产 OCR 和两张真实截图离线重放：正确敌迹页被拒绝；零分漏读；目标领取按钮可读但宝箱列表为空。重放脚本 `test_out/TestSep16Replay.py` 两个诊断用例通过，表示复现了旧行为，不表示修复通过。此前测试过度模拟 claim_daily 和页面判断，漏掉“0 分 + 多个任务待领取”和真实敌迹标题的组合。

## 修复与验收方案

详见 `docs/superpowers/plans/2026-09-16-daily-regression-repair.md`。优先恢复领取前置步骤、修正页面正向识别和数字读取；保留现有多账号身份核验、断点与备用波片策略。不通过增加整账号重试、放宽所有识别阈值或把未知视作成功来掩盖问题。

验收必须含真实截图回归、流程测试及 A1/A3/A4 实机验证，区分新一天未做、目标已做未领奖、全部已完成三种状态。未经实机验证不得宣称多账号问题已经解决。本轮没有运行游戏、修改生产代码或发布修复版。
