# 2026-09-07 诊断整改执行记录

## 范围与版本

用户授权按《2026-09-07 diagnostic remediation plan》实施。代码基线为 `v1.33.03`（`f9ca2684`），本次中等变更版本为 `1.34.00`。保留此前单一 StartTab 热键所有者和失效 HWND 处理；不改写历史标签，不编辑真实账号配置。

输入为《今日错误诊断汇总_20260907.zip》，SHA256：`b7b8ef3280e1b2ac4dc1c73a1507072112f64b216f818d2010c2ccb8a7c726bf`。附件源码只用于对照，未执行；其中四份源码对应 v1.33.01。原始材料与测试日志留在本机忽略目录 `test_out/diagnosis_20260907/`，不纳入发布仓库。

## 已实施

| 项 | 结果 | 关键验证 |
| --- | --- | --- |
| T1 星期兼容 | 共用标准化函数；中文显示、英文保存；非法值拒绝，“无”仍为周日补检 | metadata、账号表单、每日周常回归 |
| T2 独立每日确认 | 运行前明确所选账号与参数；默认取消；确认后再次核对 revision 与选中项 | 实际 Qt 事件循环测试接受、取消、超时、停止、窗口销毁；生产 Daily.run 的游戏边界与完成写入保护 |
| T3 WGC 重建 | 空池允许执行器进入恢复；5 秒节流；旧回调隔离；清除旧帧 | TaskExecutor.next_frame 入口、过期 FrameArrived/Closed、失败冷却 |
| T4 启动分类 | world/login/unknown 分离；新帧确认；至多一次重分派保留快照和原回登账号 | 异常/停止/无帧、真实合成仓库快照、登录上下文释放；分类等待不用可能处理月卡的通用 sleep |
| T5 热键 | 仅成功注册才显示键；失败提示错误码并节流重试；切键、禁用和销毁释放 | 替身 Win32 API 覆盖失败恢复、F9→F10、None、销毁 |
| T6 技能日志 | 新增尝试数、时长、帧龄、窗口状态，输入送达仍为 unverified | 日志路径无新增输入；未修改 Hiyuki 或战斗时序 |

新增 `DailyRunConfirmation.py` 由 MainWindow 在 GUI 线程创建，工作线程等待信号；新 `StartCard.py` 随 custom_ok 覆盖同步。5 种语言的 PO/MO 同步，新增文案占位符及重复条目校验通过。生产与测试切号路径继续共享，A1→A3→A4 约束不变。

## 验证记录

使用仓库 `.venv/Scripts/python.exe`，未启动游戏或进行真实切号。专项通过：多账号 94 项、运行服务 8 项、战斗基类 6 项、账号管理 UI 19 项、周常相关 19 项，以及新增确认/分类/WGC/热键用例。

首轮全量暴露旧周常参数测试缺少新账号确认前置条件；已补齐替身，保留其“参数无效时不得进入游戏初始化”的断言。随后全量 85 个文件、779 项、8 项跳过、0 失败。最终审查修正分类等待调用通用 sleep 的问题后再次运行全量，最终结果见下方发布验收。

版本元数据校验及 pip check 通过。源码更新包从 v1.33.03 在临时目录覆盖验证，239 个文件逐一核对 SHA256，合成 configs 前后相同；新增确认桥接、StartCard 与翻译均进入受控包。该验证不代表 EXE 安装或实机已通过。

## 明确待验事项

1. ZIP 不能证明实际 A1/B13 错配：游戏身份和有效配置图缺少完整证据。独立每日确认属于用户声明，不宣称自动识别成功。
2. WGC 的真实显卡、DPI、遮挡、窗口重建兼容矩阵尚未完成；离线替身不覆盖实际 COM/驱动行为。
3. 三次共鸣解放异常没有对应截图/短视频，尚不能区分输入未送达与动画识别失败。不增加重试/延时，不承诺角色轮转根因已修复。
4. Defender 0x800106ba 保留环境诊断；未修改服务或防护设置，原日志中应用更新/启动已成功。
5. 真实 A1→A3→A4 循环、默认确认界面的实机可用性及 EXE/MSI 安装仍需受控验收。

## 发布验收

最终全量通过：85 个测试文件，779 项（8 项跳过），0 失败/错误，退出码 0。结果目录 `test_out/test_runs/20260907-160239-025/`，汇总日志 `test_out/diagnosis_20260907/full-regression-release.log`。最终更新包 SHA256 为 `d1de771fd1fa3a0bf9b432280cfb5b07c406bf7d3749e888b4d781b9ab379820`。

此记录随版本提交发布；推送与 CI 状态以远端为准：[版本标签](https://github.com/xihuojun2020-tech/okww-custom/releases/tag/v1.34.00)。正式标签为 `v1.34.00`，发布分支 `codex/account-switch-foreground-bitblt`。GitHub Actions 由标签触发，推送成功不等于安装器构建成功。
