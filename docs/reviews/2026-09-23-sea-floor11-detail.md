# 海墟第11层详情识别修复（1.84.01）

## 范围

检查2026-09-22本机安装版1.83.05的海墟失败日志、转场截图和当前生产代码，修复第10层进入第11层后的目标页面误判。本次没有审阅NAS诊断ZIP，没有修改战斗、出口、配队、信物规则、多账号或每日任务。

## 结论与证据

- 第10层正常完成，上半2220、下半2220、总分4440；结算读取成功。
- 16:17:22程序正确点击「继续挑战」，约1秒后截图已经显示第11层「海墟详情」和右下角「开启挑战」。
- 导航日志随后持续为`source=False, target=False`，16:19:21耗尽120秒并暂停；不是点击失效、加载卡死、解锁弹窗或战斗失败。
- 旧代码要求大号层数OCR直接返回唯一完整的`11`。真实截图回放表明该区域在不同缩放下可能为空或拆成两个`1`，导致正确页面被拒绝。

## 修复

`AutoSeaRuinsTask._detail_floor`合并合法数字OCR；OCR仍为空时，第11层使用真实画面提取的数字模板。模板兜底只在「海墟详情」和右下角「开启挑战/开始挑战/再次挑战」同时成立时启用。`_continue`的目标判定也要求预期详情层号与挑战按钮同时成立，不依赖编队或信物是否已重新选择。

故障截图在纳入测试前遮盖顶部性能信息和右下角特征码；独立数字模板随程序资源发布。

## 验证

海墟规则、流程、真实截图、暂停恢复以及发布元数据共88项通过。第11层真实截图在720p、1080p、1440p均返回层号11；既有第7、8层和地图/信物负例继续通过。版本校验通过，源码为1.84.01。

提交`5b5b6441`，注解标签`v1.84.01`和分支`codex/sea-floor11-detail`已推送GitHub，远端标签解引用到同一提交。

## 验证限制

没有自动启动游戏或运行完整海墟。GitHub标签存在不代表安装器CI、NAS更新或目标机器安装成功。用户更新后仍需实测第10层结算点击「继续挑战」能否立即进入第11层预设与信物扫描；在此之前不宣称7—11层整轮已经验收。
