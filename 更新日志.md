# 更新日志（CHANGELOG）

版本号规则：固定宽度 X.YY.ZZ（如 1.03.74）。小改动改第三位，中等改动改第二位且第三位归 00，大改动改第一位且后两位归 00；大版本升级仅在用户亲自提出时执行。

## 1.24.00（自动深渊第二阶段：角色列表自动扫描）

- 自动深渊完成三座塔关卡扫描后，会重新进入残响之塔并选择第一个未完成且已解锁的关卡；只在关卡详情页精确点击“挑战开始”，进入编辑队伍后只点击“快速编队”，明确不点击相邻的“开启挑战”。
- 进入角色列表后自动等待画面稳定、截取第一屏、向下滚动并验证头像网格或滚动条确实变化，再截取第二屏；滚动失败时停止，避免重复识别同一屏。
- 复用现有角色头像库进行多尺度 ORB 特征匹配，按标准 7×3 网格识别前两行完整卡片；体力和等级采用整行 OCR 加局部补识别，修正闪电图标被读成前导 1 的情况，只展示体力大于 0 且等级大于 60 的去重角色。
- 角色扫描结果按当前账号只保存在本次程序内存中，提供“清空识别结果”按钮，关闭程序后自动清除。本阶段不会选择角色、不会点击完成、不会进入战斗或调用自动战斗。
- 所有验证均使用离线截图与自动化测试，没有启动或操控游戏。

## 1.23.03（逆境深塔独立选择与前往定位）

- 周期挑战入口确认后，自动深渊现在先识别并点击左侧“逆境深塔 / 深境区”卡片，明确排除冥歌海墟与终焉矩阵。
- 只有中间内容区出现“深境区”后，才在该卡片右侧匹配“前往”；左侧活动卡片的“深境区”副标题不再参与前往按钮配对。
- 调整同卡片前往的相对纵向容差，覆盖标题与右侧按钮不处于严格同行的正常布局；仍不会进入编队或战斗。

## 1.23.02（周期挑战入口全栏双状态识别）

- 修复自动深渊在 F2 左侧导航栏将高亮的“敌迹探寻”误判为“周期挑战”：原实现使用包含背景颜色的单张金色模板，未选中的灰色周期挑战图标分数较低，导致错误图标获胜。
- 新增透明背景的灰色未选中、金色选中图标模板；在整个左侧导航栏内使用图标边缘匹配，图标可随游戏内容上下移动，不依赖固定纵向坐标。
- 点击后仍强制 OCR 确认“周期挑战”；未确认即保存失败截图并停止，不会继续进入深境区、编队或战斗。

## 1.23.01（自动深渊第一阶段：深塔关卡扫描）

- 在“测试功能”新增“自动深渊：深塔关卡扫描”。任务从大世界打开 F2，识别周期挑战入口，在周期挑战页精确选择“深境区”卡片同一行的“前往”，避免误点超载区或实验区。
- 进入逆境深塔后依次通过“残响之塔、深境之塔、回音之塔”文字锚点定位柱子，扫描每座塔的四个关卡并展示“已完成、未完成（可挑战）、未解锁”。完成与锁定图标只在各关卡局部区域匹配，避免整屏误识别。
- 本期严格止步于扫描：不会进入编队、不会选择角色、不会点击“挑战开始”，也不会调用自动战斗；不操作游戏的自动化测试覆盖卡片“前往”匹配、状态聚合、塔点击锚点、任务导航和发布清单。

## 1.23.00（PC 正式版剥离 Android 实验）

- 将已确认无法投入使用的 Android/MuMu 实验完整归档到仓库外 `E:\AI work\废弃代码\okww-android-experiment-20260901.zip`，归档包含当时尚未提交的 `nemu.py`、`preflight.py`、`agent-app` 和设计文档。
- 从正式程序移除 `src/android`、Android Agent、Nemu IPC 兼容层、预检脚本、Android 资源、专用测试和设计文档；删除关闭状态的 `android_config`，更新包不再携带 MuMu 探测程序。
- 暂时保留 PC 任务中的三个 `_android_boundary` 空兼容钩子，避免本次范围扩展；它们在没有 Android 协调器时不会改变任务行为，后续可独立清理。
- 不修改 WGC、Foreground BitBlt、SendInput、点击连接、账号切换、每日任务或状态悬浮窗。

## 1.22.08（账号联动 GUI 线程崩溃修复）

- 根据两份崩溃转储确认直接原因：任务工作线程在账号方案联动后直接刷新 Qt 任务卡，进入 `QLabel → QTextDocument::setPlainText → QTextFrame::begin` 时发生空指针访问；两次转储调用栈一致。
- 移除多账号运行路径中不必要的直接 GUI 刷新；每日任务 GUI 刷新入口统一检查 Qt 主线程，非主线程调用安全跳过，不再触碰任务卡或 `QLabel`。
- 设置页等原本位于 GUI 主线程的刷新保持不变；不修改 WGC、Foreground BitBlt、SendInput 或状态悬浮窗。
- 新增工作线程不得更新 Qt 控件、GUI 主线程仍可正常刷新任务卡的回归测试；所有自动验证均不启动或操作游戏。

## 1.22.07（游戏登录页分类冲突修复）

- 修复 `1.22.06` 将真实游戏登录页误判为启动器：`KURO GAMES`、公告、修复和 `CN_Windows Product` 同样存在于游戏登录资源界面，不再作为启动器专属证据。
- 抽取并复用统一的登录就绪判断；账号身份与精确“登录/登入/Log”按钮同时出现时，优先确认为可操作的游戏登录页，退登状态机与稳定等待不再对同一画面产生冲突结论。
- 保留 Unicode NFKC 全角账号修复；全角 U 账号仍能参与登录页确认、账号列表计数和本地方案映射。
- 真正启动器候选必须没有账号身份、明确出现“开始游戏/启动游戏”，并且前台进程不同于游戏进程；证据不足时只安全等待，不自动点击。
- 新增压缩包失败画面、登录优先级、启动器候选和跨进程验证回归测试；所有自动验证均不启动或操作游戏。

## 1.22.06（全角账号与启动器状态修复）

- 账号身份公共归一化增加 Unicode NFKC，OCR 输出的全角 `Ｕ`、字母和数字会先转换为 ASCII，再进入备用登录名、账号条目计数和账号选择匹配。
- 登录界面等待改为优先判断启动器强特征；`KURO` 与公告、修复、`CN_Windows Product` 或开始游戏文字同时出现时，即使页面还包含“登录”或 U 账号也会判定为启动器。
- 检测到启动器后明确停止并提示重新进入游戏，不再继续等待账号下拉框；本版本不增加启动器自动点击，也不改变现有前台 BitBlt 与 SendInput 安全边界。
- 新增全角身份映射、账号列表计数、启动器混合文字和判断优先级回归测试；所有自动验证均不启动或操作游戏。

## 1.22.05（退登局部识别加速）

- 退登状态判断改为在同一张整显示器 `Foreground BitBlt` 帧上优先检查右侧“返回登录”区域、确认框模板、`esc_setting` 设置页模板及世界状态；只有这些本地视觉状态均未命中时，才执行组合登录窗口整屏 OCR。
- “返回登录/Return to Login”从“整屏 OCR 后按边界过滤”改为真正只 OCR 屏幕 `x=50%–80%、y=52%–72%` 的右侧按钮区域，继续按识别文字框点击，避免左侧“退出游戏”。
- 设置页查找 `logout_power_icon` 失败时不再扫描整屏“退出登录”文字，而是在同一帧复核 `esc_setting` 后使用既有左下角归一化位置后备。
- ESC 和电源按钮投递继续各保留 1 秒界面稳定时间，但移除其后重复的第二个 1 秒等待；确认框重试、45 秒状态续期、三次输入预算、前台校验和 `SendInput` 安全边界保持不变。
- 新增局部 OCR 参数、模板优先级、登录 OCR 延后及重复等待移除回归测试；所有自动验证均不启动或操作游戏。

## 1.22.04（整屏 BitBlt OCR 通道修复）

- 整显示器 BitBlt 捕获在统一出口把 Windows BGRA 位图转换为 OCR 所需的三通道 BGR，修复账号切换在打开下拉列表前因四通道图像广播失败而停止。
- 保持整屏坐标、前台校验和 SendInput 路径不变；新增通道顺序、连续内存及独立缓冲区回归测试，自动测试不操作游戏。

## 1.22.03（账号切换整显示器捕获与连接入口修复）

- 完整账号切换会话从退登持续到目标账号进入主界面；期间强制鸣潮进程前台，并用 `Foreground BitBlt` 捕获鸣潮所在整块显示器的最终桌面画面，不修改全局截图设置。
- 组合登录界面的 `#32770`、`ComboBox` 和挂在系统桌面树下的 `ComboLBox` 统一从整屏图像识别；OCR 坐标统一叠加显示器原点，兼容副屏负坐标。
- 活动账号切换会话捕获失败时安全等待或停止本次识别，不回退 `WGC`、不单独截图登录控件，也不会在未知画面发送输入。
- 登录入口只精确识别“点击连接”，不会把“登录状态：0”当作按钮；每次输入后重新截图，只有观察到账号登录特征才确认进入成功，最多按既有预算重试三次。
- 主窗口、独立登录窗口和账号列表点击继续统一使用前台、PID、窗口矩形及命中关系校验后的 `SendInput`，不回退 `PostMessage`。
- 任务状态、恢复日志和账号诊断只显示 A3/A4 等短名，不显示配置 UUID、手机号或备用身份；自动测试不启动或操作游戏。

## 1.22.02（慢速退登续期与电源图标识别）

- 退登状态机每次明确进入新的主界面、设置页或确认框状态后，重新获得 45 秒观察时间，避免前台捕获和 OCR 较慢时在已经有效推进后仍被旧的整段总截止时间中断。
- 每个可见状态仍最多投递 3 次输入；未知状态不点击、不发键，状态续期不会放宽重复输入预算。
- 设置页优先从当前捕获帧识别脱敏的 `logout_power_icon` 特征并点击实际框中心；未命中时才沿用“同帧确认 `esc_setting` + 归一化坐标”后备。
- 主登录窗口中的账号下拉列表展开后，允许 `WindowFromPoint` 命中同游戏进程的 `ComboLBox` 弹出窗口；其他同进程但无关的窗口仍拒绝投递，继续保留前台、PID、目标矩形和 SendInput 完整性校验。
- 多账号任务绑定内部每日方案后，状态窗继续使用 A3/A4 等账号短名，不再被内部方案名称覆盖；任务结束或失败时清除临时状态标签。
- 新增状态耗时和模板优先级回归测试；自动验证不启动或操作游戏。

## 1.22.01（当前 PC 退登入口修复）

- 保留旧界面的“退出登录/登出/Log Out”OCR 路径；当前设置页没有文字入口时，只在同一捕获帧再次确认 `esc_setting` 后，按窗口尺寸点击左下角电源图标。
- 退出确认框只 OCR 定位并点击右侧“返回登录/Return to Login”，不再使用可能同时匹配“退出游戏”的通用确认按钮。
- 电源图标与确认按钮均使用捕获帧自己的窗口原点换算屏幕坐标，并继续经过前台 HWND、PID 与命中窗口校验后的 SendInput 边界。
- 新增伪帧测试覆盖设置页同帧验证、非零窗口原点、拒绝未知页面固定坐标以及“返回登录”目标优先；自动回归不启动或操作游戏。

## 1.22.00（退登可靠截图与任务状态窗）

- 多账号退登开始后使用任务级 ForegroundBitBlt 观察主窗口；空帧、纯色帧或前台校验失败时回退 WGC，不修改全局捕获设置。
- 独立登录窗口、ComboBox 和 ComboLBox 保持各自 HWND BitBlt 与实时客户区坐标。
- 新增轻量透明置顶状态窗，显示当前账号、阶段、详情、运行时间以及最新告警或错误；窗口点击穿透且优先放置在游戏捕获区域之外。
- 自动验证仅使用伪窗口、伪帧和 Qt 离屏测试，不启动或操作游戏。

## 1.21.00（PC 登录输入边界）

### 登录、退登与账号切换统一使用校验后的 SendInput

- PC 登录相关鼠标点击集中到独立 Win32 输入边界；每次投递前验证目标 HWND、可见/启用状态、游戏 PID、实际前台根窗口、虚拟桌面坐标、实时窗口矩形和 `WindowFromPoint` 命中关系，只有移动、按下、释放三个事件全部投递才视为 `delivered=True`。
- 强制前台使用目标窗口自己的 `GA_ROOT`，不会把独立 `#32770` 登录对话框折叠成游戏主窗口；`GA_ROOTOWNER` 仅用于命中窗口关系校验。
- 游戏主窗口和内嵌登录继续使用 WGC；独立 `#32770`、`ComboBox`、`ComboLBox` 使用各自 HWND 的客户区 BitBlt 和 `ClientToScreen` 原点，避免跨窗口混用 OCR 坐标，也不动态重绑 WGC。
- 打开账号列表、选择账号和点击登录不再回退 `PostMessage` 或固定坐标；输入投递与界面确认分开，列表未展开、账号未连续两次一致或登录界面未转换都会重新发现窗口、刷新 OCR 并按预算重试。
- 退登确认使用当前识别框，设置页按实际“退出登录/登出/Log Out”文字框点击；删除旧全桌面截图、固定退登坐标、`SetCursorPos` 和 `mouse_event` 路径。
- 通用 `BaseWWTask.wait_login()` 的 PC 公告、登录、隐私同意、更新确认、开始游戏和切换账号入口统一使用该输入边界；单账号每日任务自动退登复用正式多账号退登状态机。
- 多账号生产入口继续委托 `LoginFlowService`，目标已选时不重复点击，账号稳定确认仍保持 20 秒窗口与连续两次一致条件；账号切换测试继续复用正式 A1、A3、A4 流程及备用登录名、掩码手机号匹配。
- 新增伪 Win32 API、伪 OCR 和状态机测试，覆盖独立登录窗口、多显示器负原点、前台拒绝、PID/坐标/命中窗口错误和部分投递；验证过程不启动、聚焦或操作游戏。

## 1.20.02（候选实机验证通过）

### 适配登录器慢速账号选择核验

- A4 实机选择已正确投递，当前账号与目标也完全一致，但登录器窗口切换与两次 OCR 采样超过原 8 秒等待，旧逻辑在取得第一次正确结果后立即超时。
- 共享选择稳定窗口扩展为 20 秒，继续要求账号列表收起且目标连续两次一致；没有降低身份核验强度，也不会用盲目重复点击换取速度。
- 正式多账号每日任务与账号切换测试继续复用同一等待边界。
- 2026-08-31 隔离打包环境完成 A3→A4 连续 2 轮实测，两轮均完成稳定选择、登录、退登和后续切换，最终状态为“测试通过”；测试未执行每日任务、未写完成进度。
- 实测后复核打包版 326 项账号配置基线，缺失 0、变化 0、额外 0；候选 UI 已关闭，游戏停留在安全、可恢复的开放世界界面。
- 本地编译、版本一致性、差异检查和各测试组断言已通过；正式发布继续等待 GitHub 候选构件及隔离复验。
- GitHub Actions 候选 Run `33326613512` 已完成版本校验、完整测试、打包、包烟测和 SHA-256；下载构件的 5 项本地哈希及包烟测均通过。
- 全新隔离槽显式检出候选提交 `78e8eae` 后成功启动 1.20.02；无配置安全退出未生成总配置或账号工作配置。正式标签和 Release 仍未创建。

## 1.20.01（候选修复）

### 修复账号选择重复点击与状态栏身份泄露

- A3/A4 隔离候选实机验收确认：登录器当前已选账号与目标一致时，重复点击该列表项可能不会收起下拉框，旧逻辑会等待稳定状态超时并反复重选。
- 共享生产选择链路现在会在下拉列表收起且当前账号已精确匹配目标时跳过重复点击；多账号每日任务与账号切换测试共同生效。
- 账号切换测试和多账号完成状态只在 UI 显示 A3/A4 等短名；无法提取短名时统一显示“账号”，不再把旧账号标签、手机号、U…A 备用名或特征码写入任务详情。
- 增加当前目标已选时不打开列表、不发送点击，以及 UI 状态短名化的回归测试。

## 1.20.00（候选基线）

### 安全、可靠性与易用性综合修复

- 正在修复账号配置重复发布、启动入口完整性门禁、日志和诊断脱敏、五页导航及个人 GitHub 发布流水线。
- 正在补充多账号生产链路回归、故障注入和 A3/A4 打包版实机验收；全部门禁通过后再标记为正式发布。
- 本次修改不会主动改写实际账号配置；游戏内特征码继续只保存，不参与自动切换。
- 五个项目页面的内部导航统一回到顶层页面，启动、实时触发、一次性任务和计划任务不再把内嵌组件传给顶层切换器；恢复任务时也不会产生 `index -1`。
- 通用设置、账号设置、任务、活动和测试功能统一解除内嵌页面的滚动区所有权，由各顶层页面负责滚动和水平扩展，避免嵌套滚动及右侧空白。
- 测试入口改为显式分组的有序去重并集，支持模块不再被误执行；每个测试文件使用独立 Python 进程，图像测试不再共享已关闭的运行资源。
- 个人发布流水线移除原作者仓库同步、组织签名和第三方分发依赖，改为当前仓库内的版本校验、完整测试、未签名打包、包内容烟测、SHA-256 清单和 GitHub Release。
- 多账号登录编排从大型任务类移入共享 `LoginFlowService`，身份解析与登录后核验由选择/核验服务统一负责；正式多账号任务和账号切换测试继续复用同一等待、选号、重试、登录及回到主界面链路。
- 修复诊断任务未定义日志对象；协奏值异常日志增加 30 秒限速与重复计数；通用设置可直接更改或停用程序启停快捷键并显示冲突状态；Git 代理自愈只修改本程序拥有的 `http.proxy` 键，不再覆盖同节其他配置。
- 检查确认 PySide6 已由 `setup.py` 明确声明；本机虚拟环境仍存在来源不明的 `~k` 残留目录，按安全边界不直接删除，候选包构建前通过干净环境重建规避。
- 发布工作流支持手动生成经过测试、包内容烟测和 SHA-256 校验的候选 artifact；候选构建不会创建 GitHub Release，正式发布仍只接受 `v*` 标签。
- 配置完整性恢复测试显式使用 UTF-8 读取中文 JSON，消除 GitHub Windows 默认 cp1252 区域设置导致的候选构建误失败。
- 账号切换入口测试显式模拟运行时门禁通过，不再隐式依赖本机被忽略的真实 `configs`，干净 CI 环境也能验证测试任务复用生产入口。

## 1.19.12（2026-08-30）

### 修复多账号每日任务启动与重试

- 当账号下拉框不可见但队伍 HUD 明确可见时，正式账号切换入口会先执行安全退登，再等待登录界面；无法确认游戏状态时不会盲目发送 ESC 或点击。
- 多账号任务正常完成、手动停止和异常失败都会结束本轮协调状态；登录界面超时后可立即重试，不再残留“任务已经在运行”。
- 增加 A4、A3 权威序列以 A3 为当前执行账号时旋转为 A3 → A4 的回归测试，账号选择、身份核验和独立配置绑定继续复用现有生产链路。
- 修复配置备份校验错误排除发布目录内嵌套 `manifest.json` 的问题；只排除快照根清单，嵌套清单现在会被备份并验证。
- 未修改任何实际账号配置，也未纳入 MuMu/Android 未提交内容。

## 1.19.11（2026-08-29）

### 修复单人队伍自动战斗

- 角色加载按实际队伍人数（1～3）识别，不再强制读取第二名角色。
- 单人队伍现在会正常完成自动战斗初始化，并跳过无意义的队友切换。
- 增加单人队伍回归测试，双人和三人队伍逻辑保持不变。

## 本机部署记录（2026-08-26）

- 在当前 Windows 桌面创建 `OK-WW.lnk`，目标为项目根目录的 `启动okww.bat`。
- 快捷方式工作目录固定为项目根目录，启动脚本继续负责管理员权限检查、运行时修复和程序启动。
- 这是本机快捷方式部署，不修改程序代码和产品版本号。
- 账号配置备份以用户指定的实际运行目录 `E:\game\okww owener\data\apps\okww-custom\working` 为准；此前工作区备份不作为本次备份使用。

## 1.19.02（2026-08-26）

### 每日任务凝素领域名称

- 凝素领域选择改为整数值下拉框：第 1～4 项显示“陨翼云渊-武器及技能材料：迅刀”“静灭云渊-武器及技能材料：音感仪”“裂斩云渊-武器及技能材料：长刃”“碎蚀云渊-武器及技能材料：臂铠”。
- 第 5～20 项暂保留“第 N 个凝素领域（待命名）”占位，后续可继续补充游戏内名称。
- 保存格式仍为原来的整数 1～20，旧账号配置无需迁移，任务传送逻辑不变。

## 1.19.01（历史）

### 修复配置页面右侧空白

- 修复账号设置页将 `CustomTab.view` 直接重新挂载后仍由原 `QScrollArea` 调整几何尺寸的问题；重新嵌入前调用 `takeWidget()`，交由分区面板单一管理布局。
- `SectionPanel`、账号设置嵌入视图、账号配置和序列配置根容器统一采用水平 `QSizePolicy.Expanding`，五个主页面中的配置区块会随窗口可用宽度展开。
- 增加布局契约回归测试，验证分区内容扩展策略和滚动区解耦逻辑；不改变外层页面滚动与账号数据。

## 1.19.00（历史）

### 账号身份保护与显式重新绑定

- 普通账号任务配置编辑器锁定手机号、带星号手机号、昵称、U…A 备用识别名、游戏内特征码、别名和 UUID，避免误改切换身份。
- 新增“重新绑定身份”独立流程：校验当前身份、检查跨账号冲突、二次确认、账号级备份并通过修订号 CAS 发布。
- 账号配置页身份字段改为只读，身份变更不会混入普通任务配置保存。
- 新增 Windows DPAPI 本机备份加密服务和恢复路径遍历/符号链接边界校验；不支持 DPAPI 时拒绝回退为明文。
- 账号级备份保留兼容元数据 JSON，完整账号快照写入同名 `.dpapi` 加密旁车文件；无法加密敏感数据时拒绝写入。
- Windows 备份根目录默认应用当前用户、SYSTEM 和 Administrators ACL，恢复事务记录经过验证、激活和镜像阶段。
- 开始收敛 active bundle 运行时边界：新增 `AccountGraphStore`，为完整账号/序列图提供只读加载和 CAS 发布接口。
- 发布服务补充 `prepared/verified/activated/mirrored` 状态，镜像失败不会推进 active 指针。
- 账号仓库的账号、序列发布统一经过 `AccountGraphStore`，继续保留旧 JSON 兼容投影。
- 新增账号选择、序列快照、登录适配器、任务状态模型和停止协调器；账号切换测试继续复用生产任务方法。
- 新增统一脱敏与安全调用结果接口，以及账号筛选条、草稿脏状态、差异展示和单字段重置能力。
- 序列配置页的“当前序列包含的账号”改为按内容自动展开，隐藏内部滚动条，账号成员一次性平铺显示。
- 账号序列列表和每日任务配置方案列表同步改为内容自适应高度，避免配置区域出现嵌套滚动条。

## 1.18.00（历史）

### 账号配置与序列配置实时联动

- 账号配置、序列配置在成功保存、删除、重命名、排序、启停或导入恢复后，通过发布事件立即同步兄弟面板。
- 每日任务、多账号每日任务和账号切换测试的下拉选项在不重建任务页的情况下实时更新；当前选项仍可用时会保留，失效时安全回退。
- 运行中的任务继续使用启动时冻结的序列快照，配置变更只对下一次运行生效，避免中途改变目标账号。
- 新增 `AccountChangeEvent` 事件契约及账号/序列联动回归测试。

### MuMu V6.5.5 阶段01只读预检

- 增加 MuMu/ADB 设备预检契约，自动检测实际鸣潮包名、Android 属性、1280×720/240ppi 横屏条件和 Nemu IPC 截图。
- 增加 Combat Agent JAR/哈希/心跳检查边界；阶段01不会发送任何游戏输入。
- 增加 MuMuManager/ADB 的进程、注册表和默认目录定位；未启动实例缺少端口时仅展示推断 serial，不自动选择或连接。
- 增加 Nemu IPC 只读帧提供者；依赖或实例未就绪时按失败关闭。
- 固定后续 Android 战斗队伍元数据为 Augusta、Iuno、ShoreKeeper。
- 任务页移除运行日志常驻面板，并关闭游戏覆盖层日志；日志继续写入文件，避免诊断信息占用操作界面。

## 1.17.00（开发中）

### 账号配置安全与三层身份识别

- 增加独立的手机号、带星号脱敏手机号、昵称、U…A 备用识别名和游戏内特征码字段。
- 当前账号切换优先使用带星号的脱敏手机号，U…A 名称作为备用匹配。
- 游戏内特征码目前只保存和展示，不参与任务执行。
- 增加 UUID 隔离的账号文件存储和已发布账号快照的原子指针。

## 1.17.01（开发中）

### 任务读取已发布账号快照

- 任务运行时优先读取经过 SHA-256 清单校验的 `configs/published/active.json` 快照，避免读取总配置原子替换过程中的半写入内容。
- 已存在但校验失败的快照会进入安全错误，不会静默回退到可变总配置；编辑器仍通过发布服务生成新快照。

## 1.17.02（开发中）

### 正式任务接入 active 快照

- 每日任务、多账号每日任务和序列读取现在都会通过账号仓库读取已校验的 active 快照，不再优先使用启动时的完整性内存快照。
- active 快照不可用时保留既有完整性安全分支，禁止猜测或继续执行。

## 1.16.00（2026-08-26）

### 固定浅色 Codex UI 与五页平铺

- 桌面程序固定使用浅色 Codex 配色：窗口 `#F7F8FA`、面板 `#FFFFFF`、边框 `#E5E7EB`、主文字 `#1F2328`、强调色 `#0969DA`，不再跟随 Windows 深色模式。
- 左侧一级入口固定为“通用设置、账号设置、任务、活动、测试功能”；程序设置、关于、重启和调试继续放在底部辅助区。
- 通用设置在一个连续页面内平铺监控与启动、实时触发、游戏快捷键和全局行为；账号设置、活动页移除内部标签页，任务与测试入口使用统一区块壳层。
- 新增 `SectionPanel`、`FlatSettingRow` 和 `CodexTheme`，统一设置行、错误状态、日志面板、焦点态和滚动页面视觉；保留账号 CAS、备份、完整性、任务执行和账号切换测试接口。
- `悲鸣行动：无音危机` 继续由活动分类规则归入常驻活动；账号切换测试继续明确标记为多账号每日任务测试。

## 1.16.01（2026-08-26）

### 缺失账号总配置时安全启动

- 修复开发版或首次安装缺少 `configs/account_master_config.json` 时，账号配置页初始化直接抛出 `AccountRepositoryError`、导致 UI 启动后立即退出的问题。
- 账号仓库缺失/损坏时，账号配置页和序列配置页显示可恢复状态；完整性审查仍负责提示导入、恢复或首次锚定，不自动生成或覆盖账号数据。

## 1.14.03（2026-08-26）

### 账号与序列操作对象明确化

- 账号配置页按钮改为“删除当前账号”，页面说明明确该操作只针对当前账号。
- 序列配置页按钮改为“删除当前序列”，页面说明明确该操作删除整个序列。
- 序列成员区域增加“当前序列包含的账号”标题；“上移／下移”改为“上移账号／下移账号”，明确只调整成员顺序。
- 底层账号级联删除、序列删除和成员重排逻辑保持不变。

## 1.15.01（2026-08-26）

- 修复账号设置中新建序列或调整账号成员后，每日任务与多账号每日任务继续读取启动时旧序列快照的问题；现在优先读取账号仓库的最新投影。
- 任务页隐藏 `do_update_window_size changed` 这类窗口内部诊断日志，文件日志仍保留。

## 1.15.00（2026-08-26）

### 账号配置直接管理序列归属

- 账号配置页新增“所属序列”区域，勾选或取消勾选即可把当前账号加入或移出序列。
- 保存账号时，账号任务配置与序列成员变化通过同一修订检查和原子配置包发布提交，避免只更新一半。
- 未勾选的序列会移除当前账号，其他账号顺序保持不变；未知序列和重复序列会被拒绝。
- A1→序列2仅作为操作示例，不会在导入、启动或升级时自动改变任何现有账号。

## 发版流程（固定习惯，每次发版必须执行）

1. 改代码并自测（py_compile / 回归测试）；
2. 升版本号：`config.py` 的 `version` + `custom_ok/ok/gui/about/AboutTab.py` 加一条更新说明；
3. 更新本文档（新版本条目）；
4. `git commit`（消息带版本号，如 `...；v1.03.70`）；
5. **`git tag vX.YY.ZZ`**（必须！PyAppify 使用端靠 tag 接收更新）；
6. **`git push origin master` + `git push origin vX.YY.ZZ`**（commit 和 tag 都要推，缺 tag 则使用端收不到）；
7. 可选：GitHub API 建 Release（发布页可见）。

> 关键教训（2026-08-17）：只推 commit 不推 tag → 使用端 app.json 的 available_versions 一直收不到新版本。tag 与 commit 必须同时推送。

---

## 1.14.02（2026-08-25）

### 体力用途下拉框防误触

- 禁止在“体力用途”控件上通过鼠标滚轮切换无音区、凝素领域或模拟领域。
- 滚轮事件交还外层账号配置页面继续滚动；选项只能通过展开下拉菜单并主动点击来更改。
- 中文显示与英文内部存储值保持不变。

---

## 1.14.01（2026-08-25）

### 账号配置页中文显示

- 将账号配置表单中的副本类型、材料类型和梦魇目标改为中文显示，包括无音区、凝素领域、模拟领域、共鸣者经验、武器经验、贝币、梦魇拔除和残像聚落。
- 中文仅属于界面显示层；下拉框数据及保存后的 JSON 仍使用原有英文内部值，避免影响每日任务分支判断和目标识别。
- 列表和字典值使用递归可逆映射，未知值保持原样，避免未来新增配置被误改。
- 现有简体中文 gettext 目录已包含相关术语，本版本复用既有译文，不新增重复翻译条目。

---

## 1.14.00（2026-08-25）

### 五栏导航、账号级联删除与序列删除修复

- 左侧项目入口严格整合为“通用设置、账号设置、任务、活动、测试功能”五项；计划任务隐藏，程序设置保留在底部，游戏快捷键移入通用设置。
- 通用设置改为同一滚动页面内连续展示“监控与启动、实时触发、游戏快捷键、全局行为”，不再为这些分区创建左侧入口或内部页签。
- 账号设置整合账号配置、序列配置以及导入导出/备份/完整性维护；账号配置增加中文字段名、帮助说明和按类型选择的结构化控件，同时保留高级 JSON 兼容入口。
- 新增账号删除：两次确认后先备份，再从全部序列删除引用并清理账号运行状态；通过修订门禁、配置包原子发布、完整性后验检查和失败回滚防止半写入。修复真实 schema v1 发布时 manifest 分区哈希未重算的问题。
- 修复序列点击 Yes 后看似无反应：所有增删改操作统一显示进行中、成功或脱敏失败信息，成功后刷新并尽量保留选择。
- 任务和测试功能分别汇总到单页；“账号切换链路测试”明确归属多账号每日任务。活动页分限时与常驻，“悲鸣行动：无音危机”移入常驻活动。
- 未引入 Android、MuMu、ADB、设备绑定或封存自动战斗代码。

---

## 1.13.00（2026-08-25）

### PC 账号配置、独立序列与不可变运行快照

- 账号短名、备用名、完整/掩码手机号统一通过无副作用身份解析器处理；A1/A10 使用精确边界，歧义错误只显示安全短名，不回显完整手机号或备用登录身份。
- 新增“账号配置”页面。每次只加载一个独立草稿，账号 UUID 与登录身份字段锁定；保存前提供脱敏差异、账号短名确认、账号级备份和修订冲突检查，最终复用配置包的原子发布、工作投影重建及完整性后验检查。
- 新增“序列管理”页面与序列服务，支持新建、复制、重命名、启停、删除和成员重排；权威成员仍存于总配置并投影到 `daily_profiles.json`，不引入设备或模拟器规则。
- 正式多账号每日任务与 `TestAccountSwitchTask` 共用 `create_run_snapshot`。运行开始时一次解析账号并深冻结账号/任务内容，之后的页面或文件修改不会改变当前轮次。
- 增加每账号独立 JSON 目录的纯内存投影与往返评估。由于完整性指纹、配置包和回滚事务仍以单一 master 为原子边界，当前结论为 NO-GO；本版本不迁移真实配置。

---

## 1.08.01（2026-08-20）

### 启动器握手与完整性审查时序修复

- 主窗口首次 `showEvent` 先完成显示、隐藏启动器并发出 `start_success`，避免完整性对话框在主窗口创建阶段阻塞启动器并长期停留在“启动中”。
- 配置完整性及旧版遗漏序列恢复审查延迟到主窗口显示后的事件循环中；审查完成前不执行命令行任务、自动启动、自动任务或 `.okscript`。
- 审查失败保持安全模式并把主窗口置前；重复 show/定时回调不会重复运行启动任务。
- 新增启动顺序、审查阻断和重复回调回归测试。

---

## 1.08.00（2026-08-19）

### 账号配置包、遗漏序列恢复与切换失败证据

- 修复旧版本首次锚定只读取 `daily_profiles.json.sequences`、遗漏 `MultiAccountDailyTask.json` 序列归属的问题。首次锚定同时检查两种来源；单源采用、双源一致正常迁移，双源冲突或身份歧义时阻止并报告。
- 对“总配置合法但序列为空、旧多账号配置仍有序列”的升级状态增加启动提示和设置页恢复入口。确认后以稳定 UUID 恢复全部序列、空序列和成员顺序，事务失败时原子恢复总配置、工作副本和运行状态原始字节；已有非空总序列绝不被旧文件自动覆盖。
- 账号配置导出升级为可读 JSON v2 配置包，分离总配置、运行记录和非权威偏好并写入分区 SHA-256；导入先做纯内存预检和差异摘要，外部修改必须明确确认信任，随后完整替换、重建兼容副本、按 UUID 恢复运行记录并后验复检。旧 v1 扁平账号配置可转换为稳定 UUID schema，嵌入账号的旧完成时间迁移到运行分区；本机指纹、事件路径和导入标记不跨设备导出。
- 每日配置备份升级为整个 `configs` 的可验证快照，使用临时目录、清单和原子发布；导入、遗漏序列修复和恢复前另建事务快照。恢复预检展示总配置账号/序列摘要，复制后再次校验，最终完整性检查失败时立即恢复原目录；恢复日志路径受作用域限制。默认保留每日 30 份、事务 20 份、总量 2GB，按最旧完整快照清理。
- 正式多账号每日任务与账号切换测试统一调用 `switch_to_account`，共同使用登录界面稳定等待、目标 OCR、别名/掩码匹配、点击降级、账号复核、登录和主界面确认，正式任务不再维护第二套登录编排。
- 账号转换使用最近 60 秒、最多 30 帧的内存证据环；成功后直接丢弃。失败或安全停止时保存 JPEG、结构化事件、目标 OCR 框、实际/尝试点击点、窗口与屏幕坐标、交互方式及重试轮次，按 7 天、20 次事件、500MB 三重上限定时清理。
- 验证：配置完整性、v2 导入导出、备份、正式多账号任务、账号切换测试和失败证据聚焦 132 项通过，关联任务回归 36 项通过；运行端真实配置副本演练正确恢复 12 个账号及 4/8/0/0/0 序列，源文件哈希未改变。

---

## 1.07.00（2026-08-19）

### 旧版本账号配置首次锚定

- 当 `account_master_config.json` 不存在而旧 `daily_profiles.json` 合法时，完整性弹窗先展示迁移说明；只有用户明确确认，程序才把当前账号配置锚定为第一份 schema v1 总配置。关闭或拒绝后保持安全模式，不写配置。
- 复用合法已有 `profile_id`，缺失时生成并持久化稳定 UUID；保留已有任务值、账号顺序、备用身份、时间安排、扩展和序列成员顺序，缺失的 schema 必填值使用保守默认。
- 身份歧义、非法/空工作配置、未知序列成员、重复/非法 UUID 或损坏运行状态均阻止锚定并报告原因。
- 首次锚定将总配置、必要的工作副本补齐和已接受指纹作为一个事务；写入采用同目录临时文件、`fsync` 和原子替换，后验检查失败时删除新总配置并恢复工作配置与运行状态的原始字节。
- 旧 `last_completed` 会保留在工作副本并迁入 UUID 对应的运行状态；已有进度和未来运行字段原样保留。旧账号键与备用身份一并保留，避免登录识别信息丢失；事件目录保存迁移前备份和候选配置，成功时只留下 `RESOLVED_BY_LEGACY_BOOTSTRAP` 标记。
- 新增《账号总配置运行端 AI 详细说明》，覆盖 schema、首次迁移、外部修改、验证命令、差异恢复和安全禁令；账号完整性及切换相关 95 项、关联回归 36 项通过。

---

## 1.05.01（2026-08-18）

### 登录按钮真实点击与退登状态恢复

- 修复账号已正确选中，但登录按钮通过 PostMessage 投递后游戏界面不跳转的问题：首次仍保留后台投递，状态未变时刷新窗口句柄、置前游戏、重新 OCR 当前登录按钮，再用该 OCR 框换算的屏幕坐标进行系统鼠标点击。
- 实鼠标降级不使用固定坐标；未找到登录按钮、无法确认窗口已置前或无法安全换算 OCR 坐标时取消点击，且每次登录前继续复核目标账号。
- 退登改为状态感知循环：确认框滞留时直接重点确认，不再发送 ESC 把它关掉；设置页只点退登入口，主界面才发送 ESC，未知/加载态只等待重新识别。
- 可见状态的实际输入分别限制为最多 3 次，而游戏窗口转换/加载按 45 秒截止时间等待，避免正常的 10–12 秒退登加载被旧轮询次数提前终止。
- `TestAccountSwitchTask` 仍全量复用生产切换方法；新增 PostMessage 失效后重新 OCR/实鼠标点击、无安全坐标禁止点击、确认框不误按 ESC、长加载不提前失败及停止异常传播回归。
- 验证：账号切换、确认框、登录等待与序列调度定向 60 项通过。

---

## 1.05.00（2026-08-18）

### 多账号切换状态机与测试链路收敛

- 从游戏主界面启动时，先退登并在登录界面识别真实账号；只有账号方案唯一匹配且 DailyTask 联动成功后才执行每日任务，防止用错方案。
- 修复退登确认只等待 1 秒且不返回点击结果的问题；确认未命中、仍在菜单或 30 秒内未进入登录界面时，最多重做完整退登 3 次，不再原地空等。
- 选号和点击登录后都验证界面状态；目标不一致或点击被登录器忽略时重新选择/点击，达到有限上限才安全停止。
- 备用识别名贯穿登录就绪、列表展开、自动选号、当前账号和登录前复核，兼容旧 `Account Name`/`account_aliases` 字段；同一身份匹配多个方案时报明确歧义并禁止点击。
- 登录对话框优先检测后再恢复主窗口前台，并将 Win32 窗口捕获与点击限制在同一游戏进程，降低闪烁和误点其他对话框的风险。
- 补齐 `TaskDisabledException` 传播，点击停止后不再继续 OCR、置前或选号；回登失败时通知改为“需手动处理”，不再误报成功。
- 旧 `test_account_switch.py` 收敛为 `TestAccountSwitchTask` 兼容入口，删除独立 OCR、固定坐标和退登实现；连续 `A1 → A3 → A4` 测试继续复用生产选号、核对、重试、退登与登录方法。
- 验证：账号切换定向 54 项通过，Python 编译与 `git diff --check` 通过；全仓库运行 211 项，其中 29 项旧图像测试因现有测试框架提前抛出 `FinishedException` 仍失败，8 项跳过，与本次账号切换修改无关。

---

## 1.04.03（2026-08-18）

### 账号列表真实点击与停止响应修复

- 根据 record-and-replay 的人工操作证据，将可见 `ComboLBox` 客户区作为账号列表权威区域，排除灰色当前账号栏；通过目标账号掩码或备用名定位 OCR 框，并优先使用系统鼠标点击，不再依赖固定行号或容易被登录器忽略的 PostMessage。
- 使用 `ClientToScreen` 换算列表客户区坐标，兼容下拉列表向上或向下展开；标准 A1→A3→A4 连续序列会记录目标是否位于列表末项，但正式任务仍以目标身份为准，支持断点跳过账号。
- 系统点击前确认游戏窗口已置前、列表仍展开且目标框来自当前帧；无法获得安全屏幕坐标时才回退 PostMessage，点击后继续要求目标账号连续识别两次并在登录前复核。
- 显式传播 `TaskDisabledException`，修复点击停止后抗闪烁等待仍继续 OCR、置前窗口并执行后续账号的问题；真实鼠标点击前再次检查任务是否仍启用。
- 验证：账号切换相关 38 项测试通过，覆盖上下展开、当前账号重复、掩码/U 备用名、系统点击优先、PostMessage 后备、停止传播及 A1→A3→A4 共用生产流程。

---

## 1.04.02（2026-08-17）

### 登录器账号点击投递可靠性修复

- 主登录界面每次点击只使用一帧 OCR，同时匹配手机号掩码、U 扫码账号和备用识别名，并记录可见账号，避免两帧差异造成误点击。
- 日志区分“已发送点击”和“稳定确认”；首次 PostMessage 未确认后刷新 HwndWindow 句柄快照再投递，第二次仍未确认时仅在列表展开且目标 OCR 框仍可验证的前提下置前窗口并使用系统屏幕点击兜底。
- 兜底仍经过目标账号匹配和登录前 _confirm_target_before_login，并新增投递方式、目标 HWND/类和坐标诊断；正式多账号任务与账号切换测试继续共用生产选择/核对/重试路径。
- 验证：覆盖 A1 备用名 U570994311A → A3 手机号掩码 153****9621 的首次失败、句柄刷新重投失败和屏幕点击后连续稳定确认。

---

## 1.04.1（2026-08-17）

### 登录器账号切换抗闪烁稳定检测

- **根因**：游戏登录器点击账号后可能短暂保留下拉列表、返回空 OCR 帧，或先显示旧账号；原流程点击后只做一次 OCR，因瞬时旧帧误判账号不一致并立即重选。
- **稳定检测**：点击目标账号后等待列表收起，容忍空帧、旧账号和识别值变化；目标账号连续识别至少两次才确认成功。等待超时后才进入既有最多 5 次重选及最终防误登录停止逻辑。
- **正式/测试路径共享**：`MultiAccountDailyTask` 的正式每日任务和 `TestAccountSwitchTask` 的单账号/连续 A1→A3→A4 测试均复用同一套稳定检测、账号匹配、重试和登录核对方法。
- **验证**：新增登录器闪烁回归测试，覆盖列表收起、`None`/旧账号/目标账号变化，以及稳定错误账号超时后重选；通过单元测试、编译检查与 `git diff --check`。

## 1.04.0（2026-08-17）

### 账号切换重试统一 + A1→A3→A4 连续测试

- **防误登录行为修正**：选择后或点击登录前识别到的账号与目标不一致时，不再立即停止；改为重新展开账号列表、再次选择目标并复核。只有达到有限重试上限仍不一致时才安全停止。
- **正式链路统一**：多账号每日任务、应用内账号切换测试、独立验证脚本统一复用选账号、核对、重试和登录完成确认逻辑，避免测试通过但正式任务仍走旧实现。
- **连续切换测试**：账号切换测试新增「连续序列切换」，默认精确按 `A1 → A3 → A4` 执行；每个账号登录后模拟其每日任务已完成，仅执行退登和下一个账号的选择登录，不运行每日任务、不写完成进度。
- **备用名与短名安全**：A1 的备用识别名与 A3/A4 的手机号掩码均纳入回归；账号短名采用完整编号解析，防止 A1 误匹配 A10。
- **独立脚本可靠性**：适配当前嵌套方案结构与独立 `#32770` 登录对话框，补齐 ComboLBox 捕获、选择重试、登录完成确认与 GDI 资源释放。
- **验证**：账号切换/多账号定向测试 24 项通过；全仓库 31 个测试模块隔离运行全部通过（8 项依赖未纳入 Git 的本地图像样本，样本缺失时明确跳过）；Python 编译与 `git diff --check` 通过。

---

## 1.02.06（2026-08-17）

### 项目：ok-ww v1.03.74 —— 修复「点击下拉框无效果」误判卡死 + 多账号↔每日任务方案真正联动

依据运行端诊断报告(4)（v1.03.73 复测：登录界面识别成功，但卡在 `click drop down no effect` ×5 重试）修复：

- **根因1（卡死点）**：`_dialog_open_account_list()` 点击 ComboBox 后账号列表**已展开**（截图证据：5 个账号可见），但 `do_find_account_drop_down` 的命中条件把「存在任意 U 账号 + 登录文本」也判为命中 → 展开态被误判为「点击无效果」→ 反复重试永远走不到选账号步骤。
  - `do_find_account_drop_down` 改为「登录特征存在 且 至少 1 个账号条目（掩码或 U 账号）」即视为登录就绪（收起/展开态都命中）；
  - 新增 `_account_entry_count`（掩码/U 账号框计数，同文本不同位置各算一个）与 `_account_list_expanded`（账号条目 ≥2，或对话框 ComboLBox 可见且尺寸正常）；
  - `_select_and_login_account` / `_select_and_login_specific`：点下拉框后改为 `wait_until(_account_list_expanded)` 等待列表展开（10s），失败才报 `click drop down no effect` 并截图重试；列表已展开时跳过再次点击（防 ComboBox 切换成收起）。
- **根因2（联动矛盾）**：多账号任务与每日任务是两个独立 Config（各自 `configs/*.json`），此前 `self.config[DAILY_PROFILE] = target` 只改了多账号任务自身的配置，**DailyTask 实例执行/界面仍用旧方案**（v1.03.73 复测：联动 A4 却执行 A3，属侥幸正确）。
  - 新增 `_link_daily_profile(profile_name)`：调用 `DailyTask.switch_profile()`（保存旧方案 → 加载新方案配置 → 写 DAILY_PROFILE）并刷新任务卡片显示，三处调用点（主界面分支 / 登录界面分支 / 循环分支）统一改用它。
- 验证：py_compile 通过；TestMultiAccountDailyTask 新增 7 个下拉框收起/展开状态用例（9/10 通过，仅剩既有陈旧测试错误，与基线一致）。
- 发版：config.py 1.03.74 + AboutTab V1.03.74 行；commit + tag v1.03.74 + push master/tag + GitHub Release；同步部署 repo/working 与 app.json。

---

### 项目：ok-ww v1.03.73 —— 登录界面完整对话框支持（基于 v1.03.72 复测反馈）

依据运行端诊断报告(3)（v1.03.72 复测：阶段1 #32770 识别成功，但阶段2/账号操作仍基于主窗口帧失败；且 WGC 捕获 #32770 报 0x80070057）修复：

- **撤销框架捕获切换**：`custom_ok/.../windows_graphics.py` 的 `get_capture_hwnd` 恢复只捕获主窗口（WGC 无法捕获 #32770 对话框，CreateForWindow 0x80070057；修复该回归）。
- **登录界面完整对话框模式（MultiAccountDailyTask.py）**：
  - `do_find_account_drop_down`：主窗口帧无特征时回退 #32770 对话框帧，命中置 `_login_in_dialog=True`；
  - 新增 `_dialog_capture` / `_find_control_hwnd` / `_screen_click` / `_box_center_screen` / `_dialog_open_account_list`（点 ComboBox 展开列表）/ `_dialog_find_and_click_account`（ComboLBox 或对话框帧找掩码/U 账号并屏幕坐标点击）/ `_dialog_click_login`；
  - `_click_account_in_list`、`_click_specific_account`、`_detect_current_account_from_login` 支持对话框模式；
  - `_select_and_login_account` / `_select_and_login_specific`：对话框模式下开下拉框、点登录均用对话框帧 + 屏幕坐标（win32 点击）。
- 验证：py_compile 通过；TestMultiAccountDailyTask 与基线一致（仅剩既有陈旧测试错误）。
- 发版：config.py 1.03.73 + AboutTab V1.03.73 行；commit + tag v1.03.73 + push master/tag + GitHub Release；同步部署 repo/working 与 app.json。

---

## 1.02.04（2026-08-17）

### 项目：ok-ww v1.03.72 —— 登录界面捕获修复（独立 #32770 窗口）+ U 账号识别 + 启动器检测

依据运行端诊断报告(2)（决定性证据）修复「退登后找不到账号下拉框」的真正根因：

- **根因确认**：登录账号下拉框（ComboBox/ComboLBox）在**独立 #32770 登录对话框**（`Client-Win64-Shipping.exe` 的另一个顶层窗口）中；okww 捕获（WGC）只针对主窗口 `hwnd`（UnrealWindow），从不使用 `top_hwnd` → 登录界面不在捕获画面里。
- **框架修复（custom_ok 覆盖，启动时同步进 site-packages）**：`ok/device/capture_methods/windows_graphics.py` 的 `get_capture_hwnd()` 优先返回 `top_hwnd`（登录对话框），与 PostMessage 交互的目标选择（本来就优先 top_hwnd）保持一致。
- **U 账号识别**：登录账号以 U 扫码形式显示（`U550500484A`）时，`do_find_account_drop_down` 与 `_click_account_in_list` 均支持（掩码或 U 账号任一命中）。
- **启动器兜底**：退过头回到启动器（KURO GAMES 公告/修复，无登录特征）时明确报错并截图，不再 120s 空等。
- **对话框捕获兜底**：主窗口 OCR 无特征时，回退 BitBlt 捕获 #32770 对话框并 OCR（`_find_login_dialog` / `_capture_hwnd_client` / `_ocr_login_dialog`）。
- 验证：py_compile 通过；TestMultiAccountDailyTask 与基线一致（仅剩既有陈旧测试错误）；`_sync_custom_ok` 机制确认可同步框架覆盖。
- 发版：config.py 1.03.72 + AboutTab V1.03.72 行；commit + tag v1.03.72 + push master/tag + GitHub Release；同步部署 repo/working 与 app.json。

---

## 1.02.03（2026-08-17）

### 项目：ok-ww v1.03.71 —— 多账号每日任务抗闪烁加固 + 配置联动修复

依据运行端诊断报告（v1.03.68，登录界面闪烁导致退登后 60s 超时卡死）修复：

- **抗闪烁加固（`MultiAccountDailyTask.py`）**：
  - 新增 `_wait_login_screen_stable()`（两阶段）：阶段1 宽松探测（容忍暗屏/闪烁，120s，OCR 为空视为过渡不限次失败；窗口不可见时 `hwnd.bring_to_front()` 恢复前台；限频日志）；阶段2 严格确认（沿用恰好1掩码+登录文本 + settle）；
  - `_switch_to_login` 改用它，退登四步骤补 INFO 日志（esc/设置/确认/等待登录页）；
  - 失败诊断：超时前截图 + 日志输出窗口可见性/OCR 文本数/最近文本。
- **配置联动修复（主界面第一轮）**：`_run_inner` 的 `in_main` 分支在跑 DailyTask 前，先把 `DAILY_PROFILE`/`CURRENT_ACCOUNT` 联动到序列中第一个未完成账号（`_next_target_account()`），使每日任务配置与界面展示跟随执行账号（选 A2 用 A2 配置、显示 A2，切 A3 跟着切）；无方案账号保留当前配置并告警。
- 验证：py_compile 通过；TestMultiAccountDailyTask 结果与基线一致（3 用例 1 错，为既有测试桩问题）。
- 发版：config.py 1.03.71 + AboutTab V1.03.71 行；commit + tag v1.03.71 + push master/tag + GitHub Release；同步部署 repo/working 与 app.json。

---

## 1.02.02（2026-08-16）

### 项目：ok-ww 运行部署同步（E:\game\okww owener）

- **复核结论**：此前报告（G:\AI work\okww-custom）所述「配置方案切换失效」修复在开发仓库（E:\AI work\ok-wuthering-waves-master，v1.03.69 / 530152a）已完整落地；但**实际运行部署 `E:\game\okww owener\data\apps\okww-custom` 停在 v1.03.68（detached 4f86974）**，缺少 Bug2（tr_dict 同步）与启动守卫（修复点 2/3/5），仍存在原故障。
- **已执行**：
  1. 将 v1.03.69 的 **3 个文件**（`src/task/DailyTask.py` 修复、`config.py` 版本号 1.03.68→**1.03.69**、`custom_ok/ok/gui/about/AboutTab.py` 更新说明）同步到部署的 `repo` + `working`（py_compile 通过、两份一致）；
  2. 部署仓库建分支 `fix-1.03.69` 提交（**`7f3386c`**，3 文件 35 增 6 删，与开发 v1.03.69 提交范围一致）；
  3. 删除运行包根目录的 `创建8个账户.bat`、`创建账户说明.txt`（与开发仓库保持一致）。
- **待办**：
  - 推送 GitHub（`xihuojun2020-tech/okww-custom`）——沙箱无外网，需用户在机器上执行 `git -C "E:\AI work\ok-wuthering-waves-master" push origin master`（开发仓库 master 即 v1.03.69）；
  - ⚠️ 安全：开发仓库 git remote 内嵌明文 GitHub Token（`ghp_...`），建议轮换该 Token 并改用凭据管理器/SSH；
  - 部署仓库 master 分支停留在 v1.03.08（陈旧），后续建议统一分支管理。

---

## 1.02.01（2026-08-16）

### 项目：ok-ww 深度审查问题修复（C2 除外）

依据深度代码审查报告（subagent）修复以下问题（C2 自定义角色代码执行链按用户要求不修）：

- **I1 修复**：`main.py` 缺少 `import time`，导致「结束旧实例并重启」时 `time.sleep(1.5)` NameError、新实例静默退出——已补 import（重启功能恢复）。
- **I2 修复**：`KRLauncherSwitchTask.py` 三处 PowerShell 命令拼接路径未校验——新增 `_ps_arg()` 拒绝 `'`/反引号/分号等元字符，非法路径直接中止（防命令注入）。
- **I3 修复**：`_verify_daily_layout.py` 覆盖真实账号数据（daily_profiles.json）且失败不恢复——改为带时间戳备份 + `try/finally` 强制恢复，原文件不存在时清理测试产物（消除数据丢失风险）。
- **I4 修复**：`custom_ok/ok/gui/MainWindow.py` 启动参数任意 `.okscript` 直接导入执行——仅接受项目目录内的 `.okscript`，项目外一律拒绝。
- **I5 修复**：`MouseResetTask.py` 每 2ms 轮询（500Hz 占满核）→ 20Hz；删除无意义自赋值 `self.mouse_pos = self.mouse_pos`。
- **M1**：`main.py` 的 `.pth` import 行只放行白名单引导模块（pywin32_bootstrap / pywin32_postinstall），其余跳过。
- **M2**：`main_web.py` debug 默认关闭（环境变量 `OKWW_WEB_DEBUG=1` 可开）。
- **M3**：`WWOneTimeTask.py`、`KRLauncherSwitchTask.py` 的 `_screen_grab` 加 `try/finally`，保证 GDI 句柄（DC/Bitmap）释放。
- **M4**：`src/diagnose.py` 的 `C:\Users` 账户清单脱敏（首字符 + \*\*\*），防日志泄露账户列表。
- **M5**：`FiveToOneTask.py`、`FarmEchoTask.py`、`ChangeEchoTask.py` 对配置/OCR 输入的正则统一 `re.escape`（防 `re.error` 崩溃）。
- **M6**：`config.py calculate_pc_exe_path` 的 `parents[3]` 层级不足时回退注册表查找（防 IndexError）。
- **M7**：`clean_session.py` 的 `OKWW_KEEP_DAYS` 环境变量非数字时回退默认 7 天。
- **M8**：`MultiAccountDailyTask.py` 登录成功返回确定的目标账号（原返回 OCR 识别值可能为 None，导致剩余账号被跳过）。
- **M9**：`LabelAndMultiSelection.py` 的 `tr_dict` 用 `.get()` 兜底（防 KeyError）。
- **M11**：`register_cleanup.bat` 加管理员检测与自动提权。
- **M12**：`windows_messenger.py` 发送内容记日志（截断 200 字符，便于审计）。
- 验证：16 个改动文件全部通过 `py_compile` 语法校验。
- **回归测试**：`run_tests.ps1` + 全量 tests 逐个运行，与「git stash 还原到改动前」的基线对比——失败项完全一致（TestChar 缺 `ok_templates/char_iuno.png`、TestCombatCheck/TestForte/TestMergeEchoTask/TestMultiAccountDailyTask/TestNightmareNestTask/TestScheduleSupport 等为既有环境/资源问题），**本次 17 处改动零回归**。
- 未修（用户确认）：**C2** 自定义角色代码以管理员权限执行（保持现状，仅提示只运行可信代码）。

---

## 1.01.01（2026-08-16）

### 项目：ok-ww（鸣潮自动化）安全检查与清理

- 检查范围：src/、custom_ok/、根目录脚本与 bat；语法检查（compileall）全部通过；config.py 无硬编码密钥；`exec()` 多为 Qt 对话框误报，真正的 `exec(code)` 来自 ok-script 框架的自定义角色代码功能（设计如此）。
- **删除两个一次性工具脚本**（用户确认）：
  - `创建8个账户.bat`（创建 ww1~ww8 本地管理员账户、密码 123456；全项目确认仅此一文件涉及这些账户）
  - `鼠标坐标查看器.bat`（配套 `mouse_pos.py` 的启动脚本；已确认无其他引用）
- 遗留说明：已创建的 ww1~ww8 账户不随文件删除而消失（用户选择保留）；`mouse_pos.py` 仍在根目录（已无启动入口，是否删除待用户确认）。
- 待办/风险备忘：
  - 自定义角色代码 = 以管理员权限执行任意代码（只运行可信代码）
  - 根目录存在一次性文件（_test_*.py、probe_*、debug.log、okww_更新包_*.zip、backup_合并前_0815/ 等），建议后续归档
